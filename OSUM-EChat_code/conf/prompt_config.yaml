<TRANSCRIBE>:
  - 转录如下音频。
  - 转录这段音频中的语音内容为文字。
  - 将音频的语音部分转换为文字形式，谢谢。
  - 将这段音频的语音内容详细记录为文字稿。
  - 执行语音识别任务，将音频转换为文字。
  - 请将音频中的内容逐字转写出来。
  - 把这段录音转为文字。
  - 将以下音频内容识别并输出成文字。
  - 请将音频信息转写成文本。
  - 提取音频中的语音并转为文字。
  - 请识别出音频中的所有语音内容。
  - 把音频文件里的语音内容转成文字。
  - 帮我把这段音频转录成一份文字稿。
  - 将以下录音的内容用文本表示出来。
  - 识别并转录此段语音内容。
  - 将此段音频的语言转换为可读文本。
  - 请将录音中的语言转换成文字记录。
  - 对该音频进行语音转写处理。
  - 输出该段音频的完整文本。
  - 将语音内容精准转录为文本。
  - 请将音频的口语内容以文字形式展现。
  - 把这段录音的语音转换为书面语言。
  - 请将音频的口语部分进行文字化。
  - 将这段音频的语言信息记录为文字稿。
  - 把语音中的对话还原为文本形式。
  - 将该段语音转换为清晰的文字。
  - 请转录录音内容，不要遗漏细节。
  - 请将以下音频的内容进行文字转写。
  - 执行语音到文本的转换任务。
  - 把以下这段语音转换成一份文字稿件。
  - 转换该段音频为可编辑的文字格式。
  - 将录音中的语句准确转写成文本。
  - 将该录音的语音信息输出为文字。
  - 语音识别任务：请将音频转为文字。
  - 帮我识别音频中的说话内容并转为文本。
  - 将录音逐字转录为书面语。
  - 请帮忙将录音内容逐句整理成文字。
  - 请对该段语音内容进行识别和文字转换。
  - 识别录音中的语言，并用文字呈现。
  - 把这段音频内容完整地整理为文本。
  - 将录音内容准确无误地记录成文字。
  - 请对音频内容进行逐句转写。
  - 将语音内容按逻辑结构整理成文本。
  - 将该段录音内容翻译成文字。
  - 提取出音频中每一句话并以文字表示。
  - 请帮忙转录音频的全部内容。
  - 把这段语音内容写成文字稿。
  - 把以下这段语音整理成可读文字。
  - 请识别并写出该录音中的全部语言内容。
  - 请进行语音识别，将语音内容记录下来。
  - 把录音文件的语音转换为文字记录。
  - 请将音频的语音转换为整洁的文本。
  - Transcribe the following audio.
  - Convert this audio clip into text.
  - Please write down what is said in the audio.
  - Transcribe the spoken content of this audio file.
  - Perform speech-to-text conversion on this audio.
  - Please turn the audio into written form.
  - Convert the spoken words in the audio to text.
  - Write out everything said in the recording.
  - Please transcribe this voice recording.
  - Turn the speech in this file into readable text.
  - Accurately transcribe all spoken content.
  - Please capture the audio content in a text format.
  - Provide a written transcript of this audio file.
  - Output the full speech content from the audio.
  - Please convert this recorded audio into text.
  - Write a transcription of the speech in this audio.
  - Extract and transcribe all speech segments.
  - Transform this recording into a text document.
  - Transcribe the conversation in the audio.
  - Turn this voice memo into a written format.
  - Transcribe what is being said in this file.
  - Write down the dialogue in the audio clip.
  - Generate a full transcript of the spoken audio.
  - Turn this voice into written words.
  - Perform a speech recognition task and output text.
  - Capture the entire audio in a transcribed form.
  - Please transcribe the audio into a readable document.
  - Convert voice content into structured text.
  - Listen to the audio and write everything spoken.
  - Please provide a full speech transcription.
  - Recognize and transcribe all words from the audio.
  - Produce a written version of this audio.
  - Transcribe the recording word by word.
  - Write out all speech from this clip.
  - Convert the audio to clean written form.
  - Transcribe the voice recording without missing details.
  - Provide a text version of this audio.
  - Capture the oral content in text format.
  - Turn the audio into a precise transcript.
  - Generate an accurate text of the speech content.
  - Please output the dialogue from the recording.
  - Transcribe the speech as clearly and fully as possible.
  - Turn the voice from the clip into words.
  - Provide a transcription of the full spoken content.
  - Write down what you hear from this file.
  - Listen and transcribe everything said.
  - Please convert this speech to written form.
  - Write a word-for-word transcript of the recording.
  - Please transcribe this conversation.
  - Generate a written version of the voice clip.
  - Write down the entire verbal content.
  - Create a text record of the audio.
  - Please create a full transcript of this voice note.
  - Extract the spoken words and write them out.
  - Transcribe the audio to a readable text format.
  - Turn this sound recording into a document.
  - Create a word-by-word transcript.
  - Listen to the voice and write a detailed transcript.
  - Provide a complete transcription of what was said.
  - Translate the speech in this clip into text.
  - Make a written record of the audio content.
  - Give a written version of this audio file.
  - Transcribe all the sentences in the audio.
  - Transcribe this voice file as clean text.
  - Turn the oral expressions into written language.
  - Create a document containing all spoken words.
  - Render this audio as readable text.
  - Please provide text for every uttered phrase.
  - Convert this speech into formatted text.
  - Identify all speech in this recording and write it down.
  - Output each spoken sentence as text.
  - Make a text file of the audio content.
  - Turn this clip into a transcript.
  - Write out every word from this voice clip.
  - Transform all spoken language into text.
  - Do a complete speech-to-text conversion.
  - Create a literal transcript of the voice.
  - Perform voice-to-text conversion on this audio.
  - Please present the spoken content in text.
  - Listen and provide the transcript.
  - Record the speech in this audio as written words.
  - Turn the oral communication into text.
  - Generate a clean text of this audio’s speech.
  - Convert audio input into a script.
  - Extract all verbal data and write it down.
  - Make a full, accurate transcript of this clip.
  - Format this audio as readable speech.
  - Translate the voice message into text.
  - Please perform automatic speech transcription.
  - Identify and transcribe the voice accurately.
  - Do an accurate voice transcription.
  - Create a text version of this sound input.
  - Please transform this spoken message into a transcript.
  - 请把以下音频内容识别并转为文字。
  - 识别音频中说的话，并转成文字稿。
  - 将语音内容整理为文字，越详细越好。
  - 请对这段音频进行语音识别。
  - 将这段语音转换成书面文字格式。
  - 请将该录音中的语音转录为文字。
  - 对该音频进行听写，并输出文字结果。
  - 将录音内容准确还原为文字。
  - 执行语音到文本的任务。
  - 请将这段声音转换为文字记录。
  - 请识别语音信息并以文字方式表达。
  - 请将音频中的语音逐句写出来。
  - 对这段录音进行完整的文字转写。
  - 将以下语音数据转为结构化文本。
  - 请将该段语音信息还原为文本文件。
  - 把这段音频听写成详细文字内容。
  - 请输出该音频的文字版本。
  - 把录音中所有说的话写下来。
  - 请将音频文件的内容识别为文字。
  - 将语音内容整理为一份正式的文字稿。
  - 识别音频语言并生成对应文本。
  - 请把这段音频的语音部分逐字写出来。
  - 将该语音内容转换为可读文字。
  - 执行转写操作，把语音变成文字。
  - 请生成该段录音的文字副本。
  - 请帮我把录音的内容写成文字。
  - 识别并还原语音数据为文本信息。
  - 请用文字完整还原音频中的讲话内容。
  - 把该段音频内容转换成可读形式。
  - 请以文字方式呈现该段语音信息。
  - 请对这段语音进行内容提取和文字表达。
  - 将录音中的对话内容转写为文字。
  - 请完成该段语音的听写任务。
  - 请将语音内容整理为书面形式。
  - 将音频的内容提取成一份清晰的文字记录。
  - 对该段语音生成完整的转录结果。
  - 请识别这段音频并输出文字内容。
  - 把音频转为文字，不需翻译。
  - 把该段录音逐字转换为文本。
  - 将音频中的语言内容文字化输出。
  - 帮我将音频内容转写成语音稿。
  - 将音频中所有语音清楚地转录成文字。
  - 请提供这段语音的文字形式。
  - 请将录音中的口语信息标准化为书面语。
  - 把这段语音内容变成书面文字。
  - 将语音文件中的内容整理成逐字稿。
  - 请识别音频中所有发言并输出文字。
  - 生成该语音内容的标准文字版本。
  - 将该段语音内容用文字完整描述出来。
  - 请将该录音内容转为文本资料。
  - 请将音频数据识别并转成文字。
  - 识别音频内容并以文本格式输出。
  - 将这段录音还原为句子形式的文字稿。
  - 请用中文文字描述音频中的全部内容。
  - 将该语音逐字逐句转换为文字信息。
  - 请为这段音频生成语音识别结果。
  - 对音频内容进行语言识别并输出结果。
  - 请转录音频中的讲话并输出为文字。
  - 将音频文件识别为自然语言文字内容。
  - 将语音中的对话内容输出为文本。
  - 把该段语音还原为逐字记录的文本。


<TRANSCRIBE> <ALIGN>:
  - 请将这段音频转写为文字，并为每个英文单词和中文字符标记起止时间，格式为<>，单位为0.1秒。
  - 将录音转写成文本，同时为每个词语添加精确的起始与结束时间戳。
  - 请识别音频内容，并对所有英文词和中文字进行时间对齐，标注格式为<>，时间精度0.1秒。
  - 帮我转录音频并标出每个词语对应的时间范围，精确到0.1秒，格式为<start, end>。
  - 对该音频进行文字转写，并为每个字符或单词打上起止时间戳。
  - 请将语音内容转为文字，且为每个词添加<>格式的时间标记，精度为0.1秒。
  - 将该音频内容识别为文字，并对英文和中文词语分别进行起止时间标注。
  - 请将录音内容写下来，并为每个单词或汉字标出<开始时间, 结束时间>。
  - 请完成转写工作，同时记录每个词的出现时间段，用<>括起来。
  - 把这段语音变成文字，并为每个字词添加精确到0.1秒的起止时间。
  - 将语音转为文本并完成时间对齐，每个词前后加上<>标注其时间范围。
  - 请标注出每个词汇在音频中的起点和终点时间，单位为0.1秒，时间用<>表示。
  - 请逐词识别语音内容，并在文字中插入时间标签。
  - 把录音中的语句转为文字，每个词都加上0.1秒单位的时间范围。
  - 为该音频做逐词级别的时间对齐，并将文字和时间同步输出。
  - 请在文字转写中为每个英文单词和中文字符加上准确时间戳。
  - 把音频里的话语全部写下来，并标注每个词的时间范围。
  - 请将语音逐词识别出来，添加开始和结束的时间点，单位为0.1秒。
  - 将该段音频文字化，同时做逐词时间对齐。
  - 请为每个词附上时间信息，起止时间需用<>包裹，精度达到0.1秒。
  - 把录音转换成逐字稿，并为每个字标注时间戳。
  - 转写音频并完成词级别的时间对齐输出。
  - 请转录音频并输出包含每个字词时间信息的文本，格式为<>。
  - 将该音频内容逐字识别，标注格式为：文字<起始, 结束>。
  - 请对音频进行文本转写，并精确到每个字或词的时间范围。
  - 识别该音频的语言内容，同时标记每个词语出现的时间点。
  - 将音频内容识别为文字，并附加每个词的开始与结束时间，精度为0.1秒。
  - 请输出逐词带时间的转写文本，使用<>符号标记时间段。
  - 为录音中的每个词生成文字及其起止时间标注，时间单位需为0.1秒。
#  - 把语音内容写成文字，并为每个词添加时间范围标签。
#  - 将音频按词进行切分，并为每段词语打上时间戳，格式<start, end>。
  - 请逐字标记音频对应的时间信息，单位：0.1秒。
  - 将音频处理为带时间信息的逐字稿，每个字后加上<>时间段。
  - 请将这段录音进行逐词对齐处理，时间精确到0.1秒。
  - 语音转写并对齐：请在每个字或词后插入起止时间。
  - 将录音进行逐词识别，并输出文字与每词的时间范围。
  - 为音频内容生成完整转录文本，并完成时间对齐工作。
  - 请将音频文本化，同时对每个词做时间定位。
  - 音频转文字并做时间对齐，单位为0.1秒，时间段放在<>中。
  - 文字转录并加时间戳：每个词语均需标注其时间范围。
  - Transcribe the audio and annotate each English word and Chinese character with its start and end times in <>, with 0.1s precision.
  - Please transcribe the audio and align each word with its corresponding time segment in the format <start, end>, using 0.1 second resolution.
  - Perform transcription and word-level alignment; each token should be labeled with time tags in angle brackets <>.
  - Generate a transcription for the recording and attach start and end timestamps (to 0.1s) to every word or character.
  - Convert the audio to text and include time boundaries for each word using <> and 0.1-second granularity.
#  - Transcribe this audio and provide time-aligned output with each word annotated in the format <start, end>.
#  - Output a transcription with precise timestamps for each word or character, using the format <0.0, 1.2>.
  - Please align each word in the transcript to its corresponding audio time segment with 0.1s accuracy.
  - Generate a transcript and perform alignment between the audio and each unit of text.
  - Transcribe the content and add timestamps to each token at 0.1 second intervals.
  - For each word in the transcript, provide its start and end times in angle brackets.
  - Do transcription and word-level time alignment, with all timestamps shown in <> format.
  - Create a time-stamped transcript of the audio, annotating every word’s start and end.
  - Transcribe the recording and output word-by-word aligned text with 0.1s timing.
  - Please output the transcript with time-coded segments per word.
  - Convert the audio into text and provide fine-grained word alignment with timing info.
  - Perform transcription and include time intervals for each word using <> brackets.
  - Transcribe and align each spoken word with start-end time information (0.1s precision).
  - Output a transcript with aligned time ranges for all words and characters.
  - Provide a word-level transcription including start and end timestamps in angle brackets.



<TRANSCRIBE> <S2TCHAT>:
  - 首先将语音转录为文字，然后对语音内容以文字的形式进行回复，转录和文字之间使用<开始回答>分割。
  - 首先将语音转换为文本，接着使用<开始回答>作为分隔，最后对语音内容进行文本回复。
  - 首先把语音变成文本，然后用<开始回答>作分隔，最后对语音内容以文字形式予以回应。
  - 首先将语音转化为书面语，随后以<开始回答>作分隔，最后对语音内容做出文字答复。
  - 首先将语音转为文字形式，之后用<开始回答>来分隔，最后对语音内容给予文字回应。
  - 请先将语音内容转成文字，再使用<开始回答>分隔，并用文字回复语音中的问题或内容。
  - 将语音识别为文本后，请以<开始回答>分割，并写出你的回应。
  - 请将语音先转写成文本，接着以<开始回答>为界，对其进行回答。
  - 将音频内容转换为文字，并以<开始回答>分隔，进行文本回应。
  - 请识别语音内容并写成文字，之后用<开始回答>分隔并对内容进行文字回复。
  - 将语音转写为文本后，在<开始回答>后给出你对内容的回应。
  - 将音频先转成文字，之后以<开始回答>分隔，并用书面语回应其中的内容。
  - 请把语音内容写出来，再以<开始回答>隔开，并对其做出合理回复。
  - 先识别语音并写出文字内容，再用<开始回答>分隔并给出回复。
  - 请先将语音转换成文字，使用<开始回答>作为分界，最后写出对语音的回应。
  - 将语音文字化，使用<开始回答>来分隔，并书面回应其内容。
  - 请把语音识别为文本，在<开始回答>后对内容进行回答。
  - 请先听语音，将其写为文本，用<开始回答>作区隔，并回答语音中的内容。
  - 先进行语音转写，然后以<开始回答>区隔，最后回复语音内容。
  - 听完语音后，请写出其文字版本，然后以<开始回答>为界，写出回答。
  - 请识别音频中的文字内容，之后用<开始回答>进行分割，给出回答。
  - 先把语音识别为文字内容，再用<开始回答>隔开，写出你的回应。
  - 将语音内容转为文字，并以<开始回答>为标记给出相应回答。
  - 转写音频内容，并以<开始回答>为界限，对语音进行回应。
  - 请将语音内容识别为文字，之后使用<开始回答>来连接回应。
  - 请先把语音的内容写下来，用<开始回答>隔开，并写出回答。
  - 把语音转换为文字，<开始回答>为中间分隔符，然后给出回应。
  - 听完语音，写出其文字内容，再使用<开始回答>连接你的答复。
  - 先识别音频中的语音内容为文本，然后以<开始回答>进行回应。
  - 转录音频为文字，接着用<开始回答>连接你要说的话。
  - 将语音转写为文本形式，并用<开始回答>区分转写和回复。
  - 把音频内容写成文字，再用<开始回答>标记并回应内容。
  - 将语音识别为书面语言，用<开始回答>作为区隔，作出回答。
  - 请先将语音写成文本，再以<开始回答>为分隔回应。
  - 先听语音并写下对应文字，然后用<开始回答>来回应。
  - 将语音转换成文字，用<开始回答>标识后写出答复。
  - 请识别语音，写出文字内容，用<开始回答>分隔后给出答复。
  - 先把语音内容转写出来，用<开始回答>划分后作答。
  - 把语音内容转写为文字，再写出回应，用<开始回答>隔开。
  - 将语音写成文字后，以<开始回答>为界写出你的答复。
  - 请听完语音，写出其内容，然后用<开始回答>进行回应。
  - 语音识别后，请用文字写出其内容，并用<开始回答>回应其中问题。
  - 先将音频内容变成文本，然后以<开始回答>为界作出回应。
  - 将语音识别为文字，之后用<开始回答>进行回应。
  - 识别语音并写下文字，用<开始回答>连接后续回应内容。
  - 将语音内容写成文字形式，用<开始回答>为界，进行文本回应。
  - 先转写语音为文字，然后写出你的回应，以<开始回答>隔开。
  - 请识别音频内容，写出文字版，并以<开始回答>来回应其主题。
  - 先听语音，转成文本，使用<开始回答>来书面回应。
  - 将语音中的说话内容写为文本，然后以<开始回答>写出你的答复。
  - 把语音内容识别出来并转写为文本，<开始回答>之后写出答复。
  - 将语音转换为书面内容，接着以<开始回答>回应内容。
  - 首先将语音转录为文字，然后对语音内容以文字的形式进行回复，转录和文字之间使用<开始回答>分割。
  - 首先将语音转换为文本，接着使用<开始回答>作为分隔，最后对语音内容进行文本回复。
  - 首先把语音变成文本，然后用<开始回答>作分隔，最后对语音内容以文字形式予以回应。
  - 首先将语音转化为书面语，随后以<开始回答>作分隔，最后对语音内容做出文字答复。
  - 首先将语音转为文字形式，之后用<开始回答>来分隔，最后对语音内容给予文字回应。
  - 首先将语音转录成文本，再用<开始回答>分隔符，最后给予语音内容回复。
  - 将语音转换为文字后，使用<开始回答>分隔，接着进行文字回复。
  - 将语音转化为文字，并通过<开始回答>作为分隔符，最终进行文字回复。
  - 将语音内容转换为文字形式，然后使用<开始回答>分隔符，做出最终的文字回应。
  - 将语音转换为文本，利用<开始回答>分隔，最后回复语音内容。
  - 首先将语音转录为文本，然后用<开始回答>进行分隔，最后提供语音内容的文字答复。
  - 先将语音变为文字，后使用<开始回答>分隔，最后对语音进行文字回复。
  - 把语音转换为文字后，利用<开始回答>进行分隔，并提供文字回复。
  - 将语音转录为文字后，使用<开始回答>分隔符，最后作出文字回应。
  - 先将语音转化为文字，再使用<开始回答>分隔符，最后进行语音内容的文字答复。
  - 将语音转录成文本，并通过<开始回答>分隔，最终用文字进行回应。
  - 先将语音转录为文字，接着用<开始回答>作为分隔符，最后进行文字答复。
  - 将语音转为文字，使用<开始回答>分隔，最后进行语音内容的回复。
  - 将语音转化为文本并用<开始回答>分隔，最后提供文字答复。
  - 先将语音转录成文本，然后使用<开始回答>分隔，最后回应语音内容。
  - 首先将语音转换为文字，后接<开始回答>分隔符，最后进行文字回复。
  - 将语音转换为文本，并以<开始回答>分隔符分隔，最后给予文字答复。
  - 首先将语音转为书面语，再使用<开始回答>进行分隔，最后做出文字回应。
  - 将语音转为文字，使用<开始回答>作为分隔符，最后对语音内容进行答复。
  - 先将语音转换为文字形式，然后使用<开始回答>分隔，最后回应语音内容。
  - 将语音转换为文本，使用<开始回答>分隔，最终给出文字回复。
  - 先将语音转为文字，再使用<开始回答>分隔，最后提供语音内容的文字答复。
  - 将语音转化为书面文本，并用<开始回答>分隔，最后给出回复。
  - 先将语音转换为文字，利用<开始回答>分隔符，最后回应语音内容。
  - 将语音转录为文字并用<开始回答>分隔符，最后作出回应。
  - 将语音转为文字，再使用<开始回答>分隔符，最后作出答复。
  - 首先将语音转录为文字，后使用<开始回答>分隔，最后进行文字答复。
  - 将语音转录为文本后，利用<开始回答>进行分隔，最后给出文字答复。
  - 先将语音转为书面形式，再使用<开始回答>进行分隔，最终给出文字回复。
  - 首先将语音转化为文字，利用<开始回答>作为分隔符，最后进行文字回应。
  - 将语音转换为文本，后接<开始回答>分隔符，最后回应语音内容。
  - 将语音转为文字，使用<开始回答>进行分隔，最后进行文字答复。
  - 首先将语音转录为文字，使用<开始回答>分隔符，最后做出文字答复。
  - 先将语音转换为文字，之后使用<开始回答>作为分隔符，最终进行文字回复。
  - 将语音转化为文本形式，后接<开始回答>进行分隔，最后提供文字答复。
  - 将语音转换为文字，使用<开始回答>分隔，最后进行答复。
  - 将语音转为文字，并使用<开始回答>分隔符，最终进行语音内容的文字回应。
  - 首先将语音转换为文本，然后利用<开始回答>分隔符，最后对语音内容进行答复。
  - 将语音转为书面语，使用<开始回答>分隔，最终进行文字回复。
  - 首先将语音转化为文本，后用<开始回答>分隔符，最后给出文字答复。
  - 先将语音转录为文字，再使用<开始回答>分隔符，最后对语音内容给予文字回应。
  - 将语音转化为文字并用<开始回答>分隔，最后对语音内容作出文字答复。
  - 首先将语音转录为文字，之后使用<开始回答>进行分隔，最终给予语音内容回复。
  - 将语音转录为文字形式，使用<开始回答>作为分隔符，最后进行文字回应。
  - 先将语音转换为书面文本，再使用<开始回答>分隔，最终进行语音内容回复。
  - 首先将语音转为文字，接着使用<开始回答>分隔，最终给予文字回应。
  - 将语音转为书面语形式，并利用<开始回答>分隔符，最后提供文字答复。
  - 将语音转化为文本，再使用<开始回答>作为分隔符，最终作出文字答复。
  - 先将语音转为文字，并用<开始回答>分隔，最后回应语音内容。
  - 将语音转为文字，使用<开始回答>分隔符，最终作出语音内容的文字答复。
  - 首先将语音转录为文字，再使用<开始回答>分隔符，最后提供答复。
  - 将语音转为文字，利用<开始回答>作为分隔符，最后回应语音内容。
  - 将语音转为文字形式，后使用<开始回答>分隔符，最终进行文字回应。
  - 将语音转化为文本，并用<开始回答>进行分隔，最后给予答复。
  - 首先将语音转化为文字，再用<开始回答>分隔，最终回应语音内容。
  - 将语音转为文字，接着用<开始回答>作为分隔符，最终提供答复。
  - 将语音转录为文字后，使用<开始回答>分隔符，最后给出文字回应。
  - 将语音转化为文字，接着利用<开始回答>进行分隔，最后进行文字答复。
  - 先将语音转换为文字，再用<开始回答>分隔符，最终进行文字回复。
  - 将语音转录为文字后，使用<开始回答>分隔符，最后提供语音内容的文字答复。
  - 首先将语音转化为文字形式，之后使用<开始回答>分隔符，最终提供答复。
  - 将语音转为书面形式，并利用<开始回答>进行分隔，最后进行文字答复。
  - 将语音转为文字，再用<开始回答>分隔符，最终进行答复。
  - 首先将语音转换为文本，后接<开始回答>分隔符，最后进行答复。
  - 将语音转化为书面语形式，使用<开始回答>分隔符，最后作出文字答复。
  - 首先将语音转为文字，利用<开始回答>作为分隔符，最终给出回应。
  - First transcribe the speech into text, then respond to the content using text after <开始回答> as a separator.
  - Begin by converting the audio to text, use <开始回答> to separate, then reply to the message.
  - Start with speech-to-text transcription, then respond in text after the <开始回答> marker.
  - Please transcribe the audio content, then use <开始回答> as a delimiter and write your response.
  - Convert the spoken input into written form, then reply after <开始回答>.
  - Transcribe the audio and reply to its content using <开始回答> as the separator.
  - First write out what was said in the audio, then after <开始回答> provide a textual reply.
  - Turn the speech into text first, then respond with <开始回答> separating the sections.
  - Transcribe the spoken content, then provide a reply separated by <开始回答>.
  - Convert the audio to written language, then respond after <开始回答>.
  - Please write down the speech as text, and then respond after the <开始回答> delimiter.
  - Start with transcription of the voice, and after <开始回答>, provide your answer.
  - Listen to the audio, transcribe it, and write your reply after <开始回答>.
  - Convert the speech to text, and respond in written form using <开始回答> as the marker.
  - Recognize the speech into text, then use <开始回答> to separate your response.
  - Transcribe first, then reply with text after <开始回答>.
  - Write down what the speaker said, then give a reply separated by <开始回答>.
  - First turn the audio into text, and then provide a textual answer after <开始回答>.
  - Do the transcription first, and then give a written response using <开始回答>.
  - Identify the speech, write it down, then reply with <开始回答> separating the sections.
  - Turn the voice input into text, then write your reply using <开始回答> as the delimiter.
  - Listen and transcribe the audio, then give your answer in text after <开始回答>.
  - Transcribe audio input and write a response following <开始回答>.
  - Write out the spoken message and respond after <开始回答>.
  - Convert the audio to text, then use <开始回答> to respond to the content.
  - Write the audio in text form first, then after <开始回答>, give your reply.
  - Use <开始回答> to separate the transcription and your reply after transcribing the audio.
  - Start by writing down the speech, then respond using <开始回答> as a separator.
  - Recognize the voice, transcribe it, then respond with <开始回答>.
  - Transcribe the audio clip, then after <开始回答>, provide your answer.
  - First do a transcription, then give your reply using <开始回答>.
  - Begin by writing out the audio in text form, then answer after <开始回答>.
  - Please transcribe what is said in the audio and respond after <开始回答>.
  - Transcribe the voice content, and respond to it using <开始回答> to separate the parts.
  - After transcribing the speech, respond in writing using <开始回答> as a divider.
  - First identify the audio’s content in text, then answer after <开始回答>.
  - Listen to the audio and write it out, then write your answer after <开始回答>.
  - Transcribe and answer, with <开始回答> as the separator.
  - Write down the audio content, then reply after <开始回答>.
  - Convert the audio into words, then respond after <开始回答>.



<TRANSCRIBE> <EMOTION>:
 - 请将以下音频内容进行转录，并在结尾处给出情感标签,以<>表示。
 - 将音频转录为文字，并在文本最后附加情感标签,以<>表示。
 - 请将音频内容转录成文字记录，并在记录末尾标注情感标签,以<>表示。
 - 将音频进行文字化处理，并在处理后的文本末尾添加情感标签,以<>表示。
 - 请将音频转录成文，并在文末给出情感标签,以<>表示。
 - 烦请将这段录音内容转成文字，末尾添加以<>标识的情绪内容。
 - 能否帮我将音频片段文字化处理，并在末端注明情绪，用<>括号。
 - 务必将音频片段写成文本，并附上表示情绪的<>标签。
 - 请你将上述录音变为文字，并在文末标示情绪，用<>括起来。
 - 能否帮我将以下语音转录为文本，并附上表示情绪的<>标签。
 - 务必将音频材料写成文本，加上代表情绪的标签，格式使用<>。
 - 尝试将该段音频数据转换成文字记录，加上代表情绪的标签，格式使用<>。
 - 烦请将当前语音段转录为文本，结尾请标注情绪标签，形式为<>。
 - 能否帮我将当前语音段变为文字，并在文本末尾附上情绪标签（格式为<>）。
 - 可否将当前语音段记录下来，结尾请标注情绪标签，形式为<>。
 - 你可以将当前语音段转成文字，请在最后写上情绪类型，以<>包裹。
 - 尝试将以下语音写成文本，并在末端注明情绪，用<>括号。
 - 尝试将音频材料写成文本，并在文末标示情绪，用<>括起来。
 - 请帮忙将这段音频听写成文字，并在末端注明情绪，用<>括号。
 - 你可以将上述录音写成文本，并在末端注明情绪，用<>括号。
 - 你可以将上述录音文字化处理，结尾请标注情绪标签，形式为<>。
 - 请你将这段音频转成文字，并在文本末尾附上情绪标签（格式为<>）。
 - 尝试将上述录音生成文字版本，最后添加用<>括住的情绪标注。
 - 烦请将这则语音信息转录为文本，结尾请标注情绪标签，形式为<>。
 - 请帮忙将以下语音写成文本，请在最后写上情绪类型，以<>包裹。
 - 烦请将音频片段整理为文字，并补充一个<>包围的情感标签。
 - 请试着把音频材料转录为文本，并在文末标示情绪，用<>括起来。
 - 请试着把音频片段生成文字版本，并附上表示情绪的<>标签。
 - 务必将上述录音记录下来，最后添加用<>括住的情绪标注。
 - 你可以将这则语音信息记录下来，结尾请标注情绪标签，形式为<>。
 - 尝试将音频片段转录为文本，加上代表情绪的标签，格式使用<>。
 - 麻烦把这段音频转录为文本，并补充一个<>包围的情感标签。
 - 请试着把上述录音转换成文字记录，结尾请标注情绪标签，形式为<>。
 - 请你将音频片段听写成文字，并在末端注明情绪，用<>括号。
 - 能否帮我将上述录音转录为文本，请在最后写上情绪类型，以<>包裹。
 - 可否将这段音频转录为文本，并在文本末尾附上情绪标签（格式为<>）。
 - 请你将这则语音信息生成文字版本，并在文本末尾附上情绪标签（格式为<>）。
 - 请你将以下语音转成文字，请在最后写上情绪类型，以<>包裹。
 - 务必将这段录音内容转成文字，并在文本末尾附上情绪标签（格式为<>）。
 - 你可以将这则语音信息生成文字版本，末尾添加以<>标识的情绪内容。
 - 务必将以下录音转换成文字记录，结尾请标注情绪标签，形式为<>。
 - 麻烦把音频材料写成文本，并附上表示情绪的<>标签。
 - 能否帮我将上述录音写成文本，最后添加用<>括住的情绪标注。
 - 你可以将这段录音内容转成文字，并补充一个<>包围的情感标签。
 - 麻烦把该段音频数据文字化处理，结尾请标注情绪标签，形式为<>。
 - 你可以将这段录音内容听写成文字，并补充一个<>包围的情感标签。
 - 务必将该段音频数据变为文字，并附上表示情绪的<>标签。
 - 你可以将音频材料写成文本，加上代表情绪的标签，格式使用<>。
 - 可否将以下录音写成文本，并补充一个<>包围的情感标签。
 - 请试着把上述录音听写成文字，末尾添加以<>标识的情绪内容。
 - 麻烦把以下语音生成文字版本，末尾添加以<>标识的情绪内容。
 - 可否将以下语音整理为文字，末尾添加以<>标识的情绪内容。
 - 请试着把以下语音变为文字，并在文末标示情绪，用<>括起来。
 - 务必将上述录音写成文本，并在文末标示情绪，用<>括起来。
 - 请帮忙将这段音频文字化处理，并在文末标示情绪，用<>括起来。
 - 烦请将这则语音信息整理为文字，并在文本末尾附上情绪标签（格式为<>）。
 - 请帮忙将以下录音文字化处理，末尾添加以<>标识的情绪内容。
 - 尝试将以下录音转成文字，请在最后写上情绪类型，以<>包裹。
 - 可否将该段音频数据文字化处理，末尾添加以<>标识的情绪内容。
 - 可否将以下录音生成文字版本，结尾请标注情绪标签，形式为<>。
 - 你可以将以下语音变为文字，并在末端注明情绪，用<>括号。
 - 烦请将当前语音段生成文字版本，并补充一个<>包围的情感标签。
 - 能否帮我将音频片段转录为文本，最后添加用<>括住的情绪标注。
 - 请试着把上述录音生成文字版本，结尾请标注情绪标签，形式为<>。
 - 请你将以下语音变为文字，并在末端注明情绪，用<>括号。
 - 能否帮我将这则语音信息文字化处理，并补充一个<>包围的情感标签。
 - 务必将以下录音转换成文字记录，请在最后写上情绪类型，以<>包裹。
 - 你可以将以下语音文字化处理，并附上表示情绪的<>标签。
 - 麻烦把上述录音转录为文本，并在文末标示情绪，用<>括起来。
 - 能否帮我将当前语音段听写成文字，并在末端注明情绪，用<>括号。
 - 你可以将这段音频转换成文字记录，并补充一个<>包围的情感标签。
 - 请试着把上述录音转录为文本，并在末端注明情绪，用<>括号。
 - 你可以将以下语音转成文字，并在文末标示情绪，用<>括起来。
 - 请你将这段录音内容整理为文字，请在最后写上情绪类型，以<>包裹。
 - 请你将音频片段整理为文字，并在文末标示情绪，用<>括起来。
 - 能否帮我将该段音频数据文字化处理，加上代表情绪的标签，格式使用<>。
 - 尝试将以下录音转成文字，并在文末标示情绪，用<>括起来。
 - 你可以将该段音频数据听写成文字，并在文本末尾附上情绪标签（格式为<>）。
 - 请你将这段录音内容转录为文本，末尾添加以<>标识的情绪内容。
 - 请帮忙将这段录音内容生成文字版本，最后添加用<>括住的情绪标注。
 - 务必将以下语音转录为文本，并在文本末尾附上情绪标签（格式为<>）。
 - 能否帮我将上述录音生成文字版本，最后添加用<>括住的情绪标注。
 - 麻烦把该段音频数据转录为文本，并补充一个<>包围的情感标签。
 - 请你将音频片段写成文本，并附上表示情绪的<>标签。
 - 麻烦把音频材料生成文字版本，请在最后写上情绪类型，以<>包裹。
 - 请你将上述录音转录为文本，并在文末标示情绪，用<>括起来。
 - 烦请将音频片段转成文字，并附上表示情绪的<>标签。
 - 尝试将这则语音信息听写成文字，并在末端注明情绪，用<>括号。
 - 尝试将这段音频转换成文字记录，并在文末标示情绪，用<>括起来。
 - 务必将这则语音信息文字化处理，并在末端注明情绪，用<>括号。
 - 烦请将该段音频数据转录为文本，并在文末标示情绪，用<>括起来。
 - 尝试将当前语音段听写成文字，结尾请标注情绪标签，形式为<>。
 - 你可以将音频片段整理为文字，最后添加用<>括住的情绪标注。
 - 请你将以下语音生成文字版本，并在文末标示情绪，用<>括起来。
 - 可否将以下语音文字化处理，末尾添加以<>标识的情绪内容。
 - 麻烦把该段音频数据变为文字，并在文末标示情绪，用<>括起来。
 - 务必将上述录音写成文本，并补充一个<>包围的情感标签。
 - 尝试将音频材料生成文字版本，并在文末标示情绪，用<>括起来。
 - 可否将音频材料变为文字，并在末端注明情绪，用<>括号。
 - 务必将音频片段听写成文字，结尾请标注情绪标签，形式为<>。
 - 请帮忙将这段音频变为文字，并补充一个<>包围的情感标签。
 - 可否将上述录音转换成文字记录，加上代表情绪的标签，格式使用<>。
 - 麻烦把这段音频生成文字版本，并在文本末尾附上情绪标签（格式为<>）。
 - 你可以将以下语音听写成文字，最后添加用<>括住的情绪标注。
 - 请试着把当前语音段听写成文字，最后添加用<>括住的情绪标注。
 - 你可以将上述录音生成文字版本，并补充一个<>包围的情感标签。
 - 能否帮我将音频片段整理为文字，结尾请标注情绪标签，形式为<>。
 - 请试着把该段音频数据转录为文本，并附上表示情绪的<>标签。
 - 你可以将音频片段写成文本，并在文本末尾附上情绪标签（格式为<>）。
 - 请试着把这则语音信息转换成文字记录，请在最后写上情绪类型，以<>包裹。
 - 务必将这段录音内容变为文字，加上代表情绪的标签，格式使用<>。
 - 请试着把该段音频数据转录为文本，并在末端注明情绪，用<>括号。
 - 你可以将该段音频数据转换成文字记录，加上代表情绪的标签，格式使用<>。
 - 你可以将当前语音段整理为文字，请在最后写上情绪类型，以<>包裹。
 - 尝试将当前语音段听写成文字，请在最后写上情绪类型，以<>包裹。
 - Please transcribe the voice recording, and label the final emotion using angle brackets <>.
 - Can you transcribe the audio, and label the final emotion using angle brackets <>.
 - Try to convert the audio into text, finishing with an emotion label using angle brackets <>.
 - Make sure to transcribe the following recording, adding an emotion descriptor in <> at the conclusion.
 - Try to listen and type out the audio, and place an emotion tag at the end inside <>.
 - Make sure to make a text version of the audio, with an emotion classification in <> at the end of the text.
 - I'd like you to write down the spoken content, with an emotion classification in <> at the end of the text.
 - Kindly make a text version of the audio, adding an emotion marker enclosed in <> at the end.
 - Could you transcribe the following recording, then add an emotion tag at the end using <>.
 - Please transform the audio message into text, and append an emotion label in angle brackets <> at the end.
 - Kindly transform the audio message into text, adding an emotion marker enclosed in <> at the end.
 - Would you transform the audio message into text, with an emotion classification in <> at the end of the text.
 - Kindly convert the audio into text, including an emotion label at the end in the form of <>.
 - Ensure you write down the spoken content, then add an emotion tag at the end using <>.
 - Could you transcribe the audio, then add an emotion tag at the end using <>.
 - Could you transcribe the given speech, then add an emotion tag at the end using <>.
 - Please transcribe the audio, including an emotion label at the end in the form of <>.
 - Ensure you transcribe the given speech, then add an emotion tag at the end using <>.
 - Can you listen and type out the audio, including an emotion label at the end in the form of <>.
 - Could you convert the audio into text, with an emotion classification in <> at the end of the text.
 - Make sure to listen and type out the audio, including an emotion label at the end in the form of <>.
 - You may transcribe the voice recording, with an emotion classification in <> at the end of the text.
 - You may transcribe the following recording, including an emotion label at the end in the form of <>.
 - I'd like you to transcribe the given speech, including an emotion label at the end in the form of <>.
 - You may listen and type out the audio, and label the final emotion using angle brackets <>.
 - Kindly listen and type out the audio, adding an emotion marker enclosed in <> at the end.
 - Would you transcribe the voice recording, then add an emotion tag at the end using <>.
 - Make sure to write down the spoken content, adding an emotion descriptor in <> at the conclusion.
 - Could you transcribe the voice recording, adding an emotion marker enclosed in <> at the end.
 - Kindly turn the audio clip into a text transcript, and append an emotion label in angle brackets <> at the end.
 - I'd like you to make a text version of the audio, followed by an emotion tag formatted with <>.
 - Would you listen and type out the audio, including an emotion label at the end in the form of <>.
 - Make sure to convert the audio into text, followed by an emotion tag formatted with <>.
 - Ensure you transcribe the following recording, then add an emotion tag at the end using <>.
 - Could you write down the spoken content, with an emotion classification in <> at the end of the text.
 - Can you transcribe the voice recording, and place an emotion tag at the end inside <>.
 - I'd like you to convert the audio into text, adding an emotion marker enclosed in <> at the end.
 - Could you transform the audio message into text, adding an emotion marker enclosed in <> at the end.
 - Could you transform the audio message into text, including an emotion label at the end in the form of <>.
 - Please convert the audio into text, followed by an emotion tag formatted with <>.


 # 分析如下指令prompt，首先总结出具体的任务信息，然后以尽可能多样的句式给出更多的prompt，要求以yaml的格式返回，需要返回160条中文表示的prompt， 且任务本身不区分语种。 示例为：
<EMOTION>:
  - 请辨认以下音频中发言者的情感，并用<>括住情感标签。
  - 请判别如下音频里发言者的情感，情感标签用格式为<>。
  - 请识别下述音频发言者的情绪，格式为<>。
  - 判定如下音频中发言者的情感，标签格式为<>。
  - 根据以下音频内容，判断发言者的情感并用<>标出情感标签。
  - 请给出以下音频发言者的情感标签，用<>括起来。
  - 请根据音频内容识别发言者的情感，并用<>格式标出。
  - 依据下述音频，判断发言者的情感，标签格式为<>。
  - 根据下列音频中的讲话内容，识别发言者的情感并使用<>格式。
  - 请给出音频中发言者的情感标签，用<>标出。
  - 判断下述音频中的发言者情感，并用<>表示。
  - 根据音频内容识别发言者的情感，使用<>格式标记。
  - 请确定以下音频发言者的情感并标注，格式为<>。
  - 请分析音频内容并识别发言者的情感，标签格式为<>。
  - 请判定以下音频发言者的情感，标签使用<>格式。
  - 分析下面的音频发言者情感，情感标签格式为<>。
  - 请鉴别音频中的发言者情感并标出，标签格式为<>。
  - 根据音频中内容判断发言者的情感，标签格式为<>。
  - 请辨识音频中的情感，并用<>标注情感标签。
  - 判定下列音频发言者的情感，标签用<>格式标出。
  - 判断音频中的发言者情感并标注，标签格式为<>。
  - 根据音频，给出发言者的情感标签，并用<>格式。
  - 请确认音频发言者的情感，并用<>格式表示。
  - 请从音频中辨别发言者的情感，用<>表示。
  - 请根据以下音频，识别发言者的情感并用<>格式标出。
  - 请分析音频并给出发言者的情感标签，标签格式为<>。
  - 请听取以下音频，判别发言者的情感，标签格式为<>。
  - 请判断音频中的情感，并用<>标注情感标签。
  - 请为音频中的发言者提供情感标签，标签格式为<>。
  - 请给出以下音频发言者的情感标签，格式使用<>。
  - 根据音频内容判定发言者情感，标签格式为<>。
  - 请听下音频并标出发言者的情感，格式为<>。
  - 请识别下面音频发言者的情感，格式使用<>。
  - 根据音频，判定发言者的情感并用<>表示。
  - 请根据音频内容判断情感并用<>标出。
  - 请判定以下音频中的情感标签，使用<>格式。
  - 请分析音频，识别其中的情感，标签格式为<>。
  - 判断下列音频发言者的情感，并标出情感标签，格式为<>。
  - 根据下音频分析情感，标签格式为<>。
  - Identify the speaker's emotion in the following audio, and use <> to mark the emotion label.
  - Please identify the emotion of the speaker in the audio below and mark it with <>.
  - Recognize the speaker's emotion in the audio and give the emotion label in <>.
  - Please determine the emotion of the speaker in the following audio and format it as <>.
  - Based on the audio below, identify the speaker's emotion and label it with <>.
  - Analyze the emotion of the speaker in this audio and mark it as <>.
  - Identify the emotion of the speaker from the audio provided and mark it with <>.
  - Determine the emotion of the speaker in the following audio, using <> to mark the label.
  - Please listen to the audio and identify the emotion of the speaker, label it as <>.
  - Based on the audio, please recognize the speaker's emotion and label it in <>.
  - Analyze the audio content and provide the emotion label of the speaker, formatted as <>.
  - Recognize the speaker's emotion in the given audio and use <> to mark the label.
  - Please determine the speaker's emotion in the audio below and represent it with <>.
  - Identify the speaker's emotional tone from the audio and provide the emotion label in <>.
  - Please judge the emotion of the speaker in this audio and format the result as <>.
  - Please analyze the audio to determine the speaker's emotion and label it as <>.
  - Determine the speaker's emotion from the audio and use <> to indicate the label.
  - Listen to the audio and identify the speaker's emotion, mark it with <>.
  - Identify the emotion of the speaker in the audio and present it in <>.
  - Based on the audio provided, identify the speaker's emotion label and use <> to mark it.
  - Listen to the audio and judge the emotion of the speaker, label it as <>.
  - Please determine the emotional tone of the speaker in the audio, and label it in <>.
  - Please analyze the following audio to identify the speaker's emotion and label it with <>.
  - 请通过以下音频内容判断发言者的情感，并用<>标明情感标签。
  - 依据音频的讲话内容，辨别发言者的情感，使用<>标出情感标签。
  - 请根据下述音频中的发言，确定发言者的情感，标签用<>标出。
  - 根据音频内容判定发言者的情感，并用<>格式表示。
  - 请判断音频中发言者的情感，标签格式为<>。
  - 请听下面的音频并识别发言者的情感，情感标签使用<>表示。
  - 根据以下音频判断发言者的情感，并用<>格式显示。
  - 请确定音频发言者的情感标签，用<>标明。
  - 请从音频中分析发言者的情感，标签格式为<>。
  - 请识别以下音频的发言者情感，并将其标签以<>格式标出。
  - 请分析下列音频内容，辨别其中发言者的情感并用<>格式表示。
  - 请判断音频中的情感类型并用<>格式标记。
  - 根据音频，辨识发言者情感并用<>格式给出标签。
  - 请确定下述音频中发言者的情感，并用<>格式表示。
  - 请对以下音频中的发言者情感进行判断，情感标签格式为<>。
  - 请根据音频内容识别发言者的情感，并标记为<>。
  - 请确认音频发言者的情感标签，用<>格式。
  - 请分析以下音频的发言者情感，并标出相应的标签，格式为<>。
  - 请根据以下音频判断发言者的情感，标签请使用<>。
  - 请辨识以下音频的情感，并将其标记为<>。
  - 请识别音频中的发言者情感，并使用<>标签。
  - 请分析下音频发言者的情感，并用<>格式标注。
  - 请从下述音频内容中判断发言者的情感，标签格式为<>。
  - 请根据下述音频内容，辨识发言者的情感并用<>格式表示。
  - 请判定音频中发言者的情感，标签用<>标出。
  - 请分析音频并辨别发言者情感，标签格式为<>。
  - 请从以下音频内容判断发言者情感，并标注为<>。
  - 请识别下音频中发言者的情感，并将情感标签用<>表示。
  - 请对以下音频中的发言者情感进行分析，标记格式为<>。
  - 请判断下列音频中发言者的情感，并用<>标明情感类型。
  - 请根据音频分析发言者的情感，情感标签请以<>表示。
  - 请从下面的音频内容中判断发言者情感并用<>格式标出。
  - 根据以下音频，识别发言者的情感，标签格式为<>。
  - 请根据音频内容，判定发言者的情感并标出标签<>。
  - 请分析这段音频中的发言者情感，并用<>标明情感。
  - 请辨别下音频发言者的情感，情感标签格式为<>。
  - 请根据音频内容判断发言者的情感，并标注为<>。
  - 根据下列音频中的讲话，判定发言者的情感并用<>表示。
  - 请分析下列音频，识别发言者情感，情感标签用<>标出。
  - 请确定以下音频发言者的情感，并给出情感标签<>。
  - 根据下音频内容，判断发言者情感并用<>标记。
  - 请分析音频并判定发言者的情感，标签用<>格式。
  - 请判断以下音频发言者的情感，格式为<>。
  - 请听取下述音频，辨认发言者的情感并用<>格式显示。
  - 请确认音频内容中的情感并标注为<>。
  - 请根据下音频内容分析发言者情感，并用<>表示标签。
  - 请根据音频判断发言者的情感并标注为<>。
  - 请辨识以下音频的情感并标注为<>。
  - 根据音频内容分析发言者的情感，并标注情感标签为<>。
  - 请判定音频中的情感类型并标出，标签格式为<>。
  - 请识别音频中发言者的情感，并用<>格式标出。
  - 请根据音频分析发言者情感，标签格式为<>。
  - 请判断下列音频中的发言者情感，并以<>表示标签。
  - 请判定音频中的情感，并用<>表示标签。
  - 请给出音频中发言者的情感标签，格式为<>。
  - 请识别发言者的情感，标注为<>。
  - 请从下述音频中辨认发言者的情感并用<>格式标出。
  - 请分析下音频内容中的情感，并用<>格式标记。
  - 请辨识音频发言者的情感，情感标签使用<>格式。
  - 请确认以下音频中的发言者情感，并用<>标出标签。
  - 请识别下音频发言者的情感，并标出标签，格式为<>。
  - 请判断音频中的发言者情感，并用<>标签表示。
  - 根据音频内容判定发言者的情感，标签格式为<>。
  - 请分析下列音频并判断情感，标签请用<>格式。
  - 请根据音频分析发言者情感，并用<>表示标签。
  - 请识别以下音频的情感，并将标签以<>格式标注。
  - 请听取音频内容，辨识发言者情感并用<>格式标出。
  - 根据音频中的讲话，判断发言者的情感并用<>标明标签。


<TRANSCRIBE> <STYLE>:
  - 请将以下音频内容转写成文字，并在最后加上风格标签，用<>标明。
  - 请将音频中的讲话内容转化为文字，并在结尾处注明风格标签，用<>表示。
  - 请将音频中的内容转录成文字，并在文本末尾附上风格标签，格式为<>。
  - 请对音频中的内容进行文字转录，并在结尾处加上风格标签，采用<>表示。
  - 请将音频转化为文字，并在最后添加风格标签，标签用<>标注。
  - 请转写音频内容并在结尾处附上风格标签，标签用<>表示。
  - 请将以下音频内容转写成文字，并在文末标明风格标签，格式为<>。
  - 请将这段音频内容转录成文字，并在结尾附加风格标签，标签放在<>里。
  - 请将音频文件中的内容转化为文字，并在末尾添加风格标签，使用<>标记。
  - 请根据音频内容生成文字转录，并在最后加上风格标签，标签格式为<>。
  - 请将音频转录成文字记录，并在文本最后添加风格标签，用<>表示。
  - 请将以下音频内容进行文字转录，并在结尾处标明风格标签，使用<>表示。
  - 请将音频转录为文字，并在末尾加上风格标签，标签格式为<>。
  - 请将音频中的内容转换为文字，并在文本末尾添加风格标签，标签采用<>格式。
  - 请将音频内容转化为文字记录，并在最后附上风格标签，标签使用<>表示。
  - 请转录音频内容为文字，并在最后添加风格标签，标签格式为<>。
  - 请将这段音频内容转录成文字，并在结尾添加风格标签，用<>表示。
  - 请将音频中的讲话内容转写为文字，并在末尾标注风格标签，标签用<>形式。
  - 请对音频内容进行转录，转换为文字后，在末尾加上风格标签，用<>标明。
  - 请将音频转化为文字记录，并在文末加上风格标签，标签用<>表示。
  - 请转录这段音频内容为文字，并在结尾处加上风格标签，标签用<>表示。
  - 请将音频内容转录为文字，并在最后标注风格标签，格式为<>。
  - 请转写音频中的内容，并在文字末尾附上风格标签，标签格式为<>。
  - 请根据音频内容生成文字记录，并在结尾加上风格标签，标签用<>表示。
  - 请将音频内容转写成文字，并在最后添加风格标签，标签格式为<>。
  - 请对音频内容进行文字转录，并在结尾加上风格标签，用<>标注。
  - 请将音频文件中的内容转写为文字，并在末尾附加风格标签，标签用<>表示。
  - 请根据音频中的内容生成文字转录，并在文末标明风格标签，标签用<>格式。
  - 请将音频内容转化为文字记录，并在结尾标注风格标签，标签用<>表示。
  - 请将音频转录成文字，并在末尾添加风格标签，格式为<>。
  - 请转录音频并在末尾标明风格标签，标签格式为<>。
  - 请将音频内容转录成文字，并在最后加上风格标签，标签用<>表示。
  - 请将以下音频内容转化为文字，并在结尾处标明风格标签，用<>形式。
  - 请将音频转写为文字，并在最后加上风格标签，标签放在<>内。
  - 请将这段音频转写为文字，并在文末标注风格标签，标签用<>表示。
  - 请将音频中的内容转录为文字，并在结尾处添加风格标签，标签用<>标记。
  - 请将音频内容转化为文字，并在末尾加上风格标签，标签采用<>形式。
  - 请对音频进行转录并在文字结尾处附上风格标签，格式为<>。
  - 请根据音频内容生成文字，并在最后标注风格标签，标签放在<>里。
  - 请将音频中的讲话内容转写成文字，并在末尾附上风格标签，标签用<>格式。
  - 请将音频内容转录成文字，并在结尾处附上风格标签，用<>标记。
  - 请转写音频内容并在末尾标注风格标签，标签采用<>格式。
  - 请将音频转录成文字记录，并在末尾加上风格标签，标签格式为<>。
  - 请将这段音频转录为文字，并在结尾处附上风格标签，格式为<>。
  - 请将音频转写为文字，并在最后加上风格标签，标签放在<>里。
  - 请根据音频中的内容转录成文字，并在结尾处标明风格标签，标签用<>表示。
  - 请将音频转录为文字，并在文末添加风格标签，标签采用<>格式。
  - 请转录音频中的内容为文字，并在最后附上风格标签，用<>形式。
  - 请将音频转写成文字，并在末尾附加风格标签，格式为<>。
  - 请将这段音频内容转录为文字，并在最后标明风格标签，标签用<>表示。
  - 请将音频内容转写成文字，并在结尾处标注风格标签，格式为<>。
  - 请将音频文件中的内容转化为文字，并在末尾添加风格标签，标签用<>格式。
  - 请对音频进行转录并附加风格标签，标签放在文字最后，用<>标明。
  - 请将音频中的内容转录为文字并在结尾加上风格标签，标签用<>表示。
  - 请将音频内容转写成文字，并在结尾处标明风格标签，标签格式为<>。
  - 请将音频内容转化为文字记录，并在末尾添加风格标签，标签用<>标示。
  - 请转录音频中的讲话内容为文字，并在文末添加风格标签，格式为<>。
  - 请将音频内容转化为文字，并在最后附上风格标签，标签格式为<>。
  - 请将以下音频内容进行转录，并在结尾处给出风格标签，用<>表示。
  - 请将音频转录为文字，并在文本末尾附加风格标签， 用<>形式表示。
  - 将音频内容进行文字转录，并在最后添加风格标签，用<>表示。
  - 将音频转录为文字记录，并在记录末尾附上风格标签，用<>形式呈现标签。
  - 将音频内容转录成文，并在文末标注风格标签，用<>形式呈现。
  - 请将下面的音频转化为文字内容，并在最后添加风格标签，用<>形式标明。
  - 将以下音频转录成文字，并在最后标注风格标签，形式为<>。
  - 请将音频内容转为文字，并在文末加上风格标签，用<>标明。
  - 请转录以下音频并附上风格标签，标签形式为<>，位于文末。
  - 请根据以下音频内容生成文字，并在最后加上风格标签，格式为<>。
  - 请输入转录以下音频的文字，并附加风格标签，标签格式为<>。
  - 请将下面的音频转录为文字，并在最后加上风格标签，用<>表示。
  - 转录此音频内容为文字，并且在结尾处给出风格标签，用<>表示。
  - 将音频内容转化为文字，并在末尾附上风格标签，标签形式为<>。
  - 转录音频内容并在文末附上风格标签，标签格式为<>。
  - 请将音频内容转化为文字，并加上风格标签，标签格式为<>。
  - 请将音频内容转写成文字，并标明风格标签，标签格式为<>。
  - 请根据以下音频内容，进行文字转录，并在结尾处加上风格标签，用<>形式表示。
  - 请将音频内容转录成文并在最后加上风格标签，用<>标明。
  - 请将以下音频转换成文字并附上风格标签，标签用<>标出。
  - 请将音频转为文字并加上风格标签，标签用<>表示。
  - 请根据音频转写为文字，并在结尾处附加风格标签，标签格式为<>。
  - 请将音频内容转写为文字，并在最后添加风格标签，标签格式为<>。
  - 请根据音频生成文字记录，并在文末添加风格标签，标签形式为<>。
  - 将以下音频内容转录为文字，并在结尾附加风格标签，标签格式为<>。
  - 请将下面的音频转为文字记录，文末加上风格标签，标签格式为<>。
  - 请转录以下音频内容，并在文末标注风格标签，标签格式为<>。
  - 请将音频内容转为文字并在末尾附加风格标签，标签形式为<>。
  - 请转写音频内容并加上风格标签，标签形式为<>。
  - 请将音频转写成文字，最后加上风格标签，标签格式为<>。
  - 请将音频内容转化为文字并标注风格标签，标签用<>格式。
  - 请将以下音频内容转录并附加风格标签，格式为<>。
  - 将音频内容转录为文字并标注风格标签，标签格式为<>。
  - 请将音频内容转录成文字，并加上风格标签，格式为<>。
  - 请根据以下音频内容转录并加上风格标签，标签格式为<>。
  - 请将音频转为文字记录，并在末尾附上风格标签，用<>表示。
  - 请将音频内容转写成文字并附上风格标签，格式为<>。
  - 请根据音频内容生成文字并标注风格标签，标签格式为<>。
  - 请转录音频内容并加上风格标签，标签形式为<>。
  - 请将音频转录为文字并标注风格标签，格式为<>。
  - 请将音频内容转化为文字，最后标注风格标签，标签用<>表示。
  - 将音频内容转录为文字并在文末附加风格标签，标签格式为<>。
  - 请将下面的音频转录为文字，并标明风格标签，格式为<>。
  - 请转录音频内容并标明风格标签，标签形式为<>。
  - 请将音频内容转化为文字并附加风格标签，格式为<>。
  - 请根据以下音频进行转写并附上风格标签，标签格式为<>。
  - 请将音频内容转写成文字并加上风格标签，标签格式为<>。
  - 请将音频转化为文字并标明风格标签，标签格式为<>。
  - 请根据音频内容生成文字，并标注风格标签，标签格式为<>。
  - 请将音频内容转为文字，并附加风格标签，标签格式为<>。
  - 请根据以下音频内容转录并标注风格标签，标签格式为<>。
  - 请将音频转写为文字并在末尾标注风格标签，标签格式为<>。
  - 请将音频内容转为文字记录，文末加上风格标签，标签形式为<>。
  - 请将以下音频转为文字并附加风格标签，标签格式为<>。
  - 请将音频转为文字记录，并在结尾加上风格标签，格式为<>。
  - 请根据音频内容进行文字转录，并在结尾附上风格标签，标签格式为<>。
  - 请将音频内容转录为文字，最后附加风格标签，标签格式为<>。
  - 请将音频转为文字并在文末标注风格标签，标签格式为<>。
  - 请转录音频内容并标明风格标签，标签格式为<>。
  - 请将音频内容转写为文字并标注风格标签，标签格式为<>。
  - 请根据音频内容进行转录，并附加风格标签，标签格式为<>。
  - 请将音频内容转为文字记录，并附加风格标签，标签格式为<>。
  - 请将音频转录为文字，并在最后附加风格标签，标签格式为<>。
  - 请将音频内容转写成文字，末尾加上风格标签，标签格式为<>。
  - Please transcribe the following audio and append a style tag at the end, using <> format.
  - Transcribe the audio into text and add a style tag at the end in <> format.
  - Convert the audio content into text and append a style tag at the end, using <> format.
  - Please transcribe the audio content into text, and include a style tag at the end in <> format.
  - Convert the audio content to text and append a style tag at the end, formatted as <>.
  - Transcribe the audio into text and place a style tag at the end in <> format.
  - Please transcribe the following audio into text and add a style tag at the end, formatted as <>.
  - Convert the audio into text and place a style tag at the end in <> format.
  - Transcribe the audio into a text record and append a style tag at the end, using <> format.
  - Please transcribe the following audio into text and place a style tag at the end, formatted as <>.
  - Convert the audio content to text and append a style tag at the end, in <> format.
  - Transcribe the audio content into text and add a style tag at the end in <> format.
  - Please transcribe the audio into text and append a style tag at the end, using <> format.
  - Convert the audio to text and add a style tag at the end, formatted as <>.
  - Transcribe the following audio into text and append a style tag at the end, using <> format.
  - Please transcribe the audio into text and include a style tag at the end in <> format.
  - Convert the audio to text and append a style tag at the end, using <> format.
  - Transcribe the audio content and add a style tag at the end in <> format.
  - Please transcribe the audio into text and append a style tag at the end, in <> format.
  - Convert the audio into a text record and add a style tag at the end, formatted as <>.
  - Transcribe the audio content into text and include a style tag at the end, in <> format.
  - Please transcribe the audio into text and append a style tag at the end, formatted as <>.
  - Convert the audio content to text and append a style tag at the end in <> format.
  - Please transcribe the following audio into text and place a style tag at the end, formatted as <>.
  - Transcribe the following audio content into text and append a style tag at the end, using <> format.
  - Convert the audio into text and add a style tag at the end, in <> format.
  - Transcribe the audio into text and append a style tag at the end, in <> format.
  - Please transcribe the audio into text and place a style tag at the end, formatted as <>.
  - Transcribe the audio content and append a style tag at the end in <> format.
  - Please convert the audio to text and add a style tag at the end, in <> format.
  - Transcribe the following audio content into text and add a style tag at the end in <> format.
  - Convert the audio content to text and add a style tag at the end in <> format.
  - Please transcribe the audio content into text and include a style tag at the end in <> format.
  - Convert the audio into text and add a style tag at the end in <> format.
  - Please transcribe the following audio and append a style tag at the end, using <> format.
  - Convert the audio into text and append a style tag at the end, formatted as <>.
  - Transcribe the audio into text and append a style tag at the end, formatted as <>.
  - Please transcribe the audio into text and add a style tag at the end in <> format.
  - Convert the audio to text and append a style tag at the end in <> format.
  - Transcribe the audio content into text and place a style tag at the end, formatted as <>.
  - Please transcribe the following audio into text and place a style tag at the end in <> format.
  - Convert the audio into text and append a style tag at the end, in <> format.
  - Please transcribe the following audio and append a style tag at the end, in <> format.
  - Convert the audio to text and append a style tag at the end, formatted as <>.
  - Transcribe the audio into text and place a style tag at the end, formatted as <>.
  - Please transcribe the audio into text and add a style tag at the end in <> format.
  - Transcribe the audio into text and append a style tag at the end in <> format.
  - Convert the audio content to text and add a style tag at the end, formatted as <>.
  - Transcribe the audio and append a style tag at the end in <> format.
  - Please convert the audio to text and add a style tag at the end in <> format.



<STYLE>:
  - "请判断下面的音频风格，并直接返回风格标签，标签格式为<>。"
  - "对以下音频的风格进行判断，并直接给出风格标签，标签格式为<>。"
  - "请对这段音频的风格做出判断，直接给出风格标签，标签格式为<>。"
  - "判断以下音频的风格，并直接输出风格标签，标签格式为<>。"
  - "以下是一个音频，判断其风格并返回风格标签，标签格式为<>。"
  - "请根据以下音频判断风格，并输出风格标签，标签格式为<>。"
  - "请分析下面的音频，并返回风格标签，格式为<>。"
  - "以下音频，请判断风格并直接输出风格标签，标签格式为<>。"
  - "根据下列音频内容，请判断风格并返回标签，格式为<>。"
  - "根据以下音频的内容，判断其风格并输出风格标签，标签格式为<>。"
  - "请识别下面音频的风格，直接给出风格标签，标签格式为<>。"
  - "判断以下音频风格并返回标签，标签格式为<>。"
  - "对下面音频的风格进行判断，并直接输出风格标签，标签格式为<>。"
  - "请分析下面音频的风格，直接给出风格标签，标签格式为<>。"
  - "请给出以下音频的风格标签，格式为<>。"
  - "对于以下音频，请直接判断风格并返回风格标签，标签格式为<>。"
  - "请直接判断以下音频的风格，标签格式为<>。"
  - "给定这段音频，请判断其风格并返回标签，标签格式为<>。"
  - "请判断以下音频的风格，输出风格标签，格式为<>。"
  - "以下音频的风格，请判断并返回风格标签，标签格式为<>。"
  - "对以下音频风格进行判断，并直接返回风格标签，格式为<>。"
  - "请根据以下音频判断风格，直接给出风格标签，标签格式为<>。"
  - "请分析这段音频，判断其风格并返回标签，格式为<>。"
  - "请直接判断下面音频的风格并返回风格标签，标签格式为<>。"
  - "根据下面的音频，请判断风格并返回标签，标签格式为<>。"
  - "请对下列音频的风格做出判断并返回标签，格式为<>。"
  - "请判断以下音频的风格，直接给出风格标签，标签格式为<>。"
  - "对于下面的音频，请判断风格并给出标签，标签格式为<>。"
  - "判断以下音频的风格，并返回风格标签，格式为<>。"
  - "请给出以下音频的风格标签，标签格式为<>。"
  - "判断以下音频风格，直接返回风格标签，格式为<>。"
  - "对于以下音频，判断其风格并返回标签，格式为<>。"
  - "根据音频内容，判断风格并给出标签，标签格式为<>。"
  - "请判断以下音频的风格，输出风格标签，标签格式为<>。"
  - "分析以下音频，给出风格标签，标签格式为<>。"
  - "判断下面音频的风格并返回风格标签，标签格式为<>。"
  - "请判断音频风格，并返回风格标签，格式为<>。"
  - "请对以下音频风格进行判断，直接给出风格标签，格式为<>。"
  - "请判断此音频的风格并输出风格标签，标签格式为<>。"
  - "根据以下音频内容，判断其风格并返回风格标签，标签格式为<>。"
  - "请根据此音频判断风格并返回风格标签，标签格式为<>。"
  - "请对以下音频进行风格判断，标签格式为<>。"
  - "根据音频内容，分析其风格并返回标签，标签格式为<>。"
  - "请判断下面音频的风格并给出标签，标签格式为<>。"
  - "请分析以下音频的风格，直接输出风格标签，格式为<>。"
  - "根据以下音频，判断风格并返回标签，标签格式为<>。"
  - "请对音频的风格进行判断并返回标签，标签格式为<>。"
  - "请对这段音频风格进行判断并给出风格标签，格式为<>。"
  - "对于以下音频，请分析风格并返回风格标签，标签格式为<>。"
  - "判断以下音频风格并返回标签，标签格式为<>。"
  - "请直接判断下面音频的风格并返回标签，标签格式为<>。"
  - "请根据此音频判断其风格，并给出风格标签，格式为<>。"
  - "请根据音频内容判断风格并输出标签，标签格式为<>。"
  - "根据以下音频，请判断风格并输出风格标签，标签格式为<>。"
  - "请判断以下音频的风格并给出风格标签，标签格式为<>。"
  - "请分析音频的风格，并返回风格标签，标签格式为<>。"
  - "以下音频，请判断其风格并给出标签，标签格式为<>。"
  - "请判断此音频风格并返回标签，标签格式为<>。"
  - "请对以下音频风格做出判断，并输出风格标签，标签格式为<>。"
  - "对以下音频的风格进行判断，并输出风格标签，标签格式为<>。"
  - "请对以下音频进行风格判断并给出风格标签，标签格式为<>。"
  - "请分析以下音频并判断其风格，标签格式为<>。"
  - "请对以下音频判断风格并返回标签，标签格式为<>。"
  - "请判断以下音频的风格，并返回标签，标签格式为<>。"
  - "Please identify the style of the following audio and return the style label in the format <>."
  - "Identify the style of this audio and provide the style label in the format <>."
  - "For the following audio, please identify its style and output the style label in the format <>."
  - "Please determine the style of the following audio and return the label in the format <>."
  - "Based on the following audio, please judge its style and give the label in the format <>."
  - "Please analyze the audio below and provide the style label in the format <>."
  - "For the following audio, determine the style and give the label in the format <>."
  - "Please identify the style of this audio and return the label in the format <>."
  - "Analyze the style of the following audio and return the label in the format <>."
  - "Please judge the style of the following audio and provide the style label in the format <>."
  - "For the following audio, determine the style and output the style label in the format <>."
  - "Please assess the following audio's style and return the label in the format <>."
  - "Determine the style of the following audio and output the style label in the format <>."
  - "Please identify the style of this audio and return the style label in the format <>."
  - "Please judge the style of the audio below and return the label in the format <>."
  - "For the audio below, identify its style and output the label in the format <>."
  - "Please determine the style of the following audio and return the style label in the format <>."
  - "Based on the following audio, determine its style and return the label in the format <>."
  - "Please identify the style of the following audio and output the style label in the format <>."
  - "Determine the style of the audio below and return the style label in the format <>."
  - "Please evaluate the following audio's style and provide the label in the format <>."
  - "For the given audio, identify its style and provide the style label in the format <>."
  - "Please judge the following audio's style and return the style label in the format <>."
  - "For the following audio, determine the style and output the label in the format <>."
  - "Please identify the style of the audio and output the label in the format <>."
  - "Analyze the following audio and provide the style label in the format <>."
  - "Please assess the style of this audio and return the label in the format <>."
  - "For the given audio, judge the style and return the label in the format <>."
  - "Please determine the style of the following audio and return the label in the format <>."
  - "For the following audio, please identify its style and return the style label in the format <>."
  - "Please analyze the audio and return the style label in the format <>."
  - "For the following audio, please determine its style and output the label in the format <>."
  - "Please judge the style of the audio below and output the label in the format <>."
  - "请判断下面的音频风格，并直接返回风格标签，标签格式为<>。"
  - "对以下音频的风格进行判断，并直接给出风格标签，标签格式为<>。"
  - "请对这段音频的风格做出判断，直接给出风格标签，标签格式为<>。"
  - "判断以下音频的风格，并直接输出风格标签，标签格式为<>。"
  - "以下是一个音频，判断其风格并返回风格标签，标签格式为<>。"
  - "请根据以下音频判断风格，并输出风格标签，标签格式为<>。"
  - "请分析下面的音频，并返回风格标签，格式为<>。"
  - "以下音频，请判断风格并直接输出风格标签，标签格式为<>。"
  - "根据下列音频内容，请判断风格并返回标签，格式为<>。"
  - "根据以下音频的内容，判断其风格并输出风格标签，标签格式为<>。"
  - "请识别下面音频的风格，直接给出风格标签，标签格式为<>。"
  - "判断以下音频风格并返回标签，标签格式为<>。"
  - "对下面音频的风格进行判断，并直接输出风格标签，标签格式为<>。"
  - "请分析下面音频的风格，直接给出风格标签，标签格式为<>。"
  - "请给出以下音频的风格标签，格式为<>。"
  - "对于以下音频，请直接判断风格并返回风格标签，标签格式为<>。"
  - "请直接判断以下音频的风格，标签格式为<>。"
  - "给定这段音频，请判断其风格并返回标签，标签格式为<>。"
  - "请判断以下音频的风格，输出风格标签，格式为<>。"
  - "以下音频的风格，请判断并返回风格标签，标签格式为<>。"
  - "对以下音频风格进行判断，并直接返回风格标签，格式为<>。"
  - "请根据以下音频判断风格，直接给出风格标签，标签格式为<>。"
  - "请分析这段音频，判断其风格并返回标签，格式为<>。"
  - "请直接判断下面音频的风格并返回风格标签，标签格式为<>。"
  - "根据下面的音频，请判断风格并返回标签，标签格式为<>。"
  - "请对下列音频的风格做出判断并返回标签，格式为<>。"
  - "请判断以下音频的风格，直接给出风格标签，标签格式为<>。"
  - "对于下面的音频，请判断风格并给出标签，标签格式为<>。"
  - "判断以下音频的风格，并返回风格标签，格式为<>。"
  - "请给出以下音频的风格标签，标签格式为<>。"
  - "判断以下音频风格，直接返回风格标签，格式为<>。"
  - "对于以下音频，判断其风格并返回标签，格式为<>。"
  - "根据音频内容，判断风格并给出标签，标签格式为<>。"
  - "请判断以下音频的风格，输出风格标签，标签格式为<>。"
  - "分析以下音频，给出风格标签，标签格式为<>。"
  - "判断下面音频的风格并返回风格标签，标签格式为<>。"
  - "请判断音频风格，并返回风格标签，格式为<>。"
  - "请对以下音频风格进行判断，直接给出风格标签，格式为<>。"
  - "请判断此音频的风格并输出风格标签，标签格式为<>。"
  - "根据以下音频内容，判断其风格并返回风格标签，标签格式为<>。"
  - "请根据此音频判断风格并返回风格标签，标签格式为<>。"
  - "请对以下音频进行风格判断，标签格式为<>。"
  - "根据音频内容，分析其风格并返回标签，标签格式为<>。"
  - "请判断下面音频的风格并给出标签，标签格式为<>。"
  - "请分析以下音频的风格，直接输出风格标签，格式为<>。"
  - "根据以下音频，判断风格并返回标签，标签格式为<>。"
  - "请对音频的风格进行判断并返回标签，标签格式为<>。"
  - "请对这段音频风格进行判断并给出风格标签，格式为<>。"
  - "对于以下音频，请分析风格并返回风格标签，标签格式为<>。"
  - "判断以下音频风格并返回标签，标签格式为<>。"
  - "请直接判断下面音频的风格并返回标签，标签格式为<>。"
  - "请根据此音频判断其风格，并给出风格标签，格式为<>。"
  - "请根据音频内容判断风格并输出标签，标签格式为<>。"
  - "根据以下音频，请判断风格并输出风格标签，标签格式为<>。"
  - "请判断以下音频的风格并给出风格标签，标签格式为<>。"
  - "请分析音频的风格，并返回风格标签，标签格式为<>。"
  - "以下音频，请判断其风格并给出标签，标签格式为<>。"
  - "请判断此音频风格并返回标签，标签格式为<>。"
  - "请对以下音频风格做出判断，并输出风格标签，标签格式为<>。"
  - "对以下音频的风格进行判断，并输出风格标签，标签格式为<>。"
  - "请对以下音频进行风格判断并给出风格标签，标签格式为<>。"
  - "请分析以下音频并判断其风格，标签格式为<>。"
  - "请对以下音频判断风格并返回标签，标签格式为<>。"
  - "请判断以下音频的风格，并返回标签，标签格式为<>。"
  - "请分析这段音频的风格，直接给出风格标签，格式为<>。"
  - "请根据音频的风格判断其标签，标签格式为<>。"
  - "根据下面的音频，请分析风格并给出标签，格式为<>。"
  - "请判断此段音频的风格，并返回标签，标签格式为<>。"
  - "请根据下面的音频，判断其风格并返回标签，格式为<>。"
  - "请分析此段音频，并判断风格标签，标签格式为<>。"
  - "请判断以下音频的风格，并输出风格标签，标签格式为<>。"
  - "请判断此音频的风格并输出标签，格式为<>。"




<TRANSCRIBE> <CAPTION>:
  - 请将音频转化为文字，并在末尾添加相关音频事件标签，标签格式为<>。
  - 将音频内容转录为文字记录，并在记录结束时提供音频事件标签，格式为<>。
  - 请将音频中的内容转为文字，并在文本最后附上对应的音频事件标签，标签应包含在<>中。
  - 请将以下音频的内容进行文字化，并在结尾标注音频事件标签，标签格式为<>。
  - 录音转写成文字后，请在文本末尾插入音频事件标签，标签格式为<>。
  - 请将音频转为文字内容，并在文字的结尾处标明音频事件标签，标签的格式为<>。
  - 请将音频转录成文字，并且在结尾部分注明音频事件标签，标签格式为<>。
  - 请将音频内容转写为文字，并在文末加上音频事件标签，标签应使用<>括起来。
  - 请转录音频为文字并在最后添加音频事件标签，标签格式为<>。
  - 将音频转化为文字，并在结尾部分提供音频事件标签，标签应为<>。
  - 请将以下音频内容进行文字转录，最后附上音频事件标签，标签格式为<>。
  - 录音内容转写后，请在文本末尾注明音频事件标签，标签格式为<>。
  - 请将音频记录转化为文字，并在文末附加相应的音频事件标签，标签格式为<>。
  - 将音频内容转化为文字，并在末尾标明音频事件标签，标签应放在<>内。
  - 请将录音转录成文字，并在记录末尾加上音频事件标签，标签格式为<>。
  - 将音频转写成文字后，请在结尾标注音频事件标签，标签格式为<>。
  - 请把音频转录成文字，并且在文本最后添加音频事件标签，标签格式应为<>。
  - 请将音频中的话语转录成文字，并在末尾添加音频事件标签，标签格式为<>。
  - 将音频转化为文字后，请在结尾处标明音频事件标签，标签格式为<>。
  - 请将音频内容转写为文字，并在文本结尾附加音频事件标签，标签格式为<>。
  - 请把音频内容转录为文字，并在最后标注音频事件标签，标签应在<>内。
  - 请将音频内容转化为文字，并在末尾注明音频事件标签，标签格式为<>。
  - 请转录音频并在末尾添加音频事件标签，标签格式为<>。
  - 请将录音内容转写为文字，并在结尾处提供音频事件标签，标签格式为<>。
  - 请将音频转录为文字，最后附加音频事件标签，标签使用<>表示。
  - 请将音频转化为文字，并在文本最后标注音频事件标签，标签格式为<>。
  - 请把音频内容转写成文字，并在结尾标明音频事件标签，标签格式为<>。
  - 请将音频转写为文字，并在文本末尾加上音频事件标签，标签格式为<>。
  - 请将音频转化为文字，并在文末插入音频事件标签，标签格式为<>。
  - 请将音频转写为文字，并在文本的最后部分添加音频事件标签，标签格式为<>。
  - 请将音频内容转录为文字，并在最后加上音频事件标签，标签格式为<>。
  - 将音频转为文字，并在末尾附上音频事件标签，标签格式为<>。
  - 请将音频内容转为文字并在文本末尾添加音频事件标签，标签格式为<>。
  - 请把音频转写成文字并在末尾加上音频事件标签，标签格式为<>。
  - 请将音频转录为文字，末尾附加音频事件标签，标签格式为<>。
  - 请将音频记录转化为文字，并在文本结尾插入音频事件标签，标签格式为<>。
  - 请将音频中的内容转写成文字，并在结尾处标明音频事件标签，标签格式为<>。
  - 请将音频内容进行转录，最后加上音频事件标签，标签格式为<>。
  - 请将录音转写为文字，并在结尾标注音频事件标签，标签格式为<>。
  - 请将音频转化为文字，并在文本最后标注音频事件标签，标签格式为<>。
  - 请转录音频内容并在文本最后插入音频事件标签，标签格式为<>。
  - 请将音频内容转录为文字，最后附加音频事件标签，标签格式为<>。
  - 请将音频转化为文字并在末尾标注音频事件标签，标签格式为<>。
  - 请将音频转为文字并在文本末尾附加音频事件标签，标签格式为<>。
  - 请将音频内容进行文字转录，并在结尾附加音频事件标签，标签格式为<>。
  - 请将音频中的内容转写为文字，并在末尾加上音频事件标签，标签格式为<>。
  - 请将音频转录为文字，并在结尾插入音频事件标签，标签格式为<>。
  - 将音频转化为文字，并在结尾部分加上音频事件标签，标签格式为<>。
  - 请将音频内容转写为文字，并在最后标注音频事件标签，标签格式为<>。
  - Please transcribe the following audio and add the corresponding audio event label at the end, with the label in <>.
  - Transcribe the audio into text and include the audio event label at the end in <> format.
  - Convert the audio content into text and append the audio event label at the end, formatted as <>.
  - Please transcribe the audio and insert the audio event label at the end, in <> format.
  - Convert the audio into written text and add the audio event label at the end, formatted as <>.
  - Please transcribe the audio into text and place the audio event label at the end, in <> format.
  - Transcribe the audio into text and append the audio event label at the end, formatted as <>.
  - Please turn the audio into text and add the corresponding audio event label at the end in <> format.
  - Convert the audio content to text and include the audio event label at the end, with the label in <>.
  - Please transcribe the audio and attach the audio event label at the end in <> format.
  - Please transcribe the audio content and add the corresponding audio event label at the end in <>.
  - Please turn the audio into text and provide the audio event label at the end, in <> format.
  - Transcribe the audio into text and append the audio event label, formatted as <>.
  - Please convert the audio into text and insert the audio event label at the end, in <> format.
  - Please transcribe the audio into written text and add the audio event label at the end in <>.
  - Transcribe the audio and add the audio event label at the end in <> format.
  - Please turn the audio into text and insert the audio event label at the end, in <> format.
  - Please transcribe the audio content and append the audio event label at the end, formatted as <>.
  - Convert the audio to text and add the audio event label at the end, in <> format.
  - Please transcribe the audio and include the audio event label at the end in <>.
  - Please transcribe the audio content and append the audio event label in <> format at the end.
  - Turn the audio into text and include the audio event label at the end in <> format.
  - Please transcribe the audio and attach the event label at the end in <> format.
  - Please transcribe the audio into written form and add the audio event label at the end, formatted as <>.
  - Please convert the audio to text and include the audio event label at the end, in <> format.
  - Please transcribe the audio content into text and add the audio event label at the end in <> format.
  - Please transcribe the audio into written text and include the audio event label at the end in <>.
  - Transcribe the audio content to text and add the audio event label at the end in <>.
  - Please turn the audio into text and append the event label at the end, formatted as <>.
  - Transcribe the audio and add the audio event label at the end, in <> format.
  - Please transcribe the audio and add the audio event label at the end, in <> format.
  - Please convert the audio into text and include the audio event label at the end in <>.
  - Turn the audio into text and append the audio event label at the end, in <> format.
  - Please transcribe the audio and add the audio event label at the end in <> format.
  - Please transcribe the audio content and insert the audio event label at the end, in <> format.
  - Transcribe the audio and add the event label at the end in <>.
  - Please convert the audio into written text and insert the audio event label at the end, formatted as <>.
  - Please turn the audio into written text and include the audio event label at the end in <>.
  - Transcribe the audio into text and append the event label at the end in <> format.
  - Please transcribe the audio and insert the audio event label at the end, formatted as <>.
  - 请将以下音频转化为文字，并在结尾处标注音频事件标签，标签格式为<>。
  - 将音频转录成文字，并在文本末尾插入音频事件标签，格式为<>。
  - 请将音频内容转录为文字，并在文本最后加上音频事件标签，标签格式为<>。
  - 请将音频转写成文字，并在结尾标注音频事件标签，标签应为<>。
  - 将音频内容转化为文字，并在文末附加音频事件标签，标签格式为<>。
  - 请把音频内容转写为文字，并在结尾插入音频事件标签，标签格式为<>。
  - 请将音频转录成文字记录，并在记录末尾添加音频事件标签，标签格式为<>。
  - 请将音频中的内容转录成文字，并在文本结尾插入音频事件标签，标签格式为<>。
  - 将音频转录为文字，并在结尾附加音频事件标签，标签格式为<>。
  - 请将音频内容转化为文字记录，并在最后附加音频事件标签，标签格式为<>。
  - 请转录音频为文字，并在最后加上音频事件标签，标签格式为<>。
  - 将音频中的话语转写成文字，并在文本末尾加上音频事件标签，标签格式为<>。
  - 请将音频内容转写为文字记录，并在记录末尾标明音频事件标签，标签格式为<>。
  - 请将以下音频转录为文字，并在文末加上音频事件标签，标签格式为<>。
  - 请将音频内容转化为文字，并在文字的最后部分标注音频事件标签，标签格式为<>。
  - 将音频内容转录为文字，并在文本结尾添加音频事件标签，标签格式为<>。
  - 请将音频转录成文字并在末尾加上音频事件标签，标签格式为<>。
  - 请将音频转化为文字记录，并在末尾插入音频事件标签，标签格式为<>。
  - 将音频内容转录成文字，并在最后插入音频事件标签，标签格式为<>。
  - 请将音频转录为文字，并在结尾附加音频事件标签，标签格式为<>。
  - 请将音频转写成文字，并在末尾加上音频事件标签，标签格式为<>。
  - 请转写音频内容为文字，并在最后标明音频事件标签，标签格式为<>。
  - 请将音频内容转录为文字，并在文本末尾插入音频事件标签，标签格式为<>。
  - 请将音频转化为文字，并在文本最后加上音频事件标签，标签格式为<>。
  - 请将音频转录为文字，并在结尾处插入音频事件标签，标签格式为<>。
  - 请将音频内容转写为文字记录，并在记录末尾加上音频事件标签，标签格式为<>。
  - 请将音频中的内容转写为文字，并在末尾插入音频事件标签，标签格式为<>。
  - 请将音频转化为文字并在最后加上音频事件标签，标签格式为<>。
  - 请转录音频内容并在末尾加上音频事件标签，标签格式为<>。
  - 请将音频中的内容转录为文字，并在文本末尾标明音频事件标签，标签格式为<>。
  - 请将音频内容转化为文字，并在文末插入音频事件标签，标签格式为<>。
  - 将音频转录为文字，并在最后插入音频事件标签，标签格式为<>。
  - 请将音频转写为文字，并在文本末尾附上音频事件标签，标签格式为<>。
  - 请将音频中的话语转录成文字，并在文本末尾加上音频事件标签，标签格式为<>。
  - 请将音频内容转化为文字，并在末尾标明音频事件标签，标签格式为<>。
  - 请转写音频内容为文字，并在文末标注音频事件标签，标签格式为<>。
  - 将音频内容转录为文字，并在末尾附加音频事件标签，标签格式为<>。
  - 请将音频内容转写为文字，并在文本最后插入音频事件标签，标签格式为<>。
  - 请转录音频为文字并在结尾附加音频事件标签，标签格式为<>。
  - 请将音频转化为文字，并在文本末尾加上音频事件标签，标签格式为<>。
  - 请将音频转录为文字，并在末尾插入音频事件标签，标签格式为<>。
  - 请将音频中的话语转化为文字，并在结尾附加音频事件标签，标签格式为<>。
  - 将音频内容转录成文字，并在末尾加上音频事件标签，标签格式为<>。
  - 请将音频转化为文字，并在结尾标注音频事件标签，标签格式为<>。
  - 请将音频转录为文字，并在结尾附加音频事件标签，标签格式为<>。
  - 请将音频内容转化为文字，并在结尾插入音频事件标签，标签格式为<>。
  - 请转写音频为文字，并在最后标注音频事件标签，标签格式为<>。
  - 将音频转写为文字，并在结尾附加音频事件标签，标签格式为<>。
  - 请将音频转化为文字，并在文本末尾附上音频事件标签，标签格式为<>。
  - 请将音频中的内容转化为文字，并在末尾标注音频事件标签，标签格式为<>。
  - 请转录音频为文字，并在文本最后加上音频事件标签，标签格式为<>。
  - 请将音频内容转写成文字，并在末尾加上音频事件标签，标签格式为<>。
  - 请将音频内容转化为文字，并在文末附上音频事件标签，标签格式为<>。
  - 将音频内容转写为文字，并在末尾加上音频事件标签，标签格式为<>。
  - 请将音频转化为文字，并在最后插入音频事件标签，标签格式为<>。
  - 请将音频内容转化为文字并在结尾处添加音频事件标签，标签格式为<>。
  - 请将音频转写为文字，并在末尾附加音频事件标签，标签格式为<>。
  - 将音频内容转写为文字，并在最后加上音频事件标签，标签格式为<>。
  - 请将音频转录成文字，并在结尾加上音频事件标签，标签格式为<>。
  - 请将音频内容转化为文字，并在文末插入音频事件标签，标签格式为<>。
  - 将音频转写为文字，并在结尾处标注音频事件标签，标签格式为<>。
  - 请将音频转化为文字，并在文本末尾插入音频事件标签，标签格式为<>。
  - 请将音频内容转录为文字，并在末尾插入音频事件标签，标签格式为<>。

<CAPTION>:
  - 分辨音频中的事件，直接返回标签，格式为<>。
  - 检测音频中的音频事件并以标签的形式输出，标签格式为<>。
  - 分析音频内容，提取并输出相关音频事件标签，标签格式为<>。
  - 对音频内容进行分析，给出音频事件标签，标签格式为<>。
  - 判断音频中的事件，直接返回标签，标签格式为<>。
  - 请对音频进行事件检测并以标签格式呈现，格式为<>。
  - 判断以下音频中有哪些音频事件并返回标签，格式为<>。
  - 对音频文件进行事件检测，输出音频事件标签，标签格式为<>。
  - 分析音频文件中的事件并返回标签，标签格式为<>。
  - 请听取音频并识别其中的音频事件，返回标签格式为<>。
  - 检测音频中所含事件并输出标签，标签格式为<>。
  - 对音频进行标签化，返回音频事件标签，标签格式为<>。
  - 对音频内容进行事件标识，返回标签格式为<>。
  - 请分析以下音频并返回其包含的音频事件标签，标签格式为<>。
  - 请对音频内容进行事件分析并输出标签，标签格式为<>。
  - 识别音频中的事件并输出标签，格式为<>。
  - 对音频进行音频事件分析，输出标签，格式为<>。
  - 请检测音频中的所有事件，并返回标签格式为<>。
  - 听取并分辨音频中的事件，输出标签格式为<>。
  - 请分析音频并给出包含的音频事件标签，标签格式为<>。
  - 请识别音频中的所有事件，返回事件标签，格式为<>。
  - 请听音频并返回包含的音频事件标签，标签格式为<>。
  - 识别音频中的事件并按标签返回，标签格式为<>。
  - 请对音频事件进行标注并输出标签，标签格式为<>。
  - 对以下音频事件进行标注，输出标签，标签格式为<>。
  - 识别并返回音频中的所有事件标签，格式为<>。
  - 请听取音频并标识其中的事件，输出标签格式为<>。
  - 检测音频中的事件并输出标签，标签格式为<>。
  - 请分析音频并识别事件，输出标签格式为<>。
  - 听取音频并分辨事件，标签格式为<>。
  - 请识别以下音频中的所有音频事件并返回标签，格式为<>。
  - 对音频中的事件进行分析，输出标签格式为<>。
  - 对音频进行事件分类，并返回标签，格式为<>。
  - 听音频并返回所有音频事件标签，标签格式为<>。
  - 请分析音频并输出相应的事件标签，标签格式为<>。
  - 对音频进行音频事件识别，输出标签格式为<>。
  - 分析音频中的音频事件并标注，标签格式为<>。
  - 请识别音频中的事件并返回对应标签，标签格式为<>。
  - 请分析音频文件中的事件，并输出标签格式为<>。
  - 请检测音频文件中包含的事件并返回标签，格式为<>。
  - 对音频内容进行事件标签化，返回标签格式为<>。
  - 识别音频中的音频事件并直接输出标签，格式为<>。
  - 请对音频中的事件进行检测并返回标签，标签格式为<>。
  - 检测音频文件中的音频事件并返回标签，格式为<>。
  - 请对音频进行事件分析，输出标签格式为<>。
  - 请分析音频并返回所有事件标签，标签格式为<>。
  - 请判断音频中包含哪些事件并标注，标签格式为<>。
  - 对音频文件中的事件进行识别，并输出标签，格式为<>。
  - 请识别音频内容中的事件并返回标签，格式为<>。
  - 请识别并返回音频中的所有事件标签，标签格式为<>。
  - 对以下音频进行分析并返回事件标签，格式为<>。
  - 请对音频进行事件检测，返回标签格式为<>。
  - 对以下音频进行音频事件标注，返回标签格式为<>。
  - 请分析以下音频，返回音频事件标签，格式为<>。
  - 请对音频进行事件识别，返回标签格式为<>。
  - 听取音频并识别音频中的事件，输出标签格式为<>。
  - 请分析并标注音频中的所有事件标签，格式为<>。
  - 对音频中的事件进行识别，直接返回标签，格式为<>。
  - 请分辨音频中的音频事件并返回标签，标签格式为<>。
  - 请识别并返回音频中的所有事件标签，格式为<>。
  - 对音频中的事件进行标注并返回标签，格式为<>。
  - 请分析音频并输出其中包含的事件标签，标签格式为<>。
  - 对音频文件进行分析并返回音频事件标签，格式为<>。
  - 请检测音频中的音频事件并返回标签，标签格式为<>。
  - 请判断音频内容中的音频事件，返回标签格式为<>。
  - 请对音频内容进行分析并给出事件标签，格式为<>。
  - 请识别并返回音频中的事件，标签格式为<>。
  - 请分析音频内容并返回标签，格式为<>。
  - Please identify the audio events in the following clip and provide event tags in the format <>.
  - Please analyze the audio and output event tags in the format <>.
  - Detect the audio events in this clip and return the tags in the format <>.
  - Identify and return the audio event tags in the format <>.
  - Listen to the audio and return the event tags in the format <>.
  - Please listen to the audio clip and output the event tags in the format <>.
  - Please analyze the audio content and provide the event tags in the format <>.
  - Please identify the events in the audio and return the tags in the format <>.
  - Analyze the audio and output the relevant event tags in the format <>.
  - Detect the events in the audio clip and output the tags in the format <>.
  - Please listen to the audio and identify the events, return the tags in the format <>.
  - Please analyze the audio clip and return the event tags in the format <>.
  - Please analyze the following audio and provide the event tags in the format <>.
  - Listen to the audio and detect the events, return the event tags in the format <>.
  - Please identify and return the event tags for the audio in the format <>.
  - Please detect the audio events in the clip and return the event tags in the format <>.
  - Analyze the audio and provide event tags in the format <>.
  - Identify the audio events in this clip and return the event tags in the format <>.
  - Please identify the events in the audio and return the tags in the format <>.
  - Please analyze the following audio and return event tags in the format <>.
  - Please detect the events in the audio and return the event tags in the format <>.
  - Listen to the audio and return the event tags in the format <>.
  - Identify the audio events and output the event tags in the format <>.
  - Please detect the events in the audio and output the event tags in the format <>.
  - Listen to the audio and return the event tags in the format <>.
  - Analyze the audio content and output the event tags in the format <>.
  - 请分辨下列音频中的事件并返回标签，格式为<>。
  - 请分析音频文件中的事件并输出标签，标签格式为<>。
  - 请识别音频中的音频事件并标记，标签格式为<>。
  - 识别并返回音频中包含的事件标签，标签格式为<>。
  - 请对以下音频内容进行事件检测并返回标签，标签格式为<>。
  - 请判断音频中包含的事件并输出标签，标签格式为<>。
  - 请分析音频中的事件并给出标签，标签格式为<>。
  - 对以下音频进行音频事件分析，返回标签格式为<>。
  - 请分析以下音频文件中的事件并返回标签，格式为<>。
  - 识别音频中的事件并返回标签，标签格式为<>。
  - 请听取并识别音频中的事件，返回标签格式为<>。
  - 请对音频进行事件检测并给出标签，格式为<>。
  - 请判断以下音频中所包含的音频事件，返回标签，格式为<>。
  - 对音频进行事件识别并返回标签，标签格式为<>。
  - 请分析音频内容并输出事件标签，标签格式为<>。
  - 请分辨以下音频中的事件并输出标签，格式为<>。
  - 请检测音频中的事件并返回标签，格式为<>。
  - 请分析以下音频并输出事件标签，格式为<>。
  - 请判断音频文件中的事件并给出标签，标签格式为<>。
  - 对音频文件进行事件识别，输出标签格式为<>。
  - 识别音频中的事件并直接返回标签，格式为<>。
  - 对以下音频进行事件识别并给出标签，格式为<>。
  - 请分析音频并识别其中的音频事件，返回标签格式为<>。
  - 请识别并输出音频中的事件标签，标签格式为<>。
  - 对音频中的事件进行识别并标记，标签格式为<>。
  - 请分析并输出音频事件标签，标签格式为<>。
  - 请识别以下音频中的所有音频事件，返回标签格式为<>。
  - 对音频内容进行事件标注并返回标签，标签格式为<>。
  - 请对音频进行分析并输出事件标签，标签格式为<>。
  - 识别音频事件并输出标签，格式为<>。
  - 请分析音频并识别其中的所有事件，返回标签格式为<>。
  - 请分辨以下音频中的事件并标注标签，格式为<>。
  - 请判断音频中包含的音频事件，并直接返回标签，格式为<>。
  - 请检测音频中的事件，并返回事件标签，标签格式为<>。
  - 请分析音频文件并识别其中的事件，输出标签格式为<>。
  - 识别音频中的事件并输出标签，标签格式为<>。
  - 请分辨音频中包含的事件并返回标签，标签格式为<>。
  - 对音频进行事件检测并输出标签，标签格式为<>。
  - 请判断音频中的事件并直接返回标签，格式为<>。
  - 对音频进行事件分析并输出标签，标签格式为<>。
  - 请分辨并返回音频中的事件标签，格式为<>。
  - 请识别并输出音频中的事件标签，格式为<>。
  - 对以下音频文件进行事件检测并输出标签，格式为<>。
  - 请分析音频中的所有事件并返回标签，标签格式为<>。
  - 识别音频中的所有事件并返回标签，标签格式为<>。
  - 对音频进行事件检测，直接返回标签，标签格式为<>。
  - 请分析并标识音频中的音频事件，标签格式为<>。
  - 请判断以下音频中的事件并给出标签，标签格式为<>。
  - 识别音频文件中的音频事件并返回标签，标签格式为<>。
  - 对音频进行音频事件检测并返回标签，标签格式为<>。
  - 请分辨音频中的事件并返回标签，标签格式为<>。
  - 识别音频中的所有事件并返回标签，标签格式为<>。
  - 请分析以下音频并输出音频事件标签，标签格式为<>。
  - 对音频进行音频事件识别，返回标签，格式为<>。
  - 对以下音频进行事件分析并返回标签，格式为<>。
  - 请检测并识别音频中的事件，返回标签格式为<>。
  - 请分析音频并返回事件标签，标签格式为<>。
  - 请对音频进行事件识别并返回标签，标签格式为<>。
  - 请分析并分辨音频中的事件，返回标签格式为<>。
  - 请听取音频并识别其中的音频事件，返回标签格式为<>。
  - 请判断音频内容中的音频事件，并返回标签格式为<>。
  - 请对音频进行事件分析，输出标签格式为<>。
  - 请分析音频并识别其中的音频事件，输出标签格式为<>.
  - 请分辨音频中的音频事件并返回标签，标签格式为<>。
  - 识别音频中的事件并给出标签，标签格式为<>。
  - 请分析以下音频并直接返回音频事件标签，标签格式为<>。
  - 请对音频文件中的音频事件进行检测，返回标签格式为<>。
  - 请对音频进行音频事件检测，输出标签格式为<>.
  - Please analyze the audio and provide the event tags in the format <>.
  - Please listen to the audio and identify the events, return the tags in the format <>.
  - Please detect the audio events and return the tags in the format <>.
  - Please identify the audio events and return the event tags in the format <>.
  - Please analyze the audio content and output the event tags in the format <>.
  - Please detect and return the event tags for the audio in the format <>.
  - Please analyze the following audio and return the event tags in the format <>.
  - Please detect the events in the audio and return the tags in the format <>.
  - Please analyze and identify the events in the audio, output the event tags in the format <>.
  - Please listen to the audio and provide the event tags in the format <>.



<CAPTION> <AUDIO>:
  - 分辨音频中的事件，直接返回标签，格式为<>。
  - 检测音频中的音频事件并以标签的形式输出，标签格式为<>。
  - 分析音频内容，提取并输出相关音频事件标签，标签格式为<>。
  - 对音频内容进行分析，给出音频事件标签，标签格式为<>。
  - 判断音频中的事件，直接返回标签，标签格式为<>。
  - 请对音频进行事件检测并以标签格式呈现，格式为<>。
  - 判断以下音频中有哪些音频事件并返回标签，格式为<>。
  - 对音频文件进行事件检测，输出音频事件标签，标签格式为<>。
  - 分析音频文件中的事件并返回标签，标签格式为<>。
  - 请听取音频并识别其中的音频事件，返回标签格式为<>。
  - 检测音频中所含事件并输出标签，标签格式为<>。
  - 对音频进行标签化，返回音频事件标签，标签格式为<>。
  - 对音频内容进行事件标识，返回标签格式为<>。
  - 请分析以下音频并返回其包含的音频事件标签，标签格式为<>。
  - 请对音频内容进行事件分析并输出标签，标签格式为<>。
  - 识别音频中的事件并输出标签，格式为<>。
  - 对音频进行音频事件分析，输出标签，格式为<>。
  - 请检测音频中的所有事件，并返回标签格式为<>。
  - 听取并分辨音频中的事件，输出标签格式为<>。
  - 请分析音频并给出包含的音频事件标签，标签格式为<>。
  - 请识别音频中的所有事件，返回事件标签，格式为<>。
  - 请听音频并返回包含的音频事件标签，标签格式为<>。
  - 识别音频中的事件并按标签返回，标签格式为<>。
  - 请对音频事件进行标注并输出标签，标签格式为<>。
  - 对以下音频事件进行标注，输出标签，标签格式为<>。
  - 识别并返回音频中的所有事件标签，格式为<>。
  - 请听取音频并标识其中的事件，输出标签格式为<>。
  - 检测音频中的事件并输出标签，标签格式为<>。
  - 请分析音频并识别事件，输出标签格式为<>。
  - 听取音频并分辨事件，标签格式为<>。
  - 请识别以下音频中的所有音频事件并返回标签，格式为<>。
  - 对音频中的事件进行分析，输出标签格式为<>。
  - 对音频进行事件分类，并返回标签，格式为<>。
  - 听音频并返回所有音频事件标签，标签格式为<>。
  - 请分析音频并输出相应的事件标签，标签格式为<>。
  - 对音频进行音频事件识别，输出标签格式为<>。
  - 分析音频中的音频事件并标注，标签格式为<>。
  - 请识别音频中的事件并返回对应标签，标签格式为<>。
  - 请分析音频文件中的事件，并输出标签格式为<>。
  - 请检测音频文件中包含的事件并返回标签，格式为<>。
  - 对音频内容进行事件标签化，返回标签格式为<>。
  - 识别音频中的音频事件并直接输出标签，格式为<>。
  - 请对音频中的事件进行检测并返回标签，标签格式为<>。
  - 检测音频文件中的音频事件并返回标签，格式为<>。
  - 请对音频进行事件分析，输出标签格式为<>。
  - 请分析音频并返回所有事件标签，标签格式为<>。
  - 请判断音频中包含哪些事件并标注，标签格式为<>。
  - 对音频文件中的事件进行识别，并输出标签，格式为<>。
  - 请识别音频内容中的事件并返回标签，格式为<>。
  - 请识别并返回音频中的所有事件标签，标签格式为<>。
  - 对以下音频进行分析并返回事件标签，格式为<>。
  - 请对音频进行事件检测，返回标签格式为<>。
  - 对以下音频进行音频事件标注，返回标签格式为<>。
  - 请分析以下音频，返回音频事件标签，格式为<>。
  - 请对音频进行事件识别，返回标签格式为<>。
  - 听取音频并识别音频中的事件，输出标签格式为<>。
  - 请分析并标注音频中的所有事件标签，格式为<>。
  - 对音频中的事件进行识别，直接返回标签，格式为<>。
  - 请分辨音频中的音频事件并返回标签，标签格式为<>。
  - 请识别并返回音频中的所有事件标签，格式为<>。
  - 对音频中的事件进行标注并返回标签，格式为<>。
  - 请分析音频并输出其中包含的事件标签，标签格式为<>。
  - 对音频文件进行分析并返回音频事件标签，格式为<>。
  - 请检测音频中的音频事件并返回标签，标签格式为<>。
  - 请判断音频内容中的音频事件，返回标签格式为<>。
  - 请对音频内容进行分析并给出事件标签，格式为<>。
  - 请识别并返回音频中的事件，标签格式为<>。
  - 请分析音频内容并返回标签，格式为<>。
  - Please identify the audio events in the following clip and provide event tags in the format <>.
  - Please analyze the audio and output event tags in the format <>.
  - Detect the audio events in this clip and return the tags in the format <>.
  - Identify and return the audio event tags in the format <>.
  - Listen to the audio and return the event tags in the format <>.
  - Please listen to the audio clip and output the event tags in the format <>.
  - Please analyze the audio content and provide the event tags in the format <>.
  - Please identify the events in the audio and return the tags in the format <>.
  - Analyze the audio and output the relevant event tags in the format <>.
  - Detect the events in the audio clip and output the tags in the format <>.
  - Please listen to the audio and identify the events, return the tags in the format <>.
  - Please analyze the audio clip and return the event tags in the format <>.
  - Please analyze the following audio and provide the event tags in the format <>.
  - Listen to the audio and detect the events, return the event tags in the format <>.
  - Please identify and return the event tags for the audio in the format <>.
  - Please detect the audio events in the clip and return the event tags in the format <>.
  - Analyze the audio and provide event tags in the format <>.
  - Identify the audio events in this clip and return the event tags in the format <>.
  - Please identify the events in the audio and return the tags in the format <>.
  - Please analyze the following audio and return event tags in the format <>.
  - Please detect the events in the audio and return the event tags in the format <>.
  - Listen to the audio and return the event tags in the format <>.
  - Identify the audio events and output the event tags in the format <>.
  - Please detect the events in the audio and output the event tags in the format <>.
  - Listen to the audio and return the event tags in the format <>.
  - Analyze the audio content and output the event tags in the format <>.
  - 请分辨下列音频中的事件并返回标签，格式为<>。
  - 请分析音频文件中的事件并输出标签，标签格式为<>。
  - 请识别音频中的音频事件并标记，标签格式为<>。
  - 识别并返回音频中包含的事件标签，标签格式为<>。
  - 请对以下音频内容进行事件检测并返回标签，标签格式为<>。
  - 请判断音频中包含的事件并输出标签，标签格式为<>。
  - 请分析音频中的事件并给出标签，标签格式为<>。
  - 对以下音频进行音频事件分析，返回标签格式为<>。
  - 请分析以下音频文件中的事件并返回标签，格式为<>。
  - 识别音频中的事件并返回标签，标签格式为<>。
  - 请听取并识别音频中的事件，返回标签格式为<>。
  - 请对音频进行事件检测并给出标签，格式为<>。
  - 请判断以下音频中所包含的音频事件，返回标签，格式为<>。
  - 对音频进行事件识别并返回标签，标签格式为<>。
  - 请分析音频内容并输出事件标签，标签格式为<>。
  - 请分辨以下音频中的事件并输出标签，格式为<>。
  - 请检测音频中的事件并返回标签，格式为<>。
  - 请分析以下音频并输出事件标签，格式为<>。
  - 请判断音频文件中的事件并给出标签，标签格式为<>。
  - 对音频文件进行事件识别，输出标签格式为<>。
  - 识别音频中的事件并直接返回标签，格式为<>。
  - 对以下音频进行事件识别并给出标签，格式为<>。
  - 请分析音频并识别其中的音频事件，返回标签格式为<>。
  - 请识别并输出音频中的事件标签，标签格式为<>。
  - 对音频中的事件进行识别并标记，标签格式为<>。
  - 请分析并输出音频事件标签，标签格式为<>。
  - 请识别以下音频中的所有音频事件，返回标签格式为<>。
  - 对音频内容进行事件标注并返回标签，标签格式为<>。
  - 请对音频进行分析并输出事件标签，标签格式为<>。
  - 识别音频事件并输出标签，格式为<>。
  - 请分析音频并识别其中的所有事件，返回标签格式为<>。
  - 请分辨以下音频中的事件并标注标签，格式为<>。
  - 请判断音频中包含的音频事件，并直接返回标签，格式为<>。
  - 请检测音频中的事件，并返回事件标签，标签格式为<>。
  - 请分析音频文件并识别其中的事件，输出标签格式为<>。
  - 识别音频中的事件并输出标签，标签格式为<>。
  - 请分辨音频中包含的事件并返回标签，标签格式为<>。
  - 对音频进行事件检测并输出标签，标签格式为<>。
  - 请判断音频中的事件并直接返回标签，格式为<>。
  - 对音频进行事件分析并输出标签，标签格式为<>。
  - 请分辨并返回音频中的事件标签，格式为<>。
  - 请识别并输出音频中的事件标签，格式为<>。
  - 对以下音频文件进行事件检测并输出标签，格式为<>。
  - 请分析音频中的所有事件并返回标签，标签格式为<>。
  - 识别音频中的所有事件并返回标签，标签格式为<>。
  - 对音频进行事件检测，直接返回标签，标签格式为<>。
  - 请分析并标识音频中的音频事件，标签格式为<>。
  - 请判断以下音频中的事件并给出标签，标签格式为<>。
  - 识别音频文件中的音频事件并返回标签，标签格式为<>。
  - 对音频进行音频事件检测并返回标签，标签格式为<>。
  - 请分辨音频中的事件并返回标签，标签格式为<>。
  - 识别音频中的所有事件并返回标签，标签格式为<>。
  - 请分析以下音频并输出音频事件标签，标签格式为<>。
  - 对音频进行音频事件识别，返回标签，格式为<>。
  - 对以下音频进行事件分析并返回标签，格式为<>。
  - 请检测并识别音频中的事件，返回标签格式为<>。
  - 请分析音频并返回事件标签，标签格式为<>。
  - 请对音频进行事件识别并返回标签，标签格式为<>。
  - 请分析并分辨音频中的事件，返回标签格式为<>。
  - 请听取音频并识别其中的音频事件，返回标签格式为<>。
  - 请判断音频内容中的音频事件，并返回标签格式为<>。
  - 请对音频进行事件分析，输出标签格式为<>。
  - 请分析音频并识别其中的音频事件，输出标签格式为<>.
  - 请分辨音频中的音频事件并返回标签，标签格式为<>。
  - 识别音频中的事件并给出标签，标签格式为<>。
  - 请分析以下音频并直接返回音频事件标签，标签格式为<>。
  - 请对音频文件中的音频事件进行检测，返回标签格式为<>。
  - 请对音频进行音频事件检测，输出标签格式为<>.
  - Please analyze the audio and provide the event tags in the format <>.
  - Please listen to the audio and identify the events, return the tags in the format <>.
  - Please detect the audio events and return the tags in the format <>.
  - Please identify the audio events and return the event tags in the format <>.
  - Please analyze the audio content and output the event tags in the format <>.
  - Please detect and return the event tags for the audio in the format <>.
  - Please analyze the following audio and return the event tags in the format <>.
  - Please detect the events in the audio and return the tags in the format <>.
  - Please analyze and identify the events in the audio, output the event tags in the format <>.
  - Please listen to the audio and provide the event tags in the format <>.


<TRANSCRIBE> <GENDER>:
  - 请将音频转录为文字，并在文本末尾标注性别标签，标签格式为<>。
  - 请将这段音频转录为文字，并在最后附上性别标签，标签格式为<>。
  - 请将音频中的内容转写为文本，并在结尾处加入性别标签，标签格式为<>。
  - 请转录音频中的内容，最后附加性别标签，格式为<>。
  - 请将音频内容转化为文字，并在文本结尾处附上性别标签，标签格式为<>。
  - 请把音频转录成文字，并在最后加上性别标签，标签格式为<>。
  - 请将音频内容转为文字，最后在文本末尾标注性别标签，格式为<>。
  - 请将这段音频转换成文字，最后附上性别标签，标签格式为<>。
  - 请将音频内容转写成文字，并在结尾处标注性别标签，标签格式为<>。
  - 请将这段音频转换为文字，并在文本末尾加上性别标签，格式为<>。
  - 请将音频中的话转录成文字，并在结尾处加入性别标签，格式为<>。
  - 请将音频转为文字，并在最后加上性别标签，格式为<>。
  - 请把这段音频转录为文字，并在文本末尾添加性别标签，标签格式为<>。
  - 请将音频内容转为文字，并在文本最后附加性别标签，格式为<>。
  - 请将音频转写成文字，并在结尾处标注性别标签，格式为<>。
  - 请将音频内容转化为文字，并在最后附加性别标签，标签格式为<>。
  - 请把音频转录成文字，并在文本结尾附加性别标签，标签格式为<>。
  - 请将音频中的内容转录为文字，并在结尾加上性别标签，格式为<>。
  - 请将音频转为文字，并在末尾附上性别标签，标签格式为<>。
  - 请将音频中的内容转录为文字，并在最后添加性别标签，标签格式为<>。
  - 请把音频转写成文字，并在最后附加性别标签，标签格式为<>。
  - 请将音频内容转为文字，并在文本最后加上性别标签，格式为<>。
  - 请将音频转录为文本，并在结尾附上性别标签，标签格式为<>。
  - 请将音频转写成文字，并在末尾添加性别标签，标签格式为<>。
  - 请将音频转化为文字，并在最后加上性别标签，标签格式为<>。
  - 请将音频中的内容转写为文本，并在末尾附加性别标签，标签格式为<>。
  - 请将音频转为文字，并在结尾处加上性别标签，格式为<>。
  - 请将音频内容转录为文字，并在最后添加性别标签，标签格式为<>。
  - 请将这段音频转录为文字，并在文本末尾附上性别标签，标签格式为<>。
  - 请把音频内容转写为文字，并在文本结尾附加性别标签，标签格式为<>。
  - 请将音频内容转录成文字，并在结尾加上性别标签，标签格式为<>。
  - 请将音频内容转化为文字，并在文本末尾附加性别标签，格式为<>。
  - 请将这段音频转录成文字，并在结尾处加上性别标签，标签格式为<>。
  - 请将音频内容转写成文字，并在末尾添加性别标签，标签格式为<>。
  - 请将音频中的内容转录为文字，并在文本末尾标注性别标签，格式为<>。
  - 请把音频转化为文字，并在结尾处附上性别标签，标签格式为<>。
  - 请将音频转写成文字，并在末尾标注性别标签，格式为<>。
  - 请将音频转录为文字，并在结尾附上性别标签，标签格式为<>。
  - 请将音频中的内容转为文字，并在末尾附上性别标签，标签格式为<>。
  - 请把这段音频转写为文字，并在最后附上性别标签，格式为<>。
  - 请将音频转为文字，并在结尾加上性别标签，标签格式为<>。
  - 请将音频转化为文字，并在文本结尾附加性别标签，标签格式为<>。
  - 请将音频中的内容转写成文字，并在文本末尾添加性别标签，标签格式为<>。
  - 请将音频中的话转录成文字，并在结尾处加上性别标签，标签格式为<>。
  - Please transcribe this audio to text and add a gender label at the end in the format <>.
  - Please transcribe the audio content into text and attach a gender label at the end in the format <>.
  - Please convert the audio to text and append a gender label at the end in the format <>.
  - Please transcribe the audio and add a gender label at the end in the format <>.
  - Please transcribe the content of the audio into text and add a gender label at the end in the format <>.
  - Please transcribe this audio into text and include a gender label at the end in the format <>.
  - Please convert the audio into text and append a gender label at the end in the format <>.
  - Please transcribe the audio to text and append a gender label at the end in the format <>.
  - Please transcribe the audio content into text and include a gender label at the end in the format <>.
  - Please transcribe the audio to text and at the end include a gender label in the format <>.
  - Please transcribe this audio and at the end add a gender label in the format <>.
  - Please transcribe the audio into text and at the end append a gender label in the format <>.
  - Please transcribe the content from the audio into text and add a gender label at the end in the format <>.
  - Please transcribe the audio into text and append a gender label at the end in the format <>.
  - Please transcribe the audio into text and add a gender label at the end in the format <>.
  - Please transcribe the audio and add a gender label at the end in the format <>.
  - Please convert this audio to text and add a gender label at the end in the format <>.
  - Please transcribe this audio content into text and include a gender label at the end in the format <>.
  - Please transcribe this audio and append a gender label at the end in the format <>.
  - Please convert the audio to text and append a gender label in the format <> at the end.
  - Please transcribe the audio and add a gender label at the end in the format <>.
  - Please transcribe the audio content into text and add a gender label at the end in the format <>.
  - Please convert the audio into text and at the end append a gender label in the format <>.
  - Please transcribe this audio to text and add the gender label at the end in the format <>.
  - Please transcribe the audio and add a gender label in the format <> at the end.
  - Please convert the audio into text and attach a gender label at the end in the format <>.
  - Please transcribe the audio to text and include the gender label at the end in the format <>.
  - Please transcribe the audio into text and append a gender label at the end in the format <>.
  - 请将音频内容转录为文字，并在结尾附加性别标签，标签格式为<>。
  - 请将这段音频转录为文字，并在文本末尾标注性别标签，标签格式为<>。
  - 请将音频转写成文字，并在最后加上性别标签，标签格式为<>。
  - 请将音频转录为文字，并在文本结尾添加性别标签，标签格式为<>。
  - 请将音频转录成文字，并在末尾附加性别标签，标签格式为<>。
  - 请将这段音频转写为文字，并在最后附加性别标签，标签格式为<>。
  - 请将音频转换为文本，并在结尾附加性别标签，标签格式为<>。
  - 请把音频内容转录为文字，并在文本末尾加上性别标签，标签格式为<>。
  - 请将音频中的内容转录为文字，并在最后附加性别标签，标签格式为<>。
  - 请把这段音频转写为文字，并在结尾附上性别标签，标签格式为<>。
  - 请将音频内容转写为文字，并在文本末尾标注性别标签，标签格式为<>。
  - 请将这段音频内容转写为文本，并在结尾处加上性别标签，标签格式为<>。
  - 请将音频转为文字，并在最后附加性别标签，格式为<>。
  - 请将音频转化为文字，并在文本结尾处附上性别标签，标签格式为<>。
  - 请把音频转录为文字，并在文本末尾加上性别标签，标签格式为<>。
  - 请将音频内容转换为文本，并在结尾处加上性别标签，标签格式为<>。
  - 请将音频转为文字，并在结尾添加性别标签，标签格式为<>。
  - 请将音频内容转录为文字，并在文本末尾加上性别标签，标签格式为<>。
  - 请把音频转化为文字，并在最后附加性别标签，标签格式为<>。
  - 请将音频转写成文字，并在结尾处标注性别标签，标签格式为<>。
  - 请将这段音频转换为文字，并在结尾处添加性别标签，标签格式为<>。
  - 请把这段音频内容转写成文字，并在文本末尾附加性别标签，标签格式为<>。
  - 请将音频中的内容转写为文字，并在文本结尾处加上性别标签，标签格式为<>。
  - 请将音频转化为文本，并在末尾添加性别标签，标签格式为<>。
  - 请将音频内容转写成文字，并在结尾附上性别标签，标签格式为<>。
  - 请将音频中的内容转为文字，并在最后加上性别标签，标签格式为<>。
  - 请将音频内容转写为文本，并在结尾附加性别标签，标签格式为<>。
  - 请将音频转写成文字，并在末尾加上性别标签，标签格式为<>。
  - 请将音频中的话转写为文字，并在最后附上性别标签，标签格式为<>。
  - 请将音频转写成文字，并在结尾处加上性别标签，标签格式为<>。
  - 请将音频转录为文字，并在文本最后附加性别标签，标签格式为<>。
  - 请将音频中的内容转录成文字，并在最后添加性别标签，标签格式为<>。
  - 请将音频内容转化为文字，并在末尾附加性别标签，标签格式为<>。
  - 请把音频转录为文字，并在最后添加性别标签，标签格式为<>。
  - 请将这段音频转录成文字，并在文本末尾附加性别标签，标签格式为<>。
  - 请将音频内容转写成文字，并在最后加上性别标签，标签格式为<>。
  - 请将音频转录为文字，并在文本末尾标注性别标签，标签格式为<>。
  - 请把音频转录为文字，并在结尾附上性别标签，标签格式为<>。
  - 请将音频转写成文字，并在最后添加性别标签，标签格式为<>。
  - 请将音频中的内容转写为文字，并在末尾标注性别标签，标签格式为<>。
  - 请把音频内容转录成文字，并在文本结尾加上性别标签，标签格式为<>。
  - 请将这段音频转录为文字，并在最后加上性别标签，标签格式为<>。
  - 请将音频内容转录成文字，并在结尾添加性别标签，标签格式为<>。
  - 请将音频内容转录为文本，并在文本末尾附加性别标签，标签格式为<>。
  - 请将音频中的话转录成文字，并在最后标注性别标签，标签格式为<>。
  - 请将音频转化为文字，并在文本结尾处附加性别标签，标签格式为<>。
  - 请将音频转录为文字，并在结尾处附加性别标签，标签格式为<>。
  - 请把音频转写为文字，并在结尾附加性别标签，标签格式为<>。
  - 请将这段音频内容转为文字，并在末尾附上性别标签，标签格式为<>。
  - 请将音频转录成文字，并在末尾附加性别标签，标签格式为<>。
  - 请将音频内容转换为文字，并在最后加上性别标签，标签格式为<>。
  - 请把音频转写成文字，并在最后标注性别标签，标签格式为<>。
  - 请将音频转写成文字，并在结尾处添加性别标签，标签格式为<>。
  - 请将音频中的内容转写为文本，并在最后附上性别标签，标签格式为<>。
  - 请将这段音频转录为文字，并在结尾附上性别标签，标签格式为<>。
  - 请将音频转录为文字，并在最后标注性别标签，标签格式为<>。
  - 请将音频转写为文本，并在结尾添加性别标签，标签格式为<>。
  - 请将音频内容转录为文字，并在结尾附加性别标签，标签格式为<>。
  - 请将音频转写成文字，并在文本末尾附加性别标签，标签格式为<>。
  - 请将音频转为文字，并在文本末尾标注性别标签，标签格式为<>。
  - 请将这段音频转写为文字，并在结尾处附加性别标签，标签格式为<>.
  - 请将音频内容转化为文字，并在结尾处添加性别标签，标签格式为<>.
  - 请将音频内容转写为文字，并在末尾加上性别标签，标签格式为<>.
  - 请将音频中的话转写为文字，并在结尾附加性别标签，标签格式为<>.



<GENDER>:
  - "请判断如下语音中发言者的性别，直接出性别标签，标签格式为<>."
  - "识别语音中说话者的性别，标注性别标签，标签格式为<>."
  - "请判断语音中发言者的性别，并标注性别标签，标签格式为<>."
  - "请通过语音判断发言者的性别，并给出性别标签，标签格式为<>."
  - "请通过如下音频中发言者的声音判断其的性别，给出性别标签，标签格式为<>."
  - "分析此语音并标注发言者的性别，标签格式为<>."
  - "请根据以下语音样本判断说话者的性别，标签格式为<>."
  - "请识别语音中的发言者性别并返回性别标签，标签格式为<>."
  - "给出音频中的发言者性别标签，格式为<>."
  - "根据下述音频内容判断说话者性别，返回性别标签，格式为<>."
  - "判断下面的音频发言者性别，并给出性别标签，标签格式为<>."
  - "请依据语音内容判断发言者性别，并返回性别标签，标签格式为<>."
  - "请标注语音中的说话者性别，标签格式为<>."
  - "通过以下语音判断发言者的性别并标注标签，格式为<>."
  - "请分析此语音并输出性别标签，标签格式为<>."
  - "根据音频判断发言者的性别，并给出性别标签，标签格式为<>."
  - "请根据语音中的内容判断说话者的性别，标注性别标签，标签格式为<>."
  - "通过音频判断发言者的性别，并返回性别标签，标签格式为<>."
  - "请通过音频样本判断性别，标注性别标签，标签格式为<>."
  - "请分析音频，判断发言者性别并标注标签，格式为<>."
  - "分析以下语音并判断性别，返回性别标签，格式为<>."
  - "通过语音判断发言者性别，并输出标签，格式为<>."
  - "请根据发音特征判断发言者性别，返回性别标签，格式为<>."
  - "请识别音频中的发言者性别，并标注标签，格式为<>."
  - "给出音频中的发言者性别标注，标签格式为<>."
  - "分析音频并标注发言者性别，格式为<>."
  - "根据语音样本判断发言者性别，并标注标签，标签格式为<>."
  - "请根据下述语音判断发言者性别，并标注性别标签，格式为<>."
  - "通过语音分析，判断说话者性别并标注性别标签，标签格式为<>."
  - "根据语音中的声音特征判断性别，给出标签，格式为<>."
  - "通过下面的语音判断发言者性别，标注标签格式为<>."
  - "请根据以下音频中的声音特征识别发言者性别，标签格式为<>."
  - "根据语音内容判断说话者性别并标注，标签格式为<>."
  - "请通过以下音频判断发言者的性别并返回性别标签，标签格式为<>."
  - "请根据语音中的特征判断性别，标签格式为<>."
  - "请分析此语音并判断发言者性别，标签格式为<>."
  - "根据语音样本判断发言者的性别，并返回性别标签，标签格式为<>."
  - "请通过下列语音判断性别并输出性别标签，格式为<>."
  - "请从下列语音判断说话者性别，输出标签，格式为<>."
  - "根据音频判断说话者的性别，标注性别标签，格式为<>."
  - "请分析音频样本并给出性别标签，标签格式为<>."
  - "根据语音中的发音特征判断发言者性别并输出标签，格式为<>."
  - "通过分析音频识别发言者性别，标注标签，格式为<>."
  - "判断此语音中的性别并返回标签，格式为<>."
  - "分析此语音的发音特征并标注性别标签，标签格式为<>."
  - "请根据发音的语音特征判断发言者性别，标签格式为<>."
  - "通过分析语音内容判断性别，并返回标签格式为<>."
  - "请通过语音样本判断发言者性别，并返回标签，格式为<>."
  - "分析此语音并给出性别标签，标签格式为<>."
  - "请根据下列音频分析发言者性别，返回标签格式为<>."
  - "请通过分析音频中的发音特征判断发言者性别，标签格式为<>."
  - "根据语音样本判断发言者的性别并返回标签，标签格式为<>."
  - "通过音频分析判断发言者的性别并给出标签，标签格式为<>."
  - "请根据语音判断发言者的性别并返回标签，格式为<>."
  - "分析语音样本并判断性别，标签格式为<>."
  - "根据以下音频内容识别性别并返回标签，格式为<>."
  - "通过语音样本判断性别并给出标签，标签格式为<>."
  - "通过音频样本判断性别并标注标签，标签格式为<>."
  - "Please determine the gender of the speaker in the following audio and return the gender label in the format <>."
  - "Identify the gender of the speaker in the audio and provide the gender label, format <>."
  - "Please analyze the audio and determine the speaker's gender, returning the gender label in the format <>."
  - "Determine the gender of the speaker from the audio and mark the gender label in the format <>."
  - "Based on the audio provided, identify the gender of the speaker and provide the label in the format <>."
  - "Analyze the audio and provide the gender label for the speaker, format <>."
  - "Identify the speaker's gender in the following audio and return the label in the format <>."
  - "Please determine the gender of the speaker and output the gender label in the format <>."
  - "Analyze the audio and return the gender label of the speaker, format <>."
  - "Please identify the speaker's gender based on the audio and provide the label in the format <>."
  - "Based on the following audio, determine the speaker's gender and return the gender label in the format <>."
  - "Please listen to the audio and return the gender label of the speaker in the format <>."
  - "Identify the gender of the speaker and mark it with the appropriate label in the format <>."
  - "Determine the gender of the speaker in this audio and output the label in the format <>."
  - "Please determine the gender of the speaker from the audio sample and provide the label in the format <>."
  - "Analyze the audio and determine the speaker's gender, returning the label in the format <>."
  - "Identify the gender of the speaker and return the label in the format <>."
  - "Please analyze the provided audio and determine the gender of the speaker, then output the label in the format <>."
  - "Determine the gender from the audio and provide the label in the format <>."
  - "Please listen to the audio and mark the gender of the speaker in the format <>."
  - "Analyze the audio and return the gender of the speaker in the format <>."
  - "Identify the gender of the speaker from the following audio and output the label in the format <>."
  - "Please determine the gender of the speaker based on the audio sample and mark it with the appropriate label in the format <>."
  - "Listen to the audio and determine the speaker's gender, then return the label in the format <>."
  - "Please analyze the speaker's audio and return the gender label in the format <>."
  - "Based on the provided audio, determine the speaker's gender and return the label in the format <>."
  - "Identify the speaker's gender based on the audio and return the label, format <>."
  - "Analyze the audio and return the gender label for the speaker, format <>."
  - "Please listen to the audio and determine the gender of the speaker, then output the label in the format <>."
  - "Based on the audio sample, determine the gender of the speaker and return the gender label in the format <>."
  - "Identify the gender of the speaker from the provided audio and mark the label in the format <>."
  - "Please determine the gender of the speaker and output the gender label, format <>."
  - "Determine the speaker's gender based on the audio and return the label in the format <>."
  - "Please listen to the audio sample and identify the gender of the speaker, returning the label in the format <>."
  - "Based on the audio, determine the gender of the speaker and return the gender label, format <>."
  - "Analyze the audio and return the gender label of the speaker, format <>."
  - "Listen to the audio and provide the gender label for the speaker, format <>."
  - "Please analyze the audio and return the gender label in the format <>."
  - "Identify the speaker's gender based on the audio sample and provide the label in the format <>."
  - "Based on the given audio, identify the speaker's gender and return the label in the format <>."
  - "Analyze the audio content and determine the speaker's gender, then output the label in the format <>."
  - "请判断如下语音中发言者的性别，直接出性别标签，标签格式为<>."
  - "识别语音中说话者的性别，标注性别标签，标签格式为<>."
  - "请判断语音中发言者的性别，并标注性别标签，标签格式为<>."
  - "请通过语音判断发言者的性别，并给出性别标签，标签格式为<>."
  - "请通过如下音频中发言者的声音判断其的性别，给出性别标签，标签格式为<>."
  - "请根据以下音频样本判断发言者的性别，返回性别标签，格式为<>."
  - "通过下列语音判断发言者的性别并给出标签，格式为<>."
  - "根据此音频判断说话者性别并标注性别标签，格式为<>."
  - "请听音频并判断说话者性别，标注性别标签，格式为<>."
  - "请根据下面的语音分析发言者性别，返回性别标签，格式为<>."
  - "通过此音频判断发言者的性别，给出性别标签，标签格式为<>."
  - "请分析此语音并判断说话者的性别，返回性别标签，格式为<>."
  - "请听取以下音频，并给出性别标签，标签格式为<>."
  - "通过语音样本判断说话者性别，并返回性别标签，格式为<>."
  - "根据音频内容判断发言者的性别，并标注标签，格式为<>."
  - "请根据以下语音样本判断发言者性别，标注性别标签，格式为<>."
  - "请根据此音频判断说话者性别并返回性别标签，标签格式为<>."
  - "请判断音频中的发言者性别并返回性别标签，格式为<>."
  - "请通过语音判断性别，标注性别标签，标签格式为<>."
  - "根据下面的音频判断发言者性别，返回标签，格式为<>."
  - "根据音频样本识别发言者性别并标注标签，格式为<>."
  - "请通过以下语音识别发言者性别，返回标签，格式为<>."
  - "请通过音频判断发言者性别并输出标签，格式为<>."
  - "请根据音频判断性别并返回标签，格式为<>."
  - "分析此音频并给出发言者性别标签，标签格式为<>."
  - "请根据音频判断性别并标注标签，标签格式为<>."
  - "请判断语音中的发言者性别并输出性别标签，格式为<>."
  - "请通过语音样本判断发言者性别并返回性别标签，标签格式为<>."
  - "请分析此音频并输出性别标签，格式为<>."
  - "请根据以下音频内容判断发言者性别并标注标签，标签格式为<>."
  - "根据音频中的声音判断发言者性别并标注标签，格式为<>."
  - "请根据音频分析说话者的性别，返回性别标签，格式为<>."
  - "根据以下语音判断性别并给出标签，格式为<>."
  - "请从以下音频判断发言者性别，返回性别标签，标签格式为<>."
  - "请分析音频中的发言者性别，并输出标签，格式为<>."
  - "请通过音频样本判断说话者性别并标注标签，标签格式为<>."
  - "根据此音频判断发言者性别，并输出性别标签，标签格式为<>."
  - "根据此语音判断发言者性别并给出标签，格式为<>."
  - "请从音频判断发言者性别并返回标签，标签格式为<>."
  - "请通过音频样本判断发言者性别并返回性别标签，格式为<>."
  - "通过音频分析判断发言者性别并返回性别标签，标签格式为<>."
  - "请通过分析音频样本判断发言者性别并标注标签，格式为<>."
  - "请根据音频样本分析发言者性别并输出标签，标签格式为<>."
  - "请从音频中判断说话者性别并标注标签，格式为<>."
  - "通过此音频判断发言者性别，并标注标签，格式为<>."
  - "请根据以下语音分析性别并标注标签，标签格式为<>."
  - "请识别此音频中的发言者性别并返回性别标签，标签格式为<>."
  - "请通过下面的音频分析发言者的性别并标注标签，标签格式为<>."
  - "请从以下音频样本判断发言者性别并返回标签，格式为<>."
  - "请通过此音频判断说话者性别并给出标签，标签格式为<>."
  - "请根据音频中的发言者声音判断性别并标注标签，格式为<>."
  - "根据音频样本判断性别并标注标签，标签格式为<>."
  - "请通过音频判断发言者的性别并给出标签，标签格式为<>."
  - "请根据下列音频判断发言者的性别并标注性别标签，标签格式为<>."
  - "根据音频样本判断发言者性别，输出标签，格式为<>."
  - "请分析此音频并输出性别标签，标签格式为<>."
  - "请根据音频中的说话者声音判断性别，返回标签，格式为<>."
  - "根据音频样本分析发言者的性别并返回标签，标签格式为<>."
  - "请判断音频中的发言者性别并标注标签，格式为<>."
  - "请通过音频样本判断发言者性别并标注性别标签，标签格式为<>."
  - "通过音频内容判断发言者性别并返回性别标签，格式为<>."
  - "通过下列音频判断说话者的性别并标注标签，标签格式为<>."
  - "请判断语音中发言者的性别并返回标签，格式为<>."





<TRANSCRIBE> <AGE>:
  - "请将这段音频转录成文字，并在末尾加上年龄标签，格式为<>。"
  - "将音频内容转录为文本，并在最后附上年龄标签，格式为<>。"
  - "请将以下音频内容转成文字，并在文字最后加上年龄标签，格式为<>。"
  - "音频转录完成后，请在文字末尾添加一个年龄标签，标签格式为<>。"
  - "请将音频转换成文字，并在文字的结尾加上年龄标签，标签格式为<>。"
  - "请对音频进行转录，并在最终文本后附上年龄标签，标签格式为<>。"
  - "将这段音频的内容转成文字，并在最后标明年龄标签，格式为<>。"
  - "请将音频内容详细转录成文字，并在转录文本末尾加上年龄标签，格式为<>。"
  - "将音频转录为文本，并在文本末尾标注年龄标签，格式为<>。"
  - "请将音频转化为文字，并在文字末尾附加年龄标签，格式为<>。"
  - "音频转录后，请加上年龄标签，格式为<>，并放在文本的最后。"
  - "请将这段音频转录成文字，并在文字结尾处加入年龄标签，标签格式为<>。"
  - "将音频内容转写为文字，并在末尾加上年龄标签，格式为<>。"
  - "请将这段音频详细转录成文字，并在结尾标注年龄标签，标签格式为<>。"
  - "请将音频转录为文字，并在末尾附加年龄标签，标签格式为<>。"
  - "请将音频转换为文字，并在结尾附上年龄标签，标签格式为<>。"
  - "音频转录完成后，请在转录文本末尾标注年龄标签，标签格式为<>。"
  - "将音频内容转化为文字，并在文本结尾加入年龄标签，标签格式为<>。"
  - "请转录这段音频并在最后添加年龄标签，格式为<>。"
  - "请将音频转写成文字，并在文本的结尾处加上年龄标签，格式为<>。"
  - "将音频转录为文本，并在最后加入年龄标签，标签格式为<>。"
  - "请将这段音频转录成文字，并在最后附加年龄标签，标签格式为<>。"
  - "请将音频内容转录为文字，并在文字最后标明年龄标签，标签格式为<>。"
  - "将音频转换成文字后，请在文本末尾标注年龄标签，标签格式为<>。"
  - "请将音频内容转写成文字，并在文本末尾加上年龄标签，标签格式为<>。"
  - "将音频转化为文字，并在最后标注年龄标签，标签格式为<>。"
  - "音频转录完成后，请在文本末尾标注年龄标签，标签格式为<>。"
  - "请将音频转录并加上年龄标签，格式为<>，并放在文字最后。"
  - "请将音频内容转写成文字并附上年龄标签，标签格式为<>。"
  - "将这段音频转成文字，并在末尾附加年龄标签，标签格式为<>。"
  - "请将音频转录为文字并加上年龄标签，标签格式为<>，放在文字最后。"
  - "请将音频内容转录并在结尾标注年龄标签，标签格式为<>。"
  - "音频转录后，请在结尾加上年龄标签，标签格式为<>。"
  - "请将音频转录成文字，并在最后加上年龄标签，标签格式为<>。"
  - "将这段音频的内容转成文字后，请在末尾附加年龄标签，标签格式为<>。"
  - "请转录音频并在末尾标注年龄标签，标签格式为<>。"
  - "音频转录完成后，请在文字末尾加上年龄标签，标签格式为<>。"
  - "将音频转录成文字，并在结尾添加年龄标签，标签格式为<>。"
  - "请将音频内容转写成文字并标注年龄标签，格式为<>。"
  - "请将音频内容转录成文字，并在末尾添加年龄标签，标签格式为<>。"
  - "将音频转换成文字后，请加上年龄标签，标签格式为<>。"
  - "请转录这段音频，并在末尾附加年龄标签，格式为<>。"
  - "请将这段音频转成文字，并在文字结尾标注年龄标签，标签格式为<>。"
  - "将音频内容转录为文字，并在结尾附上年龄标签，标签格式为<>。"
  - "请将音频内容详细转录并在结尾加上年龄标签，标签格式为<>。"
  - "将这段音频转化为文字，并在文字的末尾加上年龄标签，标签格式为<>。"
  - "请将音频转录为文字，并在最后添加年龄标签，标签格式为<>。"
  - "请转录这段音频并在末尾加入年龄标签，标签格式为<>。"
  - "请将音频转写成文字并标注年龄标签，标签格式为<>。"
  - "将音频内容转写为文字，并在结尾附上年龄标签，标签格式为<>。"
  - "请将音频转录成文字，并在文本末尾加入年龄标签，标签格式为<>。"
  - "请将音频转换成文字，并在文本的结尾加上年龄标签，标签格式为<>。"
  - "音频转录完成后，请附加年龄标签，标签格式为<>，并放在最后。"
  - "请将音频转录成文字，并在最后标注年龄标签，格式为<>。"
  - "将这段音频转成文字，并在结尾处附上年龄标签，标签格式为<>。"
  - "请转录音频内容，并在文字末尾附上年龄标签，标签格式为<>。"
  - "请将音频内容转化为文字，并在末尾标注年龄标签，标签格式为<>。"
  - "将音频转写为文字，并在文字的结尾加上年龄标签，标签格式为<>。"
  - "请将音频内容转录成文字，并在末尾加上年龄标签，标签格式为<>。"
  - "音频转录完成后，文字末尾请附上年龄标签，标签格式为<>。"
  - "请将音频转录成文字，并在文字结尾处标注年龄标签，格式为<>。"
  - "请将音频转录成文字，并在末尾加上年龄标签，标签格式为<>。"
  - "将音频内容转写为文字，并在最后附上年龄标签，标签格式为<>。"
  - "请转录音频并在结尾处标注年龄标签，标签格式为<>。"
  - "请将这段音频转写成文字，并在文字结尾处附加年龄标签，标签格式为<>。"
  - "请转录音频并在末尾标明年龄标签，标签格式为<>。"
  - "将音频转写成文字后，请在文字末尾添加年龄标签，标签格式为<>。"
  - "音频转录完成后，请在文本末尾加上年龄标签，标签格式为<>。"
  - "请将音频内容转成文字，并在结尾附加年龄标签，标签格式为<>。"
  - "请将音频内容转写为文字，并在末尾添加年龄标签，标签格式为<>。"
  - "请转录音频并在末尾加入年龄标签，标签格式为<>。"
  - "音频转录完成后，在文字末尾加上年龄标签，标签格式为<>。"
  - "请将音频转写为文字，并在文本末尾附上年龄标签，标签格式为<>。"
  - "请转录音频内容并标注年龄标签，标签格式为<>，放在最后。"
  - "请将音频内容转写为文字，并在结尾标注年龄标签，标签格式为<>。"
  - "请将音频内容转录为文字，并在最后附上年龄标签，标签格式为<>。"
  - "音频转录完成后，请在文字末尾加上年龄标签，标签格式为<>。"
  - "请将音频转写成文字，并在文字末尾加上年龄标签，标签格式为<>。"
  - "将音频内容转写成文字并标注年龄标签，标签格式为<>。"
  - "将音频内容转录为文字，并在文字结尾附上年龄标签，标签格式为<>。"
  - "请将音频转录为文字，并在末尾加上年龄标签，标签格式为<>。"
  - "请将音频转换成文字，并在文字的末尾加上年龄标签，标签格式为<>。"
  - "将这段音频转写为文字，并在末尾加上年龄标签，标签格式为<>。"
  - "请将音频内容转写成文字，并在结尾加上年龄标签，标签格式为<>。"
  - "将音频转录为文字后，请在文本结尾添加年龄标签，标签格式为<>。"
  - "请转录音频并在结尾标明年龄标签，标签格式为<>。"
  - "将音频转录为文字，并在结尾附上年龄标签，标签格式为<>。"
  - "音频转录完成后，在文本末尾加上年龄标签，标签格式为<>。"
  - "Please transcribe the audio and add an age tag at the end in the format <>."
  - "Transcribe the following audio into text and append an age tag at the end in the format <>."
  - "Please transcribe the audio and include an age tag at the end, format <>."
  - "Convert the audio into text and add an age tag at the end in the format <>."
  - "Please transcribe the audio and place an age tag at the end of the transcription in the format <>."
  - "Transcribe the audio into text and add the age tag at the end, format <>."
  - "Transcribe this audio and at the end add an age tag in the format <>."
  - "Please transcribe the audio content into text and append the age tag at the end in the format <>."
  - "Transcribe the audio content and add the age tag at the end of the text, format <>."
  - "Please transcribe this audio and include an age tag at the end, format <>."
  - "Please convert the audio into text and append an age tag at the end, format <>."
  - "After transcribing the audio, add an age tag at the end in the format <>."
  - "Please transcribe the audio and add an age label at the end in the format <>."
  - "Transcribe the audio into text, and at the end, include an age tag in the format <>."
  - "Please transcribe the audio and add an age tag at the end in the format <>."
  - "Please transcribe the audio and include an age label at the end in the format <>."
  - "Convert the audio into text and add the age tag at the end of the transcription in the format <>."
  - "Please transcribe the audio and at the end, append an age tag in the format <>."
  - "Transcribe the audio and place the age tag at the end, format <>."

<AGE>:
  - 判断下列语音中说话者的年龄，并提供一个年龄标签，标签格式为<>。
  - 请根据下面的音频判断发言者的年龄，输出标签格式为<>。
  - 请从音频中辨别说话者的年龄，并输出对应的年龄标签，格式为<>。
  - 请分析以下语音，判断说话者年龄并给出年龄标签，标签格式为<>。
  - 根据下面的语音，判定说话者的年龄并提供年龄标签，标签格式为<>。
  - 通过以下语音的内容，判断发言者的年龄并给出年龄标签，标签格式为<>。
  - 通过这段音频判断发言者年龄，并直接输出年龄标签，标签格式为<>。
  - 根据以下音频判断说话者的年龄，标签格式为<>。
  - 请分析这段语音并判断发言者的年龄，输出年龄标签，标签格式为<>。
  - 请根据以下语音给出说话者的年龄标签，格式为<>。
  - 请判断下面音频中的说话者年龄，并给出年龄标签，格式为<>。
  - 根据语音，判断发言者的年龄并输出年龄标签，标签格式为<>。
  - 判断该语音中的说话者年龄并输出标签，格式为<>。
  - 请从下列音频分析说话者的年龄，给出年龄标签，格式为<>。
  - 请分析以下语音，判断说话者年龄，并给出年龄标签，标签格式为<>。
  - 请判断下列语音的发言者年龄并输出年龄标签，格式为<>。
  - 请根据下面的语音判断说话者的年龄并给出年龄标签，格式为<>。
  - 请从以下音频中辨别说话者年龄，并给出年龄标签，标签格式为<>。
  - 请根据以下语音分析说话者年龄，并给出年龄标签，标签格式为<>。
  - 请分析这段音频，判断说话者的年龄标签，标签格式为<>。
  - 请根据语音判断发言者年龄并提供年龄标签，标签格式为<>。
  - 请根据下列音频判断说话者的年龄并输出年龄标签，标签格式为<>。
  - 请分析以下语音，判断发言者年龄并给出年龄标签，标签格式为<>。
  - 请分析语音并判断说话者年龄，输出年龄标签，标签格式为<>。
  - 请根据以下音频判断发言者的年龄并提供年龄标签，标签格式为<>。
  - 请分析语音，判断发言者的年龄并输出年龄标签，标签格式为<>。
  - 请判断这段语音中说话者的年龄，并提供年龄标签，标签格式为<>。
  - 请根据下面的语音文件判断发言者年龄并输出年龄标签，标签格式为<>。
  - 请分析以下语音中的发言者的年龄，并给出年龄标签，标签格式为<>。
  - 判断下列语音中的发言者年龄，并输出年龄标签，标签格式为<>。
  - 请根据音频判断说话者的年龄并给出年龄标签，标签格式为<>。
  - 请根据下面音频判断发言者的年龄，并输出年龄标签，标签格式为<>。
  - 请通过语音判断发言者的年龄，并给出相应的年龄标签，标签格式为<>。
  - 请根据音频中的发言判断说话者的年龄，并提供年龄标签，标签格式为<>。
  - 根据语音内容判断发言者的年龄并提供年龄标签，标签格式为<>。
  - 请根据音频内容判断说话者的年龄并给出年龄标签，标签格式为<>。
  - 请通过下列音频判断发言者的年龄，并给出相应的年龄标签，标签格式为<>。
  - 请分析音频，判断说话者年龄并给出年龄标签，标签格式为<>。
  - 请从下面的音频中分析发言者的年龄，并提供年龄标签，标签格式为<>。
  - 请分析语音文件，判断发言者年龄并提供年龄标签，标签格式为<>。
  - 请根据以下音频判断发言者年龄并给出年龄标签，标签格式为<>。
  - 根据下列音频判断说话者的年龄并提供年龄标签，标签格式为<>。
  - 请分析以下音频，判断发言者的年龄，标签格式为<>。
  - 请通过音频判断发言者年龄，并提供年龄标签，标签格式为<>。
  - 请判断下列语音的发言者年龄并给出年龄标签，标签格式为<>。
  - 根据音频文件，判断说话者的年龄并提供年龄标签，标签格式为<>。
  - 根据下列语音判断发言者年龄并给出年龄标签，标签格式为<>。
  - 请根据音频中的说话者声音判断年龄，标签格式为<>。
  - 请根据以下语音判断发言者年龄并输出年龄标签，标签格式为<>。
  - 请根据这段语音分析发言者年龄并提供年龄标签，标签格式为<>。
  - 判断以下语音中发言者的年龄，并给出相应的年龄标签，标签格式为<>。
  - 根据下列语音判断发言者的年龄，并给出年龄标签，标签格式为<>。
  - 请根据以下音频文件判断说话者的年龄，并给出年龄标签，标签格式为<>。
  - 请通过以下音频判断发言者的年龄并提供年龄标签，标签格式为<>。
  - 请根据以下语音判断说话者的年龄并给出年龄标签，标签格式为<>。
  - 判断下列语音中的说话者年龄，格式为<>。
  - 请分析音频，判断说话者年龄并给出年龄标签，标签格式为<>。
  - 请根据音频分析发言者的年龄，标签格式为<>。
  - 请通过下面的音频判断发言者的年龄并给出年龄标签，标签格式为<>。
  - 请根据以下音频判断说话者的年龄并输出年龄标签，标签格式为<>。
  - 请根据语音判断发言者年龄，并输出年龄标签，标签格式为<>。
  - 请分析以下音频，给出发言者的年龄标签，标签格式为<>。
  - 请根据下列音频判断发言者年龄并给出年龄标签，标签格式为<>。
  - 请根据音频分析发言者的年龄并输出年龄标签，标签格式为<>。
  - Please analyze the audio and identify the speaker's age, then provide the age label in the format of <>.
  - Based on the audio, determine the age of the speaker and provide the corresponding age label, format it as <>.
  - Please identify the speaker's age from the audio and output the age label in the format of <>.
  - Analyze the following audio and provide the age of the speaker along with the corresponding age label, format it as <>.
  - Based on the audio, identify the age of the speaker and provide the age label, formatted as <>.
  - From the following audio, determine the speaker's age and provide the age label in the format of <>.
  - Please identify the speaker's age and output the corresponding age label in the format of <>.
  - Analyze the following audio and determine the speaker's age, then provide the age label in the format of <>.
  - Based on the audio file, determine the speaker's age and provide the age label, format as <>.
  - Please listen to the audio and determine the age of the speaker, then give the age label in the format of <>.
  - Based on the audio, identify the speaker's age and output the corresponding age label, format it as <>.
  - Please determine the age of the speaker from the audio and output the corresponding age label, formatted as <>.
  - Analyze the audio and identify the age of the speaker, then provide the age label in the format of <>.
  - Based on the audio, determine the speaker's age and output the corresponding age label, format it as <>.
  - From the audio, identify the speaker's age and provide the age label in the format of <>.
  - Analyze the audio and provide the speaker's age, then output the age label in the format of <>.
  - Based on the audio, determine the speaker's age and provide the corresponding age label, format it as <>.
  - Please listen to the audio and determine the speaker's age, then provide the age label formatted as <>.
  - Please determine the speaker's age and provide the age label, formatted as <>.
  - Please analyze the audio and give the age of the speaker, then output the corresponding age label formatted as <>.





<TRANSCRIBE> <AGE> <GENDER>:
  - 请将这段音频转录为文字，并在文本结尾处附上年龄和性别标签，格式为<>。
  - 请将下面的音频内容转录成文本，最后加上年龄和性别标签，标签格式为<>。
  - 请将音频转换成文字，并在结尾处标注年龄和性别，标签格式为<>。
  - 请将该音频转化为文字，并在文本末尾加上年龄性别标签，标签格式为<>。
  - 请对这段音频进行文字转录，并在最后添加年龄和性别信息，标签格式为<>。
  - 请将下面的音频内容转化为文字，并在文字末尾附上年龄和性别标签，格式为<>。
  - 请将音频转换成文字，并在文本的末尾加上年龄和性别，标签格式为<>。
  - 请将音频内容转写成文字，最后加上年龄和性别标签，标签格式为<>。
  - 请将音频文件中的内容转录为文字，并在最后附上年龄和性别标签，标签格式为<>。
  - 请将音频转成文字，并在文本结尾注明年龄和性别，标签格式为<>。
  - 请对该音频进行转录，并在转录后的文本末尾加上年龄和性别标签，格式为<>。
  - 请把以下音频内容转成文字，并在最后加上年龄和性别，标签格式为<>。
  - 请将音频中的内容转换为文字，最后添加年龄和性别标签，标签格式为<>。
  - 请将这段音频转录为文字，并在文字结尾处附加年龄和性别信息，格式为<>。
  - 请将音频转为文字，并在最后附加年龄和性别标签，标签格式为<>。
  - 请将该段音频的内容转化为文本，并在文本最后加上年龄与性别标签，标签格式为<>。
  - 请把音频内容转写成文字，并在文本末尾附上年龄与性别标签，标签格式为<>。
  - 请将音频文件转录为文字，并在结尾加上年龄和性别信息，标签格式为<>。
  - 请将该段音频转写为文字，并在末尾附上年龄和性别标签，格式为<>。
  - 请将这段音频转录为文本，并在最后标注年龄和性别，标签格式为<>。
  - 请把音频中的内容转录成文字，并在最后加上年龄和性别标签，标签格式为<>。
  - 请将音频转录为文本，最后加上年龄性别标签，标签格式为<>。
  - 请将这段音频转成文字，并在结尾加上年龄与性别信息，标签格式为<>。
  - 请将音频内容转录为文字，并在文字末尾标注年龄和性别标签，标签格式为<>。
  - 请将以下音频内容转换成文本，并在最后标注年龄和性别标签，格式为<>。
  - 请将音频内容转化为文字，并在文本结尾附加年龄与性别标签，格式为<>。
  - 请将该音频中的内容转写为文字，并在结尾标注年龄和性别，标签格式为<>。
  - 请对音频进行文字转录，并在最后加上年龄与性别标签，格式为<>。
  - 请将音频内容转化为文字，并在结尾处标明年龄和性别，标签格式为<>。
  - 请将音频转换成文字，并在文本末尾附加年龄与性别标签，标签格式为<>。
  - 请将音频中的内容转录为文字，并在文本最后加上年龄和性别标签，格式为<>。
  - 请将音频转成文字，并在结尾附上年龄和性别标签，标签格式为<>。
  - 请将该段音频转录成文字，并在最后标明年龄和性别，标签格式为<>。
  - 请将音频转成文字，并在文本末尾加上年龄性别标签，格式为<>。
  - 请将音频内容转写成文字，并在末尾加上年龄性别信息，标签格式为<>。
  - 请把音频内容转化为文字，并在最后标注年龄和性别标签，标签格式为<>。
  - 请将音频内容转录为文本，并在结尾加上年龄与性别，标签格式为<>。
  - 请将音频中的内容转化为文字，并在最后加上年龄和性别，标签格式为<>。
  - 请将该音频转写为文字，并在末尾加上年龄与性别标签，格式为<>。
  - 请将音频转化为文字，并在最后附上年龄和性别标签，标签格式为<>。
  - 请将音频内容转录为文字，并在文本结尾标明年龄和性别，标签格式为<>。
  - 请对这段音频进行文字转录，并在结尾加上年龄性别信息，标签格式为<>。
  - 请将该段音频转化为文字，并在文本最后附上年龄和性别标签，标签格式为<>。
  - 请将音频转成文字，并在结尾加上年龄性别标签，标签格式为<>。
  - 请将音频转化为文字，并在最后标注年龄和性别标签，标签格式为<>。
  - 请将该音频中的内容转写为文字，并在末尾附上年龄性别标签，标签格式为<>。
  - 请将音频内容转写成文本，并在最后附加年龄和性别信息，标签格式为<>。
  - 请将音频文件转录为文字，并在结尾处添加年龄与性别标签，标签格式为<>。
  - 请把音频内容转录为文字，并在文本末尾加上年龄与性别标签，格式为<>。
  - 请将音频转写为文字，并在文字结尾附上年龄性别信息，标签格式为<>。
  - 请将音频转化为文字，并在结尾附加年龄与性别标签，标签格式为<>。
  - 请将这段音频转换为文字，并在末尾附加年龄性别标签，标签格式为<>。
  - 请将该段音频转录成文字，并在文本末尾加上年龄和性别标签，格式为<>。
  - 请将音频内容转写为文本，并在结尾加上年龄和性别标签，标签格式为<>。
  - 请把该音频转写为文字，并在末尾附上年龄和性别标签，格式为<>。
  - 请将音频中的内容转录为文本，并在最后标明年龄性别，标签格式为<>。
  - 请将音频转为文字，并在文字结尾加上年龄性别信息，标签格式为<>。
  - 请将该音频内容转录为文字，并在结尾处标明年龄性别标签，格式为<>。
  - 请把音频内容转写成文字，并在结尾加上年龄和性别标签，格式为<>。
  - 请将音频转录为文字，并在结尾处附加年龄与性别标签，格式为<>。
  - 请将音频内容转换成文字，并在文字末尾附加年龄性别标签，标签格式为<>。
  - 请把这段音频内容转录成文字，并在末尾加上年龄与性别标签，标签格式为<>。
  - 请将音频转化为文字，并在最后加上年龄性别信息，标签格式为<>。
  - 请将音频中的内容转写为文字，并在最后附加年龄性别标签，标签格式为<>。
  - 请将音频文件转写为文字，并在结尾加上年龄性别标签，标签格式为<>。
  - 请将音频转写成文字，并在文本最后加上年龄性别标签，标签格式为<>。
  - 请将该音频中的内容转写为文字，并在文本结尾标明年龄性别，标签格式为<>。
  - Please transcribe this audio into text and add age and gender information at the end of the transcription, formatted as <>.
  - Transcribe the following audio into text and append the age and gender information at the end, formatted as <>.
  - Please transcribe the audio into text and mark age and gender at the end of the text in the format <>.
  - Convert the audio into text and append age and gender information at the end of the transcription, in the format <>.
  - Please transcribe the audio content and add age and gender tags at the end of the text, formatted as <>.
  - Transcribe the following audio into text, and add age and gender information at the end in the format <>.
  - Convert the audio into text and add age and gender tags at the end of the transcription, formatted as <>.
  - Please convert the audio into written form and add age and gender labels at the end, formatted as <>.
  - Please transcribe the audio content into text and append age and gender labels at the end, formatted as <>.
  - Please transcribe the audio into text and add age and gender tags at the end, formatted as <>.
  - Transcribe this audio into text and append age and gender information at the end in the format <>.
  - Convert the audio content into text and add age and gender information at the end, formatted as <>.
  - Please convert the audio into text and append age and gender information at the end, formatted as <>.
  - Transcribe the audio into text and mark age and gender at the end in the format <>.
  - Please transcribe the audio content into text and append age and gender tags at the end, formatted as <>.
  - Convert this audio into written text and add age and gender tags at the end in the format <>.
  - Please transcribe the audio content into text and append age and gender labels, formatted as <>.
  - Convert the audio into text and mark age and gender at the end in the format <>.
  - Transcribe the audio into text and append age and gender information in the format <>.

<AGE> <GENDER>:
  - "请识别以下音频发言者的年龄和性别，标签格式为<>。"
  - "请识别以下音频中的说话者的年龄和性别,标签格式为<>。"
  - "请判断下列音频中的发言者的年龄与性别,标签格式为<>。"
  - "请确定以下音频中讲话人的年龄和性别,标签格式为<>。"
  - "请分析下列音频中的说话人的年龄和性别,标签格式为<>。"
  - "通过对音频的分析，判断发言者的年龄和性别，标签格式为<>。"
  - "根据以下音频识别讲话者的年龄和性别,输出标签格式为<>。"
  - "请通过音频数据判断发言者的性别和年龄,标签格式为<>。"
  - "请对以下音频中的发言者进行年龄和性别分析,标签格式为<>。"
  - "根据提供的音频，识别讲话者的年龄和性别,标签格式为<>。"
  - "请听取下列音频，判断其中说话者的年龄和性别,标签格式为<>。"
  - "请对以下音频发言者的年龄和性别做出判断，标签格式为<>。"
  - "请判断音频中的说话人年龄与性别，标签格式为<>。"
  - "通过音频识别发言者的年龄和性别,请以<>标签格式给出答案。"
  - "请分析音频，确认讲话者的年龄和性别，输出格式为<>标签。"
  - "对以下音频进行年龄和性别的判断，标签格式为<>。"
  - "通过分析该音频判断说话者的年龄和性别,请以<>标签形式呈现。"
  - "根据下列音频内容，请识别发言者的年龄和性别,标签格式为<>。"
  - "请判断音频中的讲话者年龄和性别，并以<>标签格式返回结果。"
  - "通过对音频的听取，分析发言者的年龄和性别，标签格式为<>。"
  - "请基于音频内容分析发言者的年龄和性别，输出标签为<>。"
  - "根据音频的内容，判断其中讲话人的年龄和性别，并以<>标签格式给出。"
  - "请确定音频中的发言者的年龄和性别，标签格式为<>。"
  - "请分析以下音频，给出讲话者的年龄和性别标签，格式为<>。"
  - "根据音频分析发言者的性别和年龄，输出标签格式为<>。"
  - "根据提供的音频文件，确定说话者的性别和年龄，标签格式为<>。"
  - "请从以下音频中识别发言者的性别和年龄，并返回标签格式<>。"
  - "请对音频内容进行分析，确定说话者的年龄和性别，格式为<>标签。"
  - "通过音频内容分析，给出发言者的性别和年龄，标签格式为<>。"
  - "请识别此音频中的发言者的年龄和性别，并以<>格式返回标签。"
  - "请根据下列音频判断说话人的年龄和性别，标签格式为<>。"
  - "通过分析音频，判断其中讲话者的年龄和性别，格式为<>标签。"
  - "请识别音频中的讲话者的性别和年龄，标签格式为<>。"
  - "通过音频判断发言者的年龄和性别，标签格式为<>。"
  - "请对以下音频内容进行分析，并识别发言者的性别与年龄，标签格式为<>。"
  - "请从以下音频中提取讲话者的年龄和性别信息，标签格式为<>。"
  - "根据音频的分析，判断其中发言者的年龄和性别，标签格式为<>。"
  - "请确认以下音频中的发言者的性别与年龄，标签格式为<>。"
  - "请根据提供的音频文件，识别发言者的年龄和性别，标签格式为<>。"
  - "通过以下音频，分析发言者的年龄与性别，输出格式为<>标签。"
  - "请识别音频中的发言者年龄和性别，并输出标签格式为<>。"
  - "请根据以下音频确定发言者的性别与年龄，标签格式为<>。"
  - "请识别音频中的说话人年龄和性别，标签格式为<>。"
  - "请根据下列音频，判断发言者的年龄与性别，标签格式为<>。"
  - "通过音频分析发言者的性别和年龄，标签格式为<>。"
  - "请确认下列音频中的讲话者的性别和年龄，标签格式为<>。"
  - "通过分析音频内容，确定发言者的年龄和性别，标签格式为<>。"
  - "请根据下列音频内容分析发言者的性别和年龄，标签格式为<>。"
  - "根据以下音频，分析发言者的年龄与性别，并返回标签格式为<>。"
  - "请分析音频，给出发言者的性别和年龄，格式为<>标签。"
  - "请从以下音频中判断发言者的年龄与性别，标签格式为<>。"
  - "根据下列音频，分析并判断发言者的年龄和性别，标签格式为<>。"
  - "请识别音频中的讲话者的年龄与性别，标签格式为<>。"
  - "通过音频识别发言者的性别和年龄，标签格式为<>。"
  - "请通过对音频的分析，确定发言者的性别和年龄，标签格式为<>。"
  - "请对音频文件进行分析，并确定其中发言者的年龄和性别，标签格式为<>。"
  - "请判断以下音频中的发言者年龄和性别，标签格式为<>。"
  - "请确定音频中的说话者的年龄与性别，输出标签格式为<>。"
  - "通过分析音频，判断其中发言者的性别和年龄，标签格式为<>。"
  - "请识别以下音频中的说话人的性别与年龄，标签格式为<>。"
  - "请对下列音频进行年龄和性别的识别，标签格式为<>。"
  - "通过音频内容判断说话者的年龄与性别，标签格式为<>。"
  - "请分析以下音频，识别发言者的性别和年龄，标签格式为<>。"
  - "请判断音频发言者的年龄和性别，标签格式为<>。"
  - "请分析并识别音频中的说话者的性别和年龄，标签格式为<>。"
  - "根据音频内容识别发言者的性别和年龄，标签格式为<>。"
  - "请确认以下音频中说话者的性别与年龄，标签格式为<>。"
  - "通过音频文件分析，确定发言者的年龄与性别，标签格式为<>。"
  - "请从音频中判断发言者的年龄和性别，标签格式为<>。"
  - "根据音频分析判断讲话者的年龄和性别，标签格式为<>。"
  - "请确定音频发言者的年龄和性别，标签格式为<>。"
  - "通过对音频内容的分析，确定发言者的年龄和性别，标签格式为<>。"
  - "根据音频数据，判断发言者的年龄和性别，标签格式为<>。"
  - "请分析并识别音频中的说话者的性别和年龄，标签格式为<>。"
  - "请识别音频中的讲话者的性别和年龄，标签格式为<>。"
  - "通过音频文件识别说话者的性别和年龄，标签格式为<>。"
  - "请从以下音频内容中识别发言者的年龄与性别，标签格式为<>。"
  - "请识别以下音频中的发言者的性别和年龄，标签格式为<>。"
  - "请判断音频中的讲话者年龄和性别，标签格式为<>。"
  - "请确认以下音频中的发言者的性别和年龄，标签格式为<>。"
  - "通过对音频分析，识别其中发言者的性别和年龄，标签格式为<>。"
  - "请分析音频，确定其中发言者的年龄与性别，标签格式为<>。"
  - "请根据音频文件识别发言者的性别和年龄，标签格式为<>。"
  - "请分析以下音频内容，确定讲话者的性别与年龄，标签格式为<>。"
  - "请根据以下音频识别发言者的性别和年龄，标签格式为<>。"
  - "根据音频内容，判断发言者的年龄与性别，标签格式为<>。"
  - "请分析音频内容，判断发言者的性别与年龄，标签格式为<>。"
  - "请识别音频中的讲话者的年龄和性别，标签格式为<>。"
  - "请对音频内容分析，识别发言者的性别和年龄，标签格式为<>。"
  - "请根据音频内容，分析发言者的性别与年龄，标签格式为<>。"
  - "请分析以下音频，确定发言者的年龄和性别，标签格式为<>。"
  - "请根据音频分析发言者的年龄和性别，标签格式为<>。"
  - "通过对音频的分析，判断发言者的性别与年龄，标签格式为<>。"
  - "请通过音频分析，确定讲话者的性别与年龄，标签格式为<>。"
  - "请分析音频内容，判断发言者的年龄和性别，标签格式为<>。"
  - "请识别以下音频中的发言者的年龄与性别，标签格式为<>。"
  - "请根据音频判断讲话者的性别与年龄，标签格式为<>。"
  - "请分析音频内容，识别发言者的性别和年龄，标签格式为<>。"
  - "请确认下列音频中的发言者的年龄和性别，标签格式为<>。"
  - "通过分析音频内容，判断发言者的性别和年龄，标签格式为<>。"
  - "根据音频内容，识别发言者的性别和年龄，标签格式为<>。"
  - "请对以下音频进行分析，并识别发言者的年龄与性别，标签格式为<>。"
  - "通过音频分析发言者的年龄与性别，标签格式为<>。"
  - "请分析音频，判断说话者的年龄和性别，标签格式为<>。"
  - "请确定以下音频中发言者的年龄与性别，标签格式为<>。"
  - "根据音频内容，分析并判断发言者的年龄和性别，标签格式为<>。"
  - "请判断以下音频发言者的年龄和性别，标签格式为<>。"
  - "通过对音频的判断，确认发言者的年龄与性别，标签格式为<>。"
  - "根据音频判断发言者的年龄和性别，标签格式为<>。"
  - "请分析以下音频中的发言者的性别和年龄，标签格式为<>。"
  - "根据音频识别发言者的年龄与性别，标签格式为<>。"
  - "请判断音频中的发言者的性别与年龄，标签格式为<>。"
  - "根据音频内容，识别发言者的年龄和性别，标签格式为<>。"
  - "请分析并判断音频中的发言者的性别与年龄，标签格式为<>。"
  - "通过分析音频内容，识别讲话者的性别和年龄，标签格式为<>。"
  - "请确定音频中的讲话者的年龄与性别，标签格式为<>。"
  - "通过对音频进行分析，判断发言者的年龄与性别，标签格式为<>。"
  - "根据下列音频，识别发言者的年龄与性别，标签格式为<>。"
  - "请根据以下音频分析说话者的年龄与性别，标签格式为<>。"
  - "请分析音频，确定其中发言者的性别与年龄，标签格式为<>。"
  - "请对以下音频内容进行分析，识别发言者的年龄和性别，标签格式为<>。"
  - "根据音频的分析，判断发言者的性别与年龄，标签格式为<>。"
  - "请通过音频识别发言者的年龄和性别，标签格式为<>。"
  - "请根据音频分析判断说话者的年龄与性别，标签格式为<>。"
  - "请根据音频识别发言者的年龄与性别，标签格式为<>。"
  - "通过音频分析，确认说话者的性别与年龄，标签格式为<>。"
  - "根据音频内容，分析发言者的年龄和性别，标签格式为<>。"
  - "请分析音频内容，识别发言者的性别与年龄，标签格式为<>。"
  - "请确认音频中的讲话者的年龄与性别，标签格式为<>。"
  - "请通过分析音频，识别发言者的年龄和性别，标签格式为<>。"
  - "请分析音频并判断说话者的性别与年龄，标签格式为<>。"
  - "通过音频分析，识别发言者的性别与年龄，标签格式为<>。"
  - "请通过音频识别发言者的年龄与性别，标签格式为<>。"
  - "请从音频分析中识别发言者的年龄和性别，标签格式为<>。"
  - "请对音频中的发言者进行性别与年龄的分析，标签格式为<>。"
  - "请通过音频分析，确定说话者的年龄和性别，标签格式为<>。"
  - "请确认音频发言者的性别与年龄，标签格式为<>。"
  - "Please identify the age and gender of the speaker in the following audio, with tags in the format <>."
  - "Please recognize the age and gender of the speaker in the following audio, with tags in the format <>."
  - "Please determine the age and gender of the speaker in the following audio, with tags in the format <>."
  - "Please analyze the age and gender of the speaker in the following audio, with tags in the format <>."
  - "Please identify the speaker's age and gender in the following audio, with tags in the format <>."
  - "Based on the following audio, determine the speaker's age and gender, with tags in the format <>."
  - "Please analyze the following audio and provide the speaker's age and gender, in the format <>."
  - "Please identify the speaker's age and gender in this audio, outputting the tags in the format <>."
  - "Using the following audio, determine the speaker's age and gender, with tags in the format <>."
  - "Based on this audio, recognize the age and gender of the speaker, with tags in the format <>."
  - "Please recognize the age and gender of the speaker in the given audio, using tags in the format <>."
  - "Please listen to the following audio and identify the speaker's age and gender, in the format <>."
  - "Please analyze the following audio and identify the speaker's age and gender, with tags in the format <>."
  - "Based on the audio provided, determine the speaker's age and gender, with tags in the format <>."
  - "Please determine the speaker's age and gender in this audio, outputting the tags in the format <>."
  - "Please identify the speaker's age and gender in the following audio, with tags formatted as <>."
  - "Please listen to the following audio and identify the speaker's gender and age, using tags in the format <>."
  - "Based on the provided audio, please identify the speaker's age and gender with tags in the format <>."
  - "Please analyze the audio and determine the speaker's gender and age, with tags in the format <>."
  - "Using the following audio, analyze the speaker's age and gender, with tags in the format <>."





<TRANSCRIBE> <STYLE> <AGE> <GENDER>:
  - 请将下面的音频内容转录，并在转录后的文本结尾附上风格、年龄、性别的标签，标签格式为<>。
  - 请对下列音频内容进行转录，并在文本结尾添加风格、年龄和性别标签，标签格式为<>。
  - 请转录以下音频内容，并在转录文本的结尾处加上风格、年龄、性别标签，标签格式为<>。
  - 请将以下音频进行转录，并在转录完成后，标注风格、年龄、性别的标签，标签格式为<>。
  - 请对下列音频内容进行转录，并在结尾附加风格、年龄、性别的标签，标签格式为<>。
  - 请将下面的音频内容进行转录，转录完毕后在文本末尾分别加上风格、年龄、性别标签，标签格式为<>。
  - 请将以下音频转录并在结尾加上风格、年龄、性别的标签，标签格式为<>。
  - 请转录以下音频内容，并在文本结尾分别标注风格、年龄和性别，标签格式为<>。
  - 请转录下面的音频内容，并在文本结尾处附加风格、年龄、性别标签，标签格式为<>。
  - 请转录以下音频，完成后在文本结尾加上风格、年龄、性别三个标签，标签格式为<>。
  - 请将下面的音频转录，并在文本末尾标注风格、年龄、性别信息，标签格式为<>。
  - 请对以下音频进行转录，并在文本末尾附加风格、年龄、性别标签，标签格式为<>。
  - 请转录以下音频，并在文本结尾分别附加风格、年龄、性别的标签，标签格式为<>。
  - 请将以下音频转录，并在转录文本末尾附加风格、年龄、性别的标签，标签格式为<>。
  - 请转录这段音频，并在结尾加上风格、年龄和性别标签，标签格式为<>。
  - 请将以下音频进行转录，并在结尾标注风格、年龄、性别信息，标签格式为<>。
  - 请将下面的音频内容转录，并在文本末尾标注风格、年龄、性别标签，标签格式为<>。
  - 请将音频内容转录后，在文本结尾加上风格、年龄、性别标签，标签格式为<>。
  - 请转录下列音频，并在转录后的文本结尾标注风格、年龄、性别标签，标签格式为<>。
  - 请对以下音频进行转录，并在文本结尾加上风格、年龄、性别的标签，标签格式为<>。
  - 请将以下音频转录，并在文本末尾加上风格、年龄、性别标签，标签格式为<>。
  - 请转录这段音频内容，并在文本结尾加上风格、年龄、性别标签，标签格式为<>。
  - 请将音频转录并在文本末尾附加风格、年龄、性别标签，标签格式为<>。
  - 请对以下音频进行转录，并在文本结尾附加风格、年龄、性别标签，标签格式为<>。
  - 请转录音频并在文本末尾附加风格、年龄、性别的标签，标签格式为<>。
  - 请将下列音频内容转录，并在文本末尾标注风格、年龄和性别的标签，标签格式为<>。
  - 请对以下音频内容进行转录，并在结尾添加风格、年龄、性别的标签，标签格式为<>。
  - 请转录下列音频，并在转录后的文本结尾附加风格、年龄和性别标签，标签格式为<>。
  - 请转录以下音频，并在文本结尾附加风格、年龄、性别标签，标签格式为<>。
  - 请将下列音频转录，并在文本末尾标注风格、年龄、性别标签，标签格式为<>。
  - 请将下面的音频转录，并在转录文本结尾附加风格、年龄和性别的标签，标签格式为<>。
  - 请将以下音频内容转录，并在结尾加上风格、年龄、性别标签，标签格式为<>。
  - 请将这段音频转录，并在结尾附加风格、年龄、性别标签，标签格式为<>。
  - 请转录音频，并在文本结尾标注风格、年龄和性别标签，标签格式为<>。
  - 请转录以下音频，结尾加上风格、年龄、性别的标签，标签格式为<>。
  - 请将这段音频转录，完成后在文本结尾附加风格、年龄、性别的标签，标签格式为<>。
  - 请转录音频内容，并在文本结尾附加风格、年龄、性别标签，标签格式为<>。
  - 请将这段音频转录并在结尾加上风格、年龄、性别标签，标签格式为<>。
  - 请转录以下音频，完成后在文本结尾标注风格、年龄、性别标签，标签格式为<>。
  - 请将音频转录后，在结尾加上风格、年龄、性别标签，标签格式为<>。
  - 请转录音频，并在转录后的文本结尾附加风格、年龄、性别标签，标签格式为<>。
  - 请对以下音频进行转录，并在转录完毕后标注风格、年龄、性别标签，标签格式为<>。
  - 请转录下列音频并在文本结尾标注风格、年龄、性别标签，标签格式为<>。
  - 请转录音频并在结尾加上风格、年龄、性别标签，标签格式为<>。
  - 请将以下音频转录，并在结尾标注风格、年龄、性别的标签，标签格式为<>。
  - 请将下面的音频转录，并在文本结尾加上风格、年龄、性别标签，标签格式为<>。
  - Please transcribe the following audio content and append tags for style, age, and gender at the end of the transcription, in the format <>.
  - Please transcribe the audio and add tags for style, age, and gender at the end, in the format <>.
  - Please transcribe the audio content and append tags for style, age, and gender at the end of the text, in the format <>.
  - Please transcribe the following audio and add the tags for style, age, and gender at the end, in the format <>.
  - Please transcribe the audio and include tags for style, age, and gender at the end of the transcription, in the format <>.
  - Please transcribe the audio content and append the style, age, and gender tags at the end, in the format <>.
  - Please transcribe the following audio content and include style, age, and gender tags at the end, in the format <>.
  - Please transcribe the following audio and append tags for style, age, and gender at the end of the text, in the format <>.
  - Please transcribe this audio content and append tags for style, age, and gender at the end, in the format <>.
  - Please transcribe the following audio and at the end of the transcription, append tags for style, age, and gender, in the format <>.
  - Please transcribe this audio and add style, age, and gender tags at the end, in the format <>.
  - Please transcribe the audio and include style, age, and gender tags at the end of the transcription, in the format <>.
  - Please transcribe the audio content and append style, age, and gender tags at the end of the transcription, in the format <>.
  - Please transcribe the following audio content and add style, age, and gender tags at the end, in the format <>.
  - Please transcribe the audio and add style, age, and gender tags at the end of the text, in the format <>.
  - Please transcribe the audio and append style, age, and gender tags at the end, in the format <>.
  - Please transcribe this audio and append tags for style, age, and gender at the end, in the format <>.
  - Please transcribe the following audio and at the end of the transcription, append the style, age, and gender tags, in the format <>.
  - Please transcribe the audio content and add the tags for style, age, and gender at the end, in the format <>.
  - Please transcribe this audio and append tags for style, age, and gender at the end of the text, in the format <>.
<STYLE> <AGE> <GENDER>:
  - "请分析以下音频的说话风格、说话者的年龄和性别，标签格式为<>。"
  - "请识别下面音频的说话风格、说话者的年龄和性别，标签格式为<>。"
  - "请对以下音频进行分析，识别说话风格、说话者年龄和性别，标签格式为<>。"
  - "请识别如下音频的说话风格、说话者的年龄和性别，标签格式为<>。"
  - "请对下面音频内容识别说话风格、说话者的年龄和性别，标签格式为<>。"
  - "请分析这个音频，识别说话风格、说话者的年龄和性别，标签格式为<>。"
  - "请识别下列音频中的说话风格、说话者的年龄和性别，标签格式为<>。"
  - "请对这段音频内容分析，识别说话风格、说话者的年龄和性别，标签格式为<>。"
  - "请分析以下这段音频，识别说话者的风格、年龄和性别，标签格式为<>。"
  - "请识别这段音频中的说话风格、年龄和性别，标签格式为<>。"
  - "请对以下音频进行分析，标出说话风格、年龄和性别，标签格式为<>。"
  - "请对下面的音频内容识别说话者的风格、年龄与性别，标签格式为<>。"
  - "请识别音频中的说话风格、说话者的年龄和性别，并以<>格式标出。"
  - "请分析以下音频并识别说话者的年龄、性别和说话风格，标签格式为<>。"
  - "请对下面的音频进行风格、年龄和性别识别，标签格式为<>。"
  - "请分析这段音频的说话风格，年龄和性别，标签格式为<>。"
  - "请识别这段音频中说话者的风格、年龄和性别，标签格式为<>。"
  - "请对这段音频内容分析，识别说话风格、年龄和性别，标签格式为<>。"
  - "请分析下列音频的说话风格、年龄和性别，标签格式为<>。"
  - "请对这段音频的说话风格、年龄和性别进行识别，标签格式为<>。"
  - "请识别下面音频中的说话风格和说话者的年龄性别，标签格式为<>。"
  - "请分析这段音频的说话风格、年龄和性别，标签格式为<>。"
  - "请对这段音频进行风格、年龄和性别的分析，标签格式为<>。"
  - "请识别以下音频中的说话风格，标出说话者的年龄与性别，标签格式为<>。"
  - "请分析下面音频，识别其中的说话风格、年龄和性别，标签格式为<>。"
  - "请对这段音频分析说话者的风格、年龄与性别，标签格式为<>。"
  - "请识别以下音频说话风格、说话者年龄和性别，标签格式为<>。"
  - "请对下面的音频内容进行分析，标出说话风格、年龄与性别，标签格式为<>。"
  - "请分析此音频并识别说话风格、年龄和性别，标签格式为<>。"
  - "请对下面这段音频进行分析，识别说话风格、年龄与性别，标签格式为<>。"
  - "请识别这段音频的说话风格、年龄和性别，并标记为<>格式。"
  - "请对音频内容分析，说话风格、年龄和性别识别，标签格式为<>。"
  - "请对这段音频进行分析，识别说话风格、说话者的年龄与性别，标签格式为<>。"
  - "请分析这段音频的说话风格，标出说话者的年龄与性别，标签格式为<>。"
  - "请识别下列音频中的说话风格、年龄和性别，标签格式为<>。"
  - "请对这段音频内容进行风格、年龄和性别的分析，标签格式为<>。"
  - "请分析这个音频并识别说话者的风格、年龄和性别，标签格式为<>。"
  - "请对这段音频进行风格、年龄、性别识别，标签格式为<>。"
  - "请分析这个音频内容，识别说话风格、年龄与性别，标签格式为<>。"
  - "请识别这段音频中的说话风格、年龄、性别，标签格式为<>。"
  - "请对以下音频的说话风格、年龄和性别进行分析，标签格式为<>。"
  - "请识别这段音频的说话风格与说话者的年龄、性别，标签格式为<>。"
  - "请对这段音频进行风格、年龄、性别的识别分析，标签格式为<>。"
  - "请分析下列音频的说话风格、年龄与性别，标签格式为<>。"
  - "请识别这段音频的说话风格、年龄与性别，标签格式为<>。"
  - "请对下面音频进行说话风格、年龄与性别分析，标签格式为<>。"
  - "请分析此音频内容，识别说话者的风格、年龄和性别，标签格式为<>。"
  - "请识别以下音频的说话风格、年龄与性别，标签格式为<>。"
  - "请分析这段音频并识别说话者的风格、年龄和性别，标签格式为<>。"
  - "请识别下面这段音频中的说话风格、年龄与性别，标签格式为<>。"
  - "请对这段音频进行风格、年龄、性别分析，标签格式为<>。"
  - "请对音频内容进行说话风格、年龄与性别识别，标签格式为<>。"
  - "请分析此段音频的说话风格、年龄和性别，标签格式为<>。"
  - "请对以下音频进行风格、年龄与性别的分析，标签格式为<>。"
  - "请识别音频中的说话风格、年龄与性别，标签格式为<>。"
  - "请分析这段音频，识别说话者的风格、年龄和性别，标签格式为<>。"
  - "请识别下列音频的说话风格、说话者的年龄和性别，标签格式为<>。"
  - "请对音频内容进行说话风格、年龄、性别的识别，标签格式为<>。"
  - "请分析音频，识别说话风格、年龄和性别，标签格式为<>。"
  - "Please analyze the audio and identify the speaking style, age, and gender of the speaker, format the tags as <>."
  - "Please identify the speaking style, age, and gender of the speaker in the following audio, format the tags as <>."
  - "Analyze this audio and identify the speaking style, age, and gender of the speaker, format the tags as <>."
  - "Please analyze the audio content and identify the speaking style, age, and gender of the speaker, tags in <> format."
  - "Identify the speaking style, age, and gender of the speaker in the following audio, use <> for tagging."
  - "Analyze the following audio to identify the speaking style, age, and gender of the speaker, format the tags as <>."
  - "Please identify the speaking style, age, and gender of the speaker in this audio, tags should be formatted as <>."
  - "Analyze the audio and identify the speaker's style, age, and gender, format the result as <>."
  - "Please identify the speaking style, age, and gender of the speaker in the given audio, tags format as <>."
  - "Please analyze this audio, identify the speaking style, age, and gender of the speaker, format the tags as <>."
  - "Analyze the audio and identify the speaker's style, age, and gender, use <> for tagging."
  - "Please analyze the given audio and identify the speaking style, age, and gender, tags in <> format."
  - "Identify the speaker's speaking style, age, and gender in the audio, tag format should be <>."
  - "Analyze the given audio, and identify the speaker's style, age, and gender in <> format."
  - "Please identify the speaking style, age, and gender of the speaker in this audio, using the <> tag format."
  - "Analyze the following audio and identify the speaker's speaking style, age, and gender, format the tags as <>."
  - "Identify the speaking style, age, and gender in this audio, and tag it in the format <>."
  - "Please analyze this audio and provide the speaker's speaking style, age, and gender, tags formatted as <>."


<TRANSCRIBE> <PUNCTUATION>:
  - 请对下面的语音文件进行转录，并在文本中适当添加标点。
  - 请将以下音频内容转写为文字，并且添加标点符号。
  - 将提供的语音文件转换为文字，并补充必要的标点。
  - 请将下方的音频转录成文本，并加上正确的标点。
  - 需要将以下录音转化成文字，并插入标点符号。
  - 对所给语音进行转写，并在转写内容中补充标点符号。
  - 请对音频文件进行文本转录，且自动加上标点。
  - 将下列录音内容转成文字，并且为文字加入标点。
  - 对下方音频进行转录并加标点，确保准确。
  - 请将以下音频内容转写为文字，并插入标点符号。
  - 对给定音频进行转录，并在文字中补充适当的标点。
  - 需要将提供的语音文件转化为文本，且自动标注标点。
  - 请转录音频内容为文字，并在转换文本时加入标点符号。
  - 对录音进行转写，并为转写后的文本添加标点符号。
  - 请对音频文件进行转录，自动为文本添加标点符号。
  - 请将提供的录音内容转化为文字，并插入标点符号。
  - 将语音转化为文字，确保文本中有合适的标点。
  - 对音频进行转录并自动插入标点符号。
  - 请将音频转化为文字，并自动插入标点符号。
  - 需将下方的语音内容转为文字，并加上适当的标点符号。
  - 将录音转写为文字，自动为文字内容加上标点。
  - 请将音频文件转写为文本，并在文本中加上标点符号。
  - 需要对录音进行文字转写，并补充适当的标点符号。
  - 对音频进行转写并插入合适的标点符号。
  - 请对下方音频进行转录并加上标点符号。
  - 将录音转写为文字，并自动为文字加上标点符号。
  - 对给定的录音进行转录，自动添加标点符号。
  - 请将语音文件转换为文字，并加入正确的标点符号。
  - 请将提供的语音转为文字并添加标点符号。
  - 请转写下方音频内容并在文字中自动插入标点符号。
  - 需要将下列音频内容转录为文字，并加上标点。
  - 请对给定录音进行转写并为文字加上标点。
  - 请将语音内容转换为文字并加入适当的标点符号。
  - 请将录音转写成文字，并补充标点符号。
  - 需要将给定的音频转为文字并为文字加上标点符号。
  - 请将音频转录成文字并为其添加标点。
  - 请将以下音频转换为文字，并在文本中加上标点符号。
  - 对下列语音进行转写，并在转写结果中添加标点符号。
  - 将音频内容转化为文字，并为文字自动插入标点。
  - 请将音频转写为文字，并为文字插入适当的标点。
  - 请将录音转写为文字，并插入句读符号。
  - 请将提供的音频内容转录成文字并加入标点。
  - 请将音频转写为文字并加上标点。
  - 需要将录音转为文字，并插入标点符号。
  - 请对音频进行转写，并在文字中补充标点符号。
  - 请将以下音频转写为文字，并加上标点符号。
  - 请将给定录音转写为文本，并插入标点符号。
  - 请将音频内容转写为文字，并加上适当的标点符号。
  - 请将下方的音频转化为文字并为其添加标点。
  - 请将录音内容转化为文字，并在文字中加上标点符号。
  - 需要将下方语音内容转写为文字，并加上标点符号。
  - 将音频转换为文字并自动为其添加标点符号。
  - 将提供的语音内容转为文字，并补充适当的标点。
  - 请将音频文件转写成文字并插入标点符号。
  - 需要将给定的录音转写为文字，并自动加上标点符号。
  - 请将语音文件转写成文字，并为文本插入标点符号。
  - 请将下方音频转为文字并补充适当的标点符号。
  - 请将音频文件转写为文字并为其自动加上标点。
  - 对以下音频进行转录并为其加入标点符号。
  - 请将给定的音频转录为文字并插入标点符号。
  - 请对提供的音频文件进行转写并插入适当的标点。
  - 请将以下语音转为文字，并为其添加标点符号。
  - 请将音频转化为文字，并加上句读符号。
  - 请将给定的语音文件转化为文字并插入标点符号。
  - 请转写给定音频文件并插入合适的标点符号。
  - 请将提供的录音转化为文字并自动补充标点符号。
  - 请将下方录音内容转为文字并自动插入标点符号。
  - 请将音频转为文字并自动为其添加标点符号。
  - 请将语音文件转写为文字，并为其添加标点符号。
  - 请转写下列音频并为其加上标点符号。
  - 请将下方录音转写为文字并自动加入标点符号。
  - 请将录音转写为文字并为文本插入标点符号。
  - 请将录音内容转为文字并自动添加标点符号。
  - 请对音频文件进行转录并为文字添加标点符号。
  - 请将给定音频转写成文字，并自动插入标点符号。
  - Please transcribe the following audio and insert punctuation into the text.
  - Convert the provided audio into text and add appropriate punctuation.
  - Transcribe the given recording and ensure punctuation is added.
  - Please transcribe the following audio and add punctuation marks in the transcription.
  - Convert the given speech to text and automatically insert punctuation.
  - Please transcribe the audio file and include punctuation in the transcription.
  - Please transcribe the provided audio and add punctuation where necessary.
  - Convert the following audio into text and add punctuation symbols.
  - Transcribe the given audio content into text and insert punctuation.
  - Please transcribe the audio and ensure the transcription includes punctuation.
  - Please convert the audio content to text and insert the necessary punctuation.
  - Transcribe the provided audio and add the correct punctuation marks.
  - Please transcribe the given audio file and add punctuation marks in the text.
  - Convert the audio to text and insert punctuation as needed.
  - Please transcribe the provided speech and add punctuation in the transcription.
  - Please convert the audio into text and insert punctuation marks.
  - Please transcribe the audio content and add punctuation where needed.
  - Please transcribe the audio file into text and insert punctuation.
  - Convert the given recording into text and ensure it has proper punctuation.
  - Please transcribe the provided audio and add punctuation symbols accordingly.


<TRANSCRIBE> <CAPTION> <AGE> <GENDER>:
  - "请将以下音频内容进行转录，并在转录完成的文本末尾分别附加音频事件、年龄、性别标签,标签格式为<>。"
  - "请将下列音频内容进行转录，并在转录文本的末尾分别添加音频事件、年龄、性别标签,标签格式为<>。"
  - "请转录以下音频内容，并在转录后的文本末尾标注音频事件、年龄、性别标签,标签格式为<>。"
  - "请将以下音频内容转录，并在转录完成后，附加音频事件、年龄、性别三个标签,标签格式为<>。"
  - "请对以下音频内容进行转录，并在文本结尾分别添加音频事件、年龄、性别标签,标签格式为<>。"
  - "请将接下来的音频内容转录，并在转录结果的末尾附加音频事件、年龄、性别标签，格式为<>。"
  - "请对下列音频内容进行转录，并在结尾添加音频事件、年龄、性别标签，格式要求为<>。"
  - "将以下音频转录为文本，并在末尾标注音频事件、年龄、性别标签，格式为<>。"
  - "请将以下音频进行转录，并在结果的末尾附上音频事件、年龄、性别标签，标签格式应为<>。"
  - "请将音频内容转录为文本，并在文本的最后标注音频事件、年龄、性别标签，格式为<>。"
  - "请根据以下音频内容进行转录，并在文本末尾添加音频事件、年龄、性别标签，格式使用<>。"
  - "请对接下来的音频进行转录，并在文本结尾加入音频事件、年龄、性别标签，格式为<>。"
  - "将以下音频转录，并在结果结尾附加音频事件、年龄、性别标签，标签格式为<>。"
  - "请将下列音频内容进行文字转录，并在文本结尾添加音频事件、年龄、性别标签，标签形式为<>。"
  - "请根据音频内容进行转录，并在转录文本末尾加入音频事件、年龄、性别标签，标签格式为<>。"
  - "请将音频内容进行文字转录，并在文本末尾标明音频事件、年龄、性别标签，标签格式为<>。"
  - "请转录下面的音频，并在转录后的文本末尾附上音频事件、年龄、性别标签，格式为<>。"
  - "请对下列音频内容进行转录，且在转录完成后，在文本的末尾加入音频事件、年龄、性别标签，标签格式为<>。"
  - "请转录音频并在文本结尾附加音频事件、年龄、性别标签，格式为<>。"
  - "请对音频进行转录，并在完成后的文本中加入音频事件、年龄、性别标签，格式为<>。"
  - "请将接下来的音频内容转录，并在文本末尾附加音频事件、年龄、性别标签，标签格式为<>。"
  - "请将音频内容进行文字转录，并在文本末尾添加音频事件、年龄、性别标签，格式为<>。"
  - "请将以下音频内容转录，并在转录完成后，附加音频事件、年龄、性别标签，标签格式为<>。"
  - "请将下列音频内容转录，并在文本结尾加入音频事件、年龄、性别标签，格式为<>。"
  - "请转录以下音频内容，并在文本末尾标注音频事件、年龄、性别标签，标签格式为<>。"
  - "请对下面的音频内容进行转录，并在转录完成后附加音频事件、年龄、性别标签，标签格式为<>。"
  - "请转录音频内容，并在文本末尾标明音频事件、年龄、性别标签，标签格式为<>。"
  - "请将以下音频转录，并在文本结尾附加音频事件、年龄、性别标签，格式为<>。"
  - "请将接下来的音频内容转录，并在转录后的文本中附加音频事件、年龄、性别标签，格式为<>。"
  - "请对下列音频内容进行转录，并在转录后的文本末尾标注音频事件、年龄、性别标签，标签格式为<>。"
  - "请将下列音频转录为文本，并在转录文本的末尾添加音频事件、年龄、性别标签，标签格式为<>。"
  - "请根据下列音频内容进行转录，并在文本结尾添加音频事件、年龄、性别标签，标签格式为<>。"
  - "请对以下音频进行转录，并在转录后的文本末尾加入音频事件、年龄、性别标签，标签格式为<>。"
  - "请将音频内容进行转录，并在文本末尾添加音频事件、年龄、性别标签，格式为<>。"
  - "请将音频转录为文本，并在末尾附上音频事件、年龄、性别标签，标签格式为<>。"
  - "请对下列音频进行转录，并在转录后文本的末尾标注音频事件、年龄、性别标签，格式为<>。"
  - "请将音频内容转录并在转录完成的文本末尾附上音频事件、年龄、性别标签，标签格式为<>。"
  - "请将以下音频转录，并在结尾标注音频事件、年龄、性别标签，标签格式为<>。"
  - "请对音频内容进行转录，并在转录文本末尾附上音频事件、年龄、性别标签，标签格式为<>。"
  - "请将下列音频内容进行文字转录，并在转录文本的末尾加上音频事件、年龄、性别标签，标签格式为<>。"
  - "请转录以下音频，并在文本结尾添加音频事件、年龄、性别标签，格式为<>。"
  - "请对接下来的音频进行转录，并在转录后的文本末尾附加音频事件、年龄、性别标签，格式为<>。"
  - "请将音频内容转录为文本，并在文本末尾附加音频事件、年龄、性别标签，标签格式为<>。"
  - "请对下列音频进行转录，并在转录后的文本末尾加上音频事件、年龄、性别标签，格式为<>。"
  - "请将接下来的音频内容转录，并在转录文本的结尾标注音频事件、年龄、性别标签，格式为<>。"
  - "请将以下音频内容转录，并在文本结尾附上音频事件、年龄、性别标签，格式为<>。"
  - "请将下列音频转录并在文本末尾加入音频事件、年龄、性别标签，标签格式为<>。"
  - "请将音频内容转录并在末尾添加音频事件、年龄、性别标签，标签格式为<>。"
  - "请对下列音频进行转录，并在文本结尾附加音频事件、年龄、性别标签，标签格式为<>。"
  - "请对音频内容进行转录，并在文本末尾标注音频事件、年龄、性别标签，标签格式为<>。"
  - "请转录下列音频内容，并在文本结尾附加音频事件、年龄、性别标签，标签格式为<>。"
  - "请将下列音频转录并在转录文本末尾附加音频事件、年龄、性别标签，标签格式为<>。"
  - "请转录下列音频内容，并在转录结果的末尾标注音频事件、年龄、性别标签，标签格式为<>。"
  - "请根据音频内容转录，并在文本末尾附加音频事件、年龄、性别标签，标签格式为<>。"
  - "请将音频内容转录为文本，并在末尾标注音频事件、年龄、性别标签，标签格式为<>。"
  - "请对以下音频内容进行转录，并在转录后的文本末尾标注音频事件、年龄、性别标签，格式为<>。"
  - "Please transcribe the following audio content and add labels for the audio event, age, and gender at the end of the transcription, in the format <>."
  - "Transcribe the audio content below and append labels for audio event, age, and gender at the end of the transcription, in the format <>."
  - "Please transcribe the following audio and add audio event, age, and gender labels at the end of the transcript, formatted as <>."
  - "Transcribe the audio content provided and append audio event, age, and gender tags at the end of the transcript in the format <>."
  - "Please transcribe the given audio and include the audio event, age, and gender labels at the end of the transcription, formatted as <>."
  - "Transcribe the following audio and add labels for audio event, age, and gender at the end of the transcription in the format <>."
  - "Please transcribe this audio content and append audio event, age, and gender labels at the end, in the format <>."
  - "Transcribe the following audio and attach the audio event, age, and gender labels at the end in the format <>."
  - "Please transcribe the provided audio and include the audio event, age, and gender labels at the end of the transcription, formatted as <>."
  - "Transcribe the audio content below and add audio event, age, and gender labels at the end of the text, formatted as <>."
  - "Please transcribe the audio and append the audio event, age, and gender labels at the end of the transcription, in the format <>."
  - "Transcribe the following audio content and attach audio event, age, and gender labels at the end of the transcription, in the format <>."
  - "Please transcribe the audio and add labels for audio event, age, and gender at the end of the transcription, formatted as <>."
  - "Please transcribe the following audio and add audio event, age, and gender labels at the end, formatted as <>."
  - "Transcribe this audio and include the audio event, age, and gender labels at the end, in the format <>."
  - "Transcribe the audio below and add labels for audio event, age, and gender at the end of the text, in the format <>."
  - "Please transcribe the following audio and include labels for the audio event, age, and gender at the end of the transcription in the format <>."
  - "Transcribe the following audio and include audio event, age, and gender labels at the end, in the format <>."
  - "Transcribe the following audio content and attach audio event, age, and gender labels at the end of the transcription, formatted as <>."
  - "Please transcribe the audio and attach the audio event, age, and gender labels at the end of the text in the format <>."
  - "Transcribe the following audio and append the labels for audio event, age, and gender at the end of the transcription, in the format <>."


<TRANSCRIBE> <EMOTION> <AGE> <GENDER>:
  - 请将以下音频内容进行转录，并在转录完成的文本末尾分别附加情感、年龄、性别标签,标签格式为<>。
  - 请将下列音频内容进行转录，并在转录文本的末尾分别添加情感、年龄、性别标签,标签格式为<>。
  - 请转录以下音频内容，并在转录后的文本末尾标注情感、年龄、性别标签,标签格式为<>。
  - 请将以下音频内容转录，并在转录完成后，附加情感、年龄、性别三个标签,标签格式为<>。
  - 请对以下音频内容进行转录，并在文本结尾分别添加情感、年龄、性别标签,标签格式为<>。
  - 请将音频转录成文本并在末尾附加情感、年龄、性别的标签，格式为<>。
  - 请转录这段音频内容，并在完成后给文本添加情感、年龄、性别标签，标签格式为<>。
  - 请转写以下音频，并在结尾加上情感、年龄和性别标签，格式为<>。
  - 请处理以下音频，转录文本后，附加情感、年龄、性别的标签，格式为<>。
  - 请将音频文件内容转写成文本，并附加情感、年龄、性别标签，格式为<>。
  - 请将该音频转录并在文本后加上情感、年龄、性别三个标签，格式为<>。
  - 请对以下音频文件进行转录，并在转录后的文本结尾添加情感、年龄、性别标签，标签格式为<>。
  - 请将音频内容转写并在转录后的文本末尾附上情感、年龄、性别标签，格式为<>。
  - 请转录下面的音频，并在转录文本的末尾标注情感、年龄、性别标签，标签格式为<>。
  - 请将音频转录成文字，并在文本后添加情感、年龄、性别标签，标签格式为<>。
  - 请对以下音频内容进行转录，并在文本末尾标注情感、年龄、性别标签，格式为<>。
  - 请将音频文件转录成文本，并在末尾附上情感、年龄、性别的标签，格式为<>。
  - 请转写以下音频内容，并在文本末尾添加情感、年龄和性别标签，格式为<>。
  - 请将音频文件内容转录并在结尾附加情感、年龄、性别标签，格式为<>。
  - 请将以下音频转录为文本，并在结尾加上情感、年龄、性别标签，格式为<>。
  - 请转录音频并在转录后的文本后附加情感、年龄、性别标签，标签格式为<>。
  - 请将音频内容转写成文本，并附加情感、年龄和性别标签，格式为<>。
  - 请转录下面的音频，并在文本末尾标注情感、年龄、性别的标签，格式为<>。
  - 请将音频内容转录成文本，并在末尾加上情感、年龄、性别的标签，格式为<>。
  - 请转写此音频，并在文本结尾附上情感、年龄、性别标签，格式为<>。
  - 请将以下音频内容转录，并在文本末尾添加情感、年龄、性别的标签，格式为<>。
  - 请转录该音频，并在文本结尾附上情感、年龄、性别标签，格式为<>。
  - 请将音频转写成文字，并在结尾附加情感、年龄、性别标签，格式为<>。
  - 请转录下面的音频，并在转录文本后附加情感、年龄、性别标签，格式为<>。
  - 请将该音频文件转写并附加情感、年龄、性别标签，标签格式为<>。
  - 请将此音频内容转录并在文本末尾标注情感、年龄、性别标签，格式为<>。
  - 请处理此音频并转录，附加情感、年龄、性别标签，标签格式为<>。
  - 请将以下音频内容转录，并在文本末尾标注情感、年龄、性别标签，标签格式为<>。
  - 请转录此段音频，并在文本结尾添加情感、年龄、性别标签，格式为<>。
  - 请将音频转写并在文本末尾附上情感、年龄、性别标签，格式为<>。
  - 请将音频内容转写成文本，并在末尾添加情感、年龄、性别标签，格式为<>。
  - 请将该音频转录并在文本末尾附加情感、年龄、性别标签，格式为<>。
  - 请转录音频并在转录后的文本末尾附上情感、年龄、性别标签，格式为<>。
  - 请将音频转写并在文本后附加情感、年龄、性别标签，格式为<>。
  - 请对该音频进行转录，并在文本末尾标注情感、年龄、性别标签，格式为<>。
  - 请处理该音频并将其转录，附加情感、年龄、性别标签，格式为<>。
  - 请将音频转录成文字，并在文本后加上情感、年龄、性别标签，格式为<>。
  - 请对这段音频内容进行转录，并在转录后的文本末尾添加情感、年龄、性别标签，格式为<>。
  - 请转录这段音频，并在转录文本末尾添加情感、年龄、性别标签，格式为<>。
  - 请将该音频内容转录成文本，并在文本结尾标注情感、年龄、性别标签，标签格式为<>。
  - 请转写此音频并在末尾附加情感、年龄、性别标签，格式为<>。
  - 请将这段音频转录，并在文本末尾附加情感、年龄、性别标签，格式为<>。
  - 请对这段音频进行转录，并在文本后附加情感、年龄、性别标签，格式为<>。
  - 请将音频转录并在末尾标注情感、年龄、性别标签，标签格式为<>。
  - 请转录该音频内容，并在文本末尾附加情感、年龄、性别标签，格式为<>。
  - 请将音频文件转写为文字，并在结尾附加情感、年龄、性别标签，标签格式为<>。
  - 请将音频转写为文字，并附加情感、年龄、性别标签，格式为<>。
  - 请将这段音频转录，并在文本末尾标注情感、年龄、性别标签，格式为<>。
  - 请转录该音频内容，并在文本结尾附加情感、年龄、性别标签，标签格式为<>。
  - 请将音频转录成文本，并在末尾加上情感、年龄、性别标签，格式为<>。
  - 请转录该音频并在末尾附加情感、年龄、性别标签，格式为<>。
  - 请将音频转写为文本，并在文本末尾加上情感、年龄、性别标签，格式为<>。
  - 请将音频内容转写并附加情感、年龄、性别标签，格式为<>。
  - 请转录该音频并在文本后附加情感、年龄、性别标签，格式为<>。
  - 请将该音频转录成文本，并在文本末尾标注情感、年龄、性别标签，格式为<>。
  - 请将音频文件转录并在文本末尾添加情感、年龄、性别标签，格式为<>。
  - 请将以下音频内容转录，并在文本末尾加上情感、年龄、性别标签，标签格式为<>。
  - 请对这段音频进行转录，并在结尾标注情感、年龄、性别标签，标签格式为<>。
  - 请将该音频转录并标注情感、年龄、性别标签，格式为<>。
  - Please transcribe the following audio and add the emotion, age, and gender tags at the end, in the format <>.
  - Please transcribe the audio and include emotion, age, and gender labels at the end, formatted as <>.
  - Please transcribe the following audio content, and at the end of the text, add emotion, age, and gender labels in the format <>.
  - Transcribe the audio below and add emotion, age, and gender tags at the end, formatted as <>.
  - Please transcribe this audio content and append emotion, age, and gender tags at the end, in the format <>.
  - Transcribe the audio and append emotion, age, and gender labels at the end, using the format <>.
  - Please transcribe the audio and attach emotion, age, and gender tags at the end in the format <>.
  - Please convert this audio to text and include emotion, age, and gender labels at the end, formatted as <>.
  - Please transcribe this audio and include emotion, age, and gender tags at the end, formatted as <>.
  - Please transcribe the following audio, and at the end of the transcript, add emotion, age, and gender tags in the format <>.
  - Transcribe this audio and at the end, add emotion, age, and gender labels in the format <>.
  - Please transcribe the audio and add the tags for emotion, age, and gender at the end, in the format <>.
  - Please transcribe the following audio and append the emotion, age, and gender tags at the end, in the format <>.
  - Please convert the following audio to text and append emotion, age, and gender tags at the end, using the format <>.
  - Please transcribe the audio and at the end, add emotion, age, and gender labels in the format <>.
  - Transcribe the audio and at the end of the text, add emotion, age, and gender tags, in the format <>.
  - Please transcribe the audio and include emotion, age, and gender labels at the end, in the format <>.
  - Please transcribe this audio and add emotion, age, and gender labels at the end, formatted as <>.
  - Please transcribe the following audio and add the emotion, age, and gender tags at the end, formatted as <>.

<TRANSCRIBE> <AGE> <GENDER> <STYLE> <EMOTION>:
  - 请将以下音频内容进行转录，并在转录完成的文本末尾分别附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将下列音频内容进行转录，并在转录文本的末尾分别添加年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录以下音频内容，并在转录后的文本末尾标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请将以下音频内容转录，并在转录完成后，附加年龄、性别、风格、情感四个标签，标签格式为<>。
  - 请对以下音频内容进行转录，并在文本结尾分别添加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频内容转录，并在文本末尾添加年龄、性别、风格、情感标签，标签格式为<>。
  - 请对音频进行转录，并在转录后添加年龄、性别、风格、情感四个标签，标签格式为<>。
  - 请将音频内容转录，并在文本末尾标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频中的内容转录出来，并在最后附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将下列音频内容转录，并在最后标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请对音频进行转录，并在结尾添加年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录这段音频，并在转录结果的末尾加上年龄、性别、风格、情感标签，标签格式为<>。
  - 请将以下语音内容转写，并附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频内容转写并标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请把音频内容转录，并在文本后标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录下列音频，并标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请对以下音频内容进行文字转录，并在转录的末尾标明年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录此音频，并在文本的最后添加年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录以下语音，并在文本结尾附上年龄、性别、风格、情感标签，标签格式为<>。
  - 请将这段音频转录出来，并在末尾添加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将此音频转录，并在末尾加上年龄、性别、风格、情感标签，标签格式为<>。
  - 请对这段音频进行转录，并在转录后附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将以下音频进行转录，并在文本末尾标记年龄、性别、风格、情感标签，标签格式为<>。
  - 请对音频内容进行转录，并在文本末尾标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请将这段音频转录，并在文本的末尾附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录此段音频并附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请对以下音频进行转录，标注年龄、性别、风格、情感四个标签，标签格式为<>。
  - 请将音频转录出来，并在转录后的文本末尾附上年龄、性别、风格、情感标签，标签格式为<>。
  - 请将下列音频转录，并在最后标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录音频并在最后加上年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频内容转写，并在文本结尾附上年龄、性别、风格、情感标签，标签格式为<>。
  - 请将此音频内容转录，并在结尾附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将下列音频转录，并在最后标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请对以下音频内容进行转录，并在最后添加年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录音频并在转录文本末尾添加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频内容转录出来，并在转录后的文本末尾附上年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录音频，并在文本结尾标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频中的内容转录，并在文本的结尾附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录以下音频，并在文本末尾标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请对这段音频进行转录，并在转录后的文本末尾加上年龄、性别、风格、情感标签，标签格式为<>。
  - 请将这段音频内容转写，并在转录后的文本末尾附上年龄、性别、风格、情感标签，标签格式为<>。
  - 请将以下音频转录，并在结尾附上年龄、性别、风格、情感标签，标签格式为<>。
  - 请对这段音频进行转录，并在文本末尾标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请将此音频转录，并在文本结尾附上年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录此音频并附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请对以下音频进行转录并标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请将下列音频内容转录，并在结尾附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录音频并标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频内容转录，并在文本末尾附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将这段音频内容转写并在结尾添加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将这段音频转录，并在文本的末尾附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频转录并标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请将以下音频转录，并在文本结尾标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请转录这段音频并在结尾加上年龄、性别、风格、情感标签，标签格式为<>。
  - 请对下列音频进行转录，并在文本结尾附上年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频内容转写，并在文本末尾标注年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频转录，并在最后附加年龄、性别、风格、情感标签，标签格式为<>。
  - 请将音频转录出来，并在转录文本的末尾附加年龄、性别、风格、情感标签，标签格式为<>。
  - Please transcribe the following audio and append age, gender, style, emotion labels at the end of the transcript, in the format of <>.
  - Please transcribe the audio and add age, gender, style, emotion labels at the end of the transcript, in the format of <>.
  - Please transcribe the audio content and include age, gender, style, emotion labels at the end of the text, in the format of <>.
  - Please transcribe the following audio and append age, gender, style, emotion tags at the end of the text, in the format of <>.
  - Please transcribe the audio and add tags for age, gender, style, emotion at the end of the transcript, in the format of <>.
  - Please transcribe this audio and include age, gender, style, emotion labels at the end, in the format of <>.
  - Please transcribe the audio content and at the end add labels for age, gender, style, emotion, in the format of <>.
  - Please transcribe the following audio and attach age, gender, style, emotion labels at the end, in the format of <>.
  - Please transcribe this audio and add labels for age, gender, style, emotion, in the format of <>.
  - Please transcribe the following audio and at the end, mark with age, gender, style, emotion labels, in the format of <>.
  - Please transcribe the audio and append age, gender, style, emotion labels in the format of <> at the end.
  - Please transcribe the audio and add the labels of age, gender, style, emotion at the end, in the format of <>.
  - Please transcribe this audio and append tags for age, gender, style, emotion, in the format of <>.
  - Please transcribe the following audio and include age, gender, style, emotion tags at the end in the format of <>.
  - Please transcribe this audio and mark the end with labels for age, gender, style, emotion in the format of <>.
  - Please transcribe the following audio and attach the tags of age, gender, style, emotion at the end, in the format of <>.
  - Please transcribe the audio and at the end of the transcript add age, gender, style, emotion tags in the format of <>.
  - Please transcribe this audio content and mark the end with labels for age, gender, style, emotion in the format of <>.
  - Please transcribe the following audio and at the end of the transcript, include the tags of age, gender, style, emotion in the format of <>.
  - Please transcribe this audio and append the labels for age, gender, style, emotion at the end, in the format of <>.
  - Please transcribe the following audio and include age, gender, style, emotion labels at the end in the format of <>.
  - Please transcribe this audio and append the labels of age, gender, style, emotion at the end in the format of <>.
  - Please transcribe the following audio and add the age, gender, style, emotion tags at the end of the text, in the format of <>.
  - Please transcribe the audio and mark the end with labels for age, gender, style, emotion in the format of <>.
  - Please transcribe this audio and at the end append age, gender, style, emotion tags, in the format of <>.
  - Please transcribe the audio and at the end of the transcript include the labels for age, gender, style, emotion in the format of <>.
  - Please transcribe this audio and add labels for age, gender, style, emotion at the end, in the format of <>.
  - Please transcribe the audio content and at the end, include the labels for age, gender, style, emotion in the format of <>.
  - Please transcribe the following audio and add labels for age, gender, style, emotion at the end of the transcript in the format of <>.
  - Please transcribe this audio content and include tags for age, gender, style, emotion at the end, in the format of <>.
  - Please transcribe the audio and mark with age, gender, style, emotion tags at the end in the format of <>.
  - Please transcribe this audio and at the end of the text, include age, gender, style, emotion labels in the format of <>.
  - Please transcribe the following audio and append labels for age, gender, style, emotion at the end in the format of <>.

<TRANSCRIBE> <CAPTION> <AGE> <GENDER> <EMOTION> <STYLE>:
  - 请将以下音频内容进行转录，并在转录完成的文本末尾分别附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将下列音频内容进行转录，并在转录文本的末尾分别添加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录以下音频内容，并在转录后的文本末尾标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将以下音频内容转录，并在转录完成后，附加音频事件、年龄、性别、情感、风格五个标签，标签格式为<>。
  - 请对以下音频内容进行转录，并在文本结尾分别添加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频内容转录，并在文本末尾添加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对音频进行转录，并在转录后添加音频事件、年龄、性别、情感、风格五个标签，标签格式为<>。
  - 请将音频内容转录，并在文本末尾标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频中的内容转录出来，并在最后附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将下列音频内容转录，并在最后标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对音频进行转录，并在结尾添加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录这段音频，并在转录结果的末尾加上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将以下语音内容转写，并附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频内容转写并标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请把音频内容转录，并在文本后标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录下列音频，并标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对以下音频内容进行文字转录，并在转录的末尾标明音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录此音频，并在文本的最后添加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录以下语音，并在文本结尾附上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将这段音频转录出来，并在末尾添加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将此音频转录，并在末尾加上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对这段音频进行转录，并在转录后附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将以下音频进行转录，并在文本末尾标记音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对音频内容进行转录，并在文本末尾标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将这段音频转录，并在文本的末尾附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录此段音频并附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对以下音频进行转录，标注音频事件、年龄、性别、情感、风格四个标签，标签格式为<>。
  - 请将音频转录出来，并在转录后的文本末尾附上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将下列音频转录，并在最后标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录音频并在最后加上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频内容转写，并在文本结尾附上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将此音频内容转录，并在结尾附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将下列音频转录，并在最后标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对以下音频内容进行转录，并在最后添加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录音频并在转录文本末尾添加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频内容转录出来，并在转录后的文本末尾附上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录音频，并在文本结尾标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频中的内容转录，并在文本的结尾附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录以下音频，并在文本末尾标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对这段音频进行转录，并在转录后的文本末尾加上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将这段音频内容转写，并在转录后的文本末尾附上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将以下音频转录，并在结尾附上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对这段音频进行转录，并在文本末尾标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将此音频转录，并在文本结尾附上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录此音频并附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对以下音频进行转录并标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将下列音频内容转录，并在结尾附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录音频并标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频内容转录，并在文本末尾附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将这段音频内容转写并在结尾添加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将这段音频转录，并在文本的末尾附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频转录并标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将以下音频转录，并在文本结尾标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请转录这段音频并在结尾加上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请对下列音频进行转录，并在文本结尾附上音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频内容转写，并在文本末尾标注音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频转录，并在最后附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - 请将音频转录出来，并在转录文本的末尾附加音频事件、年龄、性别、情感、风格标签，标签格式为<>。
  - Please transcribe the following audio and append audio event, age, gender, emotion, style labels at the end of the transcript, in the format of <>.
  - Please transcribe the audio and add audio event, age, gender, emotion, style labels at the end of the transcript, in the format of <>.
  - Please transcribe the audio content and include audio event, age, gender, emotion, style labels at the end of the text, in the format of <>.
  - Please transcribe the following audio and append audio event, age, gender, emotion, style tags at the end of the text, in the format of <>.
  - Please transcribe the audio and add tags for audio event, age, gender, emotion, style at the end of the transcript, in the format of <>.
  - Please transcribe this audio and include audio event, age, gender, emotion, style labels at the end, in the format of <>.
  - Please transcribe the audio content and at the end add labels for audio event, age, gender, emotion, style, in the format of <>.
  - Please transcribe the following audio and attach audio event, age, gender, emotion, style labels at the end, in the format of <>.
  - Please transcribe this audio and add labels for audio event, age, gender, emotion, style, in the format of <>.
  - Please transcribe the following audio and at the end, mark with audio event, age, gender, emotion, style labels, in the format of <>.
  - Please transcribe the audio and append audio event, age, gender, emotion, style labels in the format of <> at the end.
  - Please transcribe the audio and add the labels of audio event, age, gender, emotion, style at the end, in the format of <>.
  - Please transcribe this audio and append tags for audio event, age, gender, emotion, style, in the format of <>.
  - Please transcribe the following audio and include audio event, age, gender, emotion tags at the end in the format of <>.
  - Please transcribe this audio and mark the end with labels for audio event, age, gender, emotion in the format of <>.
  - Please transcribe the following audio and attach the tags of audio event, age, gender, emotion, style at the end, in the format of <>.
  - Please transcribe the audio and at the end of the transcript add audio event, age, gender, emotion tags in the format of <>.
  - Please transcribe this audio content and mark the end with labels for audio event, age, gender, emotion in the format of <>.
  - Please transcribe the following audio and at the end of the transcript, include the tags of audio event, age, gender, emotion in the format of <>.
  - Please transcribe this audio and append the labels for audio event, age, gender, emotion at the end, in the format of <>.
  - Please transcribe the following audio and include audio event, age, gender, emotion labels at the end in the format of <>.
  - Please transcribe this audio and append the labels of audio event, age, gender, emotion at the end in the format of <>.
  - Please transcribe the following audio and add the audio event, age, gender, emotion tags at the end of the text, in the format of <>.
  - Please transcribe the audio and mark the end with labels for audio event, age, gender, emotion in the format of <>.
  - Please transcribe this audio and at the end append audio event, age, gender, emotion tags, in the format of <>.
  - Please transcribe the audio and at the end of the transcript include the labels for audio event, age, gender, emotion in the format of <>.
  - Please transcribe this audio and add labels for audio event, age, gender, emotion at the end, in the format of <>.
  - Please transcribe the audio content and at the end, include the labels for audio event, age, gender, emotion in the format of <>.
  - Please transcribe the following audio and add labels for audio event, age, gender, emotion at the end of the transcript in the format of <>.
  - Please transcribe this audio content and include tags for audio event, age, gender, emotion at the end, in the format of <>.
  - Please transcribe the audio and mark with audio event, age, gender, emotion tags at the end in the format of <>.
  - Please transcribe this audio and at the end of the text, include audio event, age, gender, emotion labels in the format of <>.
  - Please transcribe the following audio and append labels for audio event, age, gender, emotion at the end in the format of <>.



